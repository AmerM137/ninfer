# Qwen3.8-27B DFlash2 Op 修正与重新 qualification checklist

> 状态：进行中，OP-01..17 已完成，OP-18..37 待完成。首轮实现中的多个合同和索引固定了推荐的七个 draft token。
> 本清单管理正式 Engine 接入前的 `K=1..15` 治理；各项完成情况和证据在对应条目内记录。
>
> 算法由
> [`2026-09-04-qwen3.8-27b-dflash2-algorithm.md`](2026-09-04-qwen3.8-27b-dflash2-algorithm.md)
> 定义，跨阶段集成由
> [`2026-09-04-qwen3.8-27b-dflash2-support-plan.md`](2026-09-04-qwen3.8-27b-dflash2-support-plan.md)
> 管理，权重与 format 以
> [`qwen3.8-27b-artifact.md`](qwen3.8-27b-artifact.md) 为准。

## 1. 范围与完成含义

当前问题是将 checkpoint 推荐配置 `block_size=8` 误用为 Op 数学域、kernel 索引和性能域。
本轮要完成受影响合同、wrapper、workspace query、dispatch、kernel、独立 oracle 和 benchmark
的对应修正，并对已有合法 routes 在新产品点重新 qualification。所有实现继续由原 Op family
拥有，不添加 target-private 平行路径，也不按 K 建立不同的 public Op。

本文逐项列出 37 个 Op/profile 工作项。OP-01..08 必须修改合同/实现并重新 qualification；
OP-09..32 已具有所需 extent，需复核数值/状态与性能并按证据修改原 routes；OP-33..37 为
准备、提交相关的直接复用项，重点验证新宽度的可观察行为，不要求无依据地重写或调优。
表格中一个 Op 的多个已注册格式均属于该项，关闭时分别说明其验证结果。

旧 K=7 的实现和测量可作为同点基线，不能沿用“全域已完成”的结论。下文列出的现有阈值
用于定位待比较的分界，不是重新调优后的固定方案。没有测量支持时，不宣称某个候选更快。

正式 Engine schedule、binder 接线、Program state/workspace composition、Graph inventory 和
端到端 acceptance/吞吐属于后续集成。本文只要求 Op 自身在代表性合法输入上可独立调用、
验证和 capture/replay。改变运行 K 不要求重新下载 source、转换 artifact、repack 权重或
引入新的量化格式。

## 2. 统一 workload、索引与数值边界

### 2.1 运行 extent

| 符号 | 含义 | 支持范围 |
|---|---|---|
| B | active requests | 1..8 |
| K | 启动选定的 draft count | 1..15，推荐 7 |
| W | proposal/verify 物理 block width | K+1，2..16 |
| T | backbone/target matrix columns | W*B，最大 128 |
| U | mask-head/selector matrix columns | K*B，最大 120 |
| P_b | 每请求可验证 draft extent | 0..K |
| Q_b | target live input columns | P_b+1 |
| A_b | accepted draft count | 0..P_b |
| L_b | provisional output count | A_b+1 |
| N_b | Frontend 最终提交数 | 0..L_b |
| W_c、B_c | context materialization 物理布局 | decode W_c=1..16、B_c=1..8；prefill W_c=1..2048、B_c=1 |
| T_c | context projection columns | W_c*B_c；prefill 最大 2048 |
| N_lane | pending-feature lane capacity | startup-fixed 1..8，且 >=B |
| S_state | StateImage slot capacity | startup-fixed，覆盖本轮 state selectors |

同一 Engine 的物理 W 由选定 K 决定；不能为全部配置计算有效 W=8 或 W=16 来规避扩展。
Proposal attention 可使用 valid_columns=W，target attention/GDN 使用 Q_b，context append
使用 counts=N_b。W_c 是 pending storage/gather extent，不能由 accepted draft count 替代；
all-accept 会提交 K+1 个 target input rows，必须保留最后一行 feature。

性能输入域以 `{(K+1)*B}` 和 `{K*B}` 的实际乘积集合定义。纯矩阵 Op 可以按相同列数去重；
卷积、attention、GDN 和状态 Op 必须保留 W/B 划分以及 device counts。已有 Linear 类 Op 的
任意正 T 合同继续成立；最大 128/120 是本轮产品性能范围，不是这些合同的新上限。

### 2.2 固定模型 geometry 与张量布局

```text
hidden H=5120; MLP I=17408; target feature H_f=25600
draft Q=32*128=4096; K/V=8*128=1024; five local layers; window/capacity=2048
conv groups=320; group width=16; taps=2; sides=2
selector candidates M=16; rank R=256
physical vocab=248320; valid global ids=0..248076; optimized head rows=131072

proposal residual       BF16 [5120,W,B]
finish_delta            BF16 [320,2,W,B]
packed mask hidden      BF16 [5120,U]
candidate ids/scores    I32/FP32 [16,K,B]
projected selector h    BF16 [256,K,B]
drafts / proposal q     I32 [K,B] / FP32 [16,K,B]
target logits           BF16 [248320,W,B]
pending features        BF16 [25600,W,N_lane]
```

Artifact Weight `[N,C]` 表示 `[output rows,input columns]`；runtime Tensor 的 ne[0] 连续。
Base kernel 保留 artifact `[side,tap,channel]=[2,2,5120]`，runtime view 为 `[5120,2,2]`；
codebook 保留 artifact `[vocab,rank]`，runtime view 为 `[256,248320]`。这些固定 8/16/256
等模型维度不能随运行 W/K 一起替换。

### 2.3 保持的数学与实现自由度

- Dynamic convolution 仅在当前 request block 内跨相邻位置，位置零没有 previous tap；
  finish 消费同一 prepare 的 output-side delta，无跨轮卷积状态。
- Proposal SWA 为 `abs(key_position-query_position)<2048`；当前 block 的有效 query 互相
  可见。Q/K 使用 plain head RMSNorm 和 theta=1e7 的 full-D128 split-half 1-D RoPE。
- Final norm-pack 跳过 anchor；top-16 使用 score 降序、同分 global-id 升序。Optimized
  head 的 row id 先映射为 global id，再供 selector 使用。
- Selector 是实际前驱条件下的逐位 walk；保留实际抽样的 FP32 q。逐 row、部分预计算、
  完整 lattice 均可作为同一 Op 的内部方案，不能换成独立逐位置 unary argmax 或 Viterbi。
- Sparse accept 使用实际 q 的 p/q rejection 和 `(p-q)+` correction；N 而非 P/A/L 决定
  最终持久状态提交。Proposal RNG purpose 与 target accept/correction/bonus 分离。
- Norm/conv prepare、projection/conv/residual、norm/RoPE、norm/pack、projection/top-k、
  selector walk 和五层 context materialization 保持闭合 public 边界。Private staging、
  reduction、tiling、launch 数由 qualified route 决定，不要求每个融合 Op 只有一个 kernel。
- Oracle 从 represented public inputs 及精确解码的 stored weights 计算完整公式；不可观察
  中间精度作为 implementation profile。OP-07 已去掉 raw K 的额外语义舍入；最终 BF16 K、
  `FP16_RNE(BF16)` V storage cast、public BF16 outputs 和实际 FP32 q 保持各自边界。

## 3. 必须修正合同和实现的 Ops

### OP-01 — rmsnorm_dynamic_grouped_conv_prepare

合同：[dynamic_grouped_conv.h](../../include/ninfer/ops/dynamic_grouped_conv.h)。落点：
`src/ops/wrapper/dynamic_grouped_conv.cpp`、`src/ops/dynamic_grouped_conv/bf16/`。

已修正的问题：合同和 wrapper 固定 W=8；plan 仅按 B 选择 split-K。Partial kernel 将八列 MMA tile
与 request batch 绑定，reduce 的 128 threads 正好只处理 8*16 个位置/通道。

- [x] 将 residual/prepared 改为 `[5120,W,B]`、finish_delta 改为 `[320,2,W,B]`，支持 W=2..16；
  workspace query 显式接收宽度域和 batch 域，和执行共用 route/layout 事实。
- [x] 分离 contraction 的 T 分片和卷积的 W 边界，改齐 partial/reduce 索引、shared storage、
  非满 tile 和线程覆盖；比较 split-K、norm staging/融合及 coefficient reduction 方案。
- [x] 从完整 norm→projection→input-conv 公式验证两个 public outputs；覆盖所有 W、不同 B、
  非零前一 request 末列和新 route seams；测完整 Op 的 launch 与 workspace 成本。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。独立 FP64 oracle 覆盖全部 120 个 W/B
组合；prepared/finish_delta 的既有 relative-L2/gross criteria 不变，输出 guard、输入保留、
scope 归还及 query high-water 均通过。W/B=3/3、7/7、14/7、16/8 另通过同址双次 Graph replay。

比较了六组 tiling/split 方案及其 RMS 缩放因子后置的两节点融合版本，共12个完整 Op 候选。
两种算术 profile 均先通过 oracle；保留 norm staging 的三节点方案更快，未采用的实例和
临时 sweep 接口已移除。最终 T<=48 使用 R16、ceil(T/8)*8 列、split-8；49..96 使用
R32/C32/split-4；97..128 使用 R32/C64/split-4。最大 workspace 为 2,621,440 bytes。

Public benchmark 已改为实际捕获 Graph，使用256 MiB L2 flush、8 warmup/60 repeats，遍历
全部 W/B。完整 Op median 如下；这是 Graph 测量，不能与旧 benchmark 的 eager-event 值混比。

| W | B=1 median us | B=8 median us |
|---:|---:|---:|
| 2 | 15.648 | 17.664 |
| 8 | 17.664 | 25.408 |
| 16 | 17.664 | 32.000 |

同条件下，B=8/W=9、12、16 相比直接扩宽的 R16 候选，从29.952/32.512/38.144 us
降至25.856/27.904/32.000 us。全域同 B 的相邻 W 最大增长为2.048 us；48→49列为
23.808→23.808 us，96→下一实际列数98为27.904→29.952 us，与候选曲线一致。
复现：`build/tests/ninfer_dynamic_grouped_conv_prepare_test` 和
`build/bench/ninfer_dynamic_grouped_conv_prepare_bench --warmup 8 --repeat 60`。

### OP-02 — linear_dynamic_grouped_conv_add

合同同 OP-01。落点：`src/ops/dynamic_grouped_conv/w8/` 和公共 wrapper。

已修正的问题：两个 C=4096/17408 profile 的 scratch、实例表和 dispatch 都基于 8B；finish 用
`col & 7` 判断 previous tap。旧 B-specific cache/K-warp 表不能覆盖新 W/B geometry。

- [x] 支持 x `[C,W,B]`、residual `[5120,W,B]` 和匹配 delta；按真实 request 内位置断开 tap。
  Query 覆盖两个 C profile 及全部选择域，不能仅将旧 capacity 乘二。
- [x] 重选小 T 与 65..128 列的 contraction；比较 private materialized projection 与融合
  finish epilogue 的完整 Op 成本。Raw projection 的 BF16 staging 仍是实现 profile。
- [x] 独立解码 signed W8/FP16 scales 后计算完整公式，检查 residual 原地更新、跨 request
  边界和 scope 归还。若采用 materialized route，原始 payload 为 `5120*W*B*2` bytes，
  最大 1.25 MiB；有额外 scratch 的其他 routes 分别计入实际 query。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。两种 C 各覆盖120个 W/B 组合；27个
跨 group、MMA tile 和矩阵区间的代表性输出行直接对完整 FP64 projection→conv→residual
oracle 比较，原 numerical criterion 不变。输出与精确容量 workspace 的 guards、输入/权重
保留和 scope high-water 检查通过。各最终 route 的代表点另验证同址双次 Graph replay，
每次 replay 前恢复 residual；共享 primitive 的 W8 LinearSwiGLU 和 attn_input_proj tests 通过。

比较了完整融合 epilogue、原 small-T 的分次 projection、普通 MMA、单次 launch 的并行
column tiles 和更宽的完整 tiles。Fusion 没有稳定收益，生产保留两节点 materialized Op；
移除了分次 projection 的65列台阶及未采用的 sweep/instances。W8 primitive 现在支持带
尾列屏蔽的二维 column grid，并统一拥有 shared-storage 类型；宽 tile 使用显式 shared
memory opt-in。88列 tile 的49,408-byte shared storage仍容纳两个CTA，96/128列候选未胜出。

最终两个 C profiles 均在 T<=64 使用 exact-small-T，65..72/73..80/81..88 使用
72/80/88列 tile。C=4096 的89..96使用并行32列 tile，97..128使用R64/C64/K128/A1；
C=17408 的89..128直接使用后者。所有 routes 使用同一 `[5120,W,B]` BF16 scratch，
最大1,310,720 bytes；finish 始终按真实 W 重置 previous tap。

Public cold-cache Graph benchmark：256 MiB L2 flush、8 warmup/60 repeats，全部240个
C/W/B点，均为2个Graph nodes。代表性完整 Op median：

| C | W | B=1 us | B=8 us |
|---:|---:|---:|---:|
| 4096 | 2 | 19.744 | 21.760 |
| 4096 | 8 | 21.760 | 40.192 |
| 4096 | 16 | 21.856 | 62.720 |
| 17408 | 2 | 66.848 | 69.376 |
| 17408 | 8 | 68.672 | 165.856 |
| 17408 | 16 | 69.408 | 236.800 |

C=17408 的64→65列为165.856→173.312 us，分次投影候选在65列约218..222 us；
T=128为236.800 us，分次投影候选约331 us。C=4096的相同最大点为62.720 us，分次
投影候选约77 us。推荐W=8的代表点保持在原测量波动范围。
同B相邻W最大增长：C=4096为B8的88→96列，50.432→58.624 us；C=17408为B8的
48→56列，约26.208 us增长。前者跨过两CTA宽tile的资源边界，后者包含8列计算量增长；
上述点的候选比较未提供更快且连续的替代，不声称全曲线无台阶。
复现：`build/tests/ninfer_linear_dynamic_grouped_conv_add_test` 和
`build/bench/ninfer_linear_dynamic_grouped_conv_add_bench --warmup 8 --repeat 60`。

### OP-03 — rmsnorm_rope（pair；single 作为 context 依赖）

合同：[rmsnorm_rope.h](../../include/ninfer/ops/rmsnorm_rope.h)。落点：`src/ops/rmsnorm_rope/`。

已修正的问题：Pair contract/wrapper 固定 `[128,32/8,8,B]` 和八列 positions；底层 launcher 已接受
token 数。Single T=1..2048 已覆盖 context，不能因改 pair 而破坏其范围。

- [x] Pair 使用真实 W 和 W*B，接纳 W=2..16、B=1..8，维持 Q/K 原地写入与 positions 对齐。
- [x] 用完整 norm→RoPE FP64 oracle 验证新 pair extent、绝对大位置及 single 的受影响路径；
  比较每 token CTA 的 head 分工在短 W 和大 batch 的代价，保留有证据的方案。

完成证据：RTX 5090、CUDA 13.1、Release/sm_120a。Pair 全部120个 W/B 组合及single
T=1/8/64/1024/2048直接通过完整FP64 oracle，原relative-L2和pair-scaled准则不变；
positions=0和接近262144的绝对位置、输入weight/positions保留及输出guards通过。
Pair W/B=3/3、16/8和single T=1/2048另通过恢复输入后的同址双次Graph replay。

比较6种pair与4种single head/warp分工。最终每CTA处理同一token的8个head、每warp一个
head；pair用4个Q CTA和1个K CTA，single保留1个K CTA，每CTA只缓存自身的norm weight。
仍为一次public调用、一个kernel、零workspace；未采用的分工和临时sweep已删除。

Public benchmark采用warm cache、8 warmup/60 repeats，每个Graph串联32次Op并按次数归一化，
避免逐次host launch间隙主导微秒级Op测量。Pair代表性median：

| W | B=1 us | B=8 us |
|---:|---:|---:|
| 2 | 2.015 | 2.023 |
| 8 | 2.001 | 2.185 |
| 16 | 2.020 | 2.429 |

同条件原40-head/CTA候选在W8/B1、W16/B8为3.242/3.294 us；最终为2.001/2.429 us。
全部pair点同B相邻W最大增长0.093 us，无新增route seam。Single T=1/128/1024/2048为
1.981/2.075/3.029/4.074 us，没有引入针对噪声点的切换阈值。
复现：`build/tests/ninfer_rmsnorm_rope_test`，以及
`build/bench/ninfer_rmsnorm_rope_bench --form pair --warmup 8 --repeat 60`（single改用`--form single`）。

### OP-04 — rmsnorm_pack_tail

合同：[rmsnorm_pack_tail.h](../../include/ninfer/ops/rmsnorm_pack_tail.h)。落点：
`src/ops/wrapper/rmsnorm_pack_tail.cpp`、`src/ops/launcher/rmsnorm_pack_tail.cu`、
`src/ops/kernel/rmsnorm_pack_tail.cuh`。

问题：输入 stride=8、输出 stride=7、grid 和 oracle fixture 均写死推荐 block。

- [x] 定义 `[5120,W,B] -> [5120,(W-1)*B]`，按 `out[:,K*b+i-1]=norm(in[:,i,b])`
  处理 i=1..K；使用真实 W/K，anchor 不参与读取、归约或输出。
- [x] 用独立 norm/索引 oracle 覆盖短、奇数、最大 W 和跨 request pack；复核 CTA/thread
  配置与完整 launch 时间，不额外物化一份完整 normalized block。

完成证据（RTX 5090 / sm_120a，CUDA 13.1）：公开 Op 全部 120 个 W/B 组合通过独立
FP64 norm + pack oracle，沿用 relative-L2 1.85e-3 / gross-relative 3.4e-3 准则。
Anchor 使用可区分数据；输入/weight 保持不变、输出 guards、W3/B3 与 W16/B8 的两次
同址 Graph replay 均通过。无 workspace、无额外 normalized block，每次 Op 一个 kernel。

比较 128/256/512-thread CTA 的完整 public-equivalent 归约，三个算术配置分别通过 oracle；
120 点平均 median 为 1.820/1.810/1.805 us，差异没有稳定超过测量波动，因此保留统一
512-thread CTA，移除候选入口。公开 benchmark 使用 warm-cache Graph，每张图连续 32 次 Op，
warmup=8/repeat=60，按每次 Op 归一化。最终逐点 median（us，列为 B）如下：

| W | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 2 | 1.623 | 1.750 | 1.731 | 1.729 | 1.740 | 1.756 | 1.732 | 1.748 |
| 3 | 1.755 | 1.729 | 1.741 | 1.741 | 1.743 | 1.760 | 1.733 | 1.739 |
| 4 | 1.722 | 1.750 | 1.725 | 1.749 | 1.737 | 1.763 | 1.776 | 1.742 |
| 5 | 1.742 | 1.739 | 1.731 | 1.739 | 1.729 | 1.744 | 1.727 | 1.738 |
| 6 | 1.729 | 1.768 | 1.773 | 1.740 | 1.752 | 1.733 | 1.722 | 1.812 |
| 7 | 1.747 | 1.738 | 1.746 | 1.748 | 1.725 | 1.736 | 1.738 | 1.806 |
| 8 | 1.730 | 1.765 | 1.763 | 1.730 | 1.772 | 1.736 | 1.805 | 1.803 |
| 9 | 1.735 | 1.742 | 1.730 | 1.752 | 1.824 | 1.853 | 1.773 | 1.841 |
| 10 | 1.729 | 1.743 | 1.736 | 1.760 | 1.832 | 1.835 | 1.832 | 1.912 |
| 11 | 1.730 | 1.731 | 1.720 | 1.748 | 1.823 | 1.866 | 1.847 | 1.861 |
| 12 | 1.752 | 1.745 | 1.733 | 1.777 | 1.825 | 1.841 | 1.871 | 1.913 |
| 13 | 1.738 | 1.747 | 1.746 | 1.832 | 1.965 | 1.994 | 2.056 | 1.967 |
| 14 | 1.715 | 1.750 | 1.744 | 1.825 | 1.839 | 1.940 | 1.967 | 1.967 |
| 15 | 1.764 | 1.768 | 1.782 | 1.815 | 1.833 | 1.898 | 2.002 | 1.970 |
| 16 | 1.744 | 1.818 | 1.805 | 1.819 | 1.897 | 1.956 | 1.980 | 2.069 |

最大同 B 相邻 W 增量为 W12→13/B7 的 0.185 us；统一 route 没有 dispatch seam，
该点落在本轮测量波动内，不据此增加阈值，也不声称归约本身取得可辨别加速。
复现：`build/tests/ninfer_rmsnorm_pack_tail_test`；
`build/bench/ninfer_rmsnorm_pack_tail_bench --warmup 8 --repeat 60`。

### OP-05 — linear_topk（三个 head profiles）

合同：[linear_topk.h](../../include/ninfer/ops/linear_topk.h)。落点：`src/ops/linear_topk/`。

问题：Wrapper 从 U%7 推断 B，workspace 只查询 B；kernel 只覆盖旧 `7,14,...,56` 集合。
W8/FP8/Q4 的若干 launcher 在未命中时直接调用固定 56 列实例，放宽 wrapper 会错误读写。

- [x] 将 public contract 定义为逐矩阵列独立的 projection-top16，hidden `[5120,U]`，
  outputs `[16,U]`；caller 可 view 成 `[16,K,B]`。列轴支持任意正 extent，workspace 按
  column interval 查询；本轮产品优化域为 U=K*B<=120。
- [x] 分别覆盖 W8/row-FP8 full `[248320,5120]` 和 Q4 optimized `[131072,5120]`；
  重建完整 dispatch、masked tail、column tiling 和 merge，删除将任意列数送入旧 exact
  instance 的分支。不得只补 U=120 或保留固定七列的平行 public overload。
- [x] 比较小 U K-split、grouped row reduction、更大 U MMA 和 merge。当前 full U=7
  producer 每 16 个 vocabulary rows 写 16 个 64-bit keys，partial plane 仍约 13.26 MiB；
  Q4 U=7/14 也逐 vocabulary row 写 key。计入实际流量，不把“不公开 dense logits”当作
  已实现内存压缩的证据；比较更大 row group 内先选 top-16 的完整开销。
- [x] 三个 profiles 各自用精确 weight decode 的独立数学 oracle 验证 scores、global-id
  tie、padding 排除和 id map。浮点边界按数值准则，精确 tie/构造的稳定候选按 exact 准则；
  补全部新增 route、非整 tile、workspace 峰值与同址 Graph replay。

完成证据（RTX 5090 / sm_120a，CUDA 13.1）：合同与 workspace 查询以独立矩阵列 U 为轴，
不再推断 K/B。较长矩阵顺序处理不超过 128 列的 chunk，复用同一 caller workspace；
所有 MMA loads、组内排序和 outputs 都使用实际 live columns，已删除固定 56 列兜底。
W8/FP8 的 grouped K-split 归入各自 producer 文件，旧直接写满词表 keys 的 full-head 路径被移除。

最终 producer（R 为每 CTA 归约的 vocabulary rows，C 为列 tile，Ktile 为 contraction tile）：

| U | W8 full | row-FP8 full | Q4 optimized |
|---|---|---|---|
| 1..16 | grouped K-split，R128 | grouped K-split，R128 | K-split，R16，逐行 key |
| 17..24 | grouped K-split，R128 | grouped K-split，R128 | MMA R64/C32/K128 |
| 25..32 | MMA R128/C32/K128 | MMA R64/C32/K128 | MMA R64/C32/K128 |
| 33..40 | MMA R64/C40/K128 | MMA R64/C40/K128 | MMA R64/C48/K128 |
| 41..48 | MMA R64/C48/K128 | MMA R64/C48/K128 | MMA R64/C48/K128 |
| 49..64 | MMA R64/C64/K128 | MMA R64/C64/K128 | MMA R64/C64/K128 |
| 65..80 | MMA R64/C80/K128 | MMA R64/C80/K128 | MMA R64/C80/K128 |
| 81..88 | MMA R64/C96/K128 | MMA R64/C96/K128 | MMA R64/C96/K128 |
| 89..96 | MMA R64/C96/K128 | MMA R64/C96/K64 | MMA R64/C96/K128 |
| 97..112 | MMA R64/C112/K64 | MMA R64/C112/K64 | MMA R64/C112/K64 |
| 113..120 | MMA R64/C120/K64 | MMA R64/C120/K64 | MMA R64/C120/K64 |
| 121..128 / chunk | MMA R64/C128/K64 | MMA R64/C128/K64 | MMA R64/C128/K64 |

C120 使用三个 column warps；C64/C80/C96/C112/C128 使用两个。Full-head grouped K-split
的 U=7 partial plane 从 13.26 MiB 降至 1.66 MiB，总 workspace 从 14,355,200 降至
1,794,844 bytes。同条件 Graph 的直接写 key 候选在 W8/FP8 U7 为 892.704/841.184 us，
最终为 855.200/806.112 us。Q4 grouped K-split 的 R64/R128 在 U7 为 314.400/338.976 us，
均慢于保留的逐行 key 路径约 289 us；因此 Q4 小 U 仍物化 dense key plane，不将其描述为压缩输出。

比较覆盖 K-split、不同 vocabulary row group、MMA 列 tile/K tile/warp 分配以及完整 merge。
额外比较过 R32/R48、直接解码权重与异步预取；未获选的实例、knobs 和私有 sweep 入口均已删除。
候选 C128/K64/R64 的定向 NCU 显示约 32.7% achieved occupancy、数学 pipe throttle，
用于定位 producer 的计算发射限制；该候选指标不冒充最终 C120 route 的指标。
公开时延均来自未开启 profiler 的完整 Op Graph。初始扩展候选在 U120 的 W8/FP8/Q4 为
2935.584/2989.824/1531.680 us，最终为 2066.016/2025.280/1203.232 us。

三种 profiles 各自通过 U=1..120、121/127/128/129/257 的独立 FP64 oracle。Fixture 包含
带符号 codes、非整齐 FP16 scales、BF16 row scales、分散非零行和零背景；oracle 精确解码
实际存储 scale，不复刻 BF16 weight staging 或 reduction tree。Scores 沿用 relative-L2
1/256、gross absolute 1/256、gross-relative 2/256 准则。构造的稳定 top-16、全词表零分 tie、
padding 排除、逆序 global-id map 按 exact 准则；逐点输出/input guards、只读 hidden/id map、
精确容量 workspace guards 与 scope 释放通过。每种 profile 的各条路线和边界均进行两次
同址 Graph replay，包含 129 列跨 chunk 的 counter 重置和 workspace 复用。
产品域 U<=120 的 workspace 峰值为 full 61,532,640、optimized 32,471,520 bytes；
任意更长 U 的 query 上界分别为 65,634,816 / 34,636,288 bytes，129/257 列已实测执行。

最终 cold-cache Graph，256 MiB eviction、warmup=8/repeat=60，逐点 median（us）：

| U | W8 full | row-FP8 full | Q4 optimized |
|---|---|---|---|
| 1 | 854.624 | 806.112 | 259.072 |
| 2 | 853.280 | 807.936 | 263.104 |
| 3 | 855.008 | 807.936 | 265.472 |
| 4 | 856.512 | 809.952 | 269.280 |
| 5 | 855.040 | 803.840 | 277.504 |
| 6 | 855.040 | 805.440 | 283.648 |
| 7 | 855.200 | 806.112 | 288.544 |
| 8 | 855.072 | 805.856 | 293.888 |
| 9 | 869.856 | 809.984 | 322.368 |
| 10 | 870.144 | 809.984 | 326.688 |
| 11 | 871.424 | 809.920 | 331.008 |
| 12 | 871.424 | 811.840 | 340.384 |
| 13 | 873.440 | 814.112 | 351.232 |
| 14 | 873.472 | 816.128 | 357.376 |
| 15 | 875.968 | 818.208 | 363.520 |
| 16 | 875.552 | 836.224 | 369.664 |
| 17 | 898.528 | 855.040 | 394.272 |
| 18 | 900.096 | 857.088 | 396.288 |
| 19 | 902.880 | 861.024 | 396.288 |
| 20 | 906.240 | 863.264 | 398.336 |
| 21 | 912.416 | 869.344 | 400.000 |
| 22 | 916.480 | 875.520 | 400.352 |
| 23 | 922.528 | 881.664 | 400.384 |
| 24 | 928.768 | 887.648 | 400.384 |
| 25 | 934.848 | 902.144 | 404.288 |
| 26 | 934.944 | 904.224 | 406.528 |
| 27 | 939.008 | 908.192 | 406.496 |
| 28 | 941.088 | 912.256 | 406.560 |
| 29 | 943.104 | 904.992 | 408.224 |
| 30 | 945.152 | 907.008 | 408.576 |
| 31 | 947.328 | 910.336 | 409.376 |
| 32 | 948.896 | 911.136 | 410.624 |
| 33 | 984.096 | 949.248 | 457.792 |
| 34 | 985.888 | 949.760 | 459.776 |
| 35 | 990.208 | 955.264 | 461.824 |
| 36 | 990.208 | 957.472 | 461.824 |
| 37 | 992.256 | 959.520 | 463.872 |
| 38 | 994.304 | 961.536 | 465.920 |
| 39 | 995.072 | 963.616 | 468.000 |
| 40 | 996.352 | 963.584 | 468.000 |
| 41 | 1078.208 | 1020.928 | 474.112 |
| 42 | 1079.936 | 1027.072 | 476.160 |
| 43 | 1090.592 | 1030.656 | 478.496 |
| 44 | 1094.624 | 1035.296 | 480.544 |
| 45 | 1100.800 | 1041.696 | 482.304 |
| 46 | 1103.712 | 1045.504 | 484.224 |
| 47 | 1102.880 | 1049.600 | 486.400 |
| 48 | 1106.592 | 1051.680 | 488.480 |
| 49 | 1164.320 | 1209.344 | 632.096 |
| 50 | 1166.272 | 1207.616 | 633.856 |
| 51 | 1170.432 | 1205.120 | 635.872 |
| 52 | 1172.448 | 1209.344 | 637.760 |
| 53 | 1176.576 | 1211.392 | 639.584 |
| 54 | 1184.384 | 1215.296 | 642.144 |
| 55 | 1186.848 | 1217.536 | 644.096 |
| 56 | 1188.864 | 1217.568 | 644.896 |
| 57 | 1203.232 | 1237.984 | 652.288 |
| 58 | 1205.536 | 1240.064 | 654.336 |
| 59 | 1211.488 | 1235.936 | 658.400 |
| 60 | 1217.568 | 1238.272 | 658.848 |
| 61 | 1217.568 | 1239.712 | 661.248 |
| 62 | 1226.496 | 1242.080 | 662.560 |
| 63 | 1223.680 | 1242.144 | 664.480 |
| 64 | 1227.680 | 1239.744 | 666.752 |
| 65 | 1359.040 | 1389.792 | 756.736 |
| 66 | 1363.712 | 1393.632 | 757.024 |
| 67 | 1366.560 | 1395.232 | 758.784 |
| 68 | 1369.536 | 1400.096 | 760.736 |
| 69 | 1375.232 | 1401.824 | 760.864 |
| 70 | 1379.168 | 1406.272 | 764.832 |
| 71 | 1379.808 | 1408.224 | 766.944 |
| 72 | 1383.392 | 1414.176 | 767.008 |
| 73 | 1399.424 | 1446.944 | 776.928 |
| 74 | 1399.968 | 1442.752 | 777.248 |
| 75 | 1412.032 | 1452.800 | 781.344 |
| 76 | 1412.096 | 1459.200 | 783.360 |
| 77 | 1419.968 | 1469.472 | 785.504 |
| 78 | 1424.384 | 1475.392 | 787.488 |
| 79 | 1420.096 | 1483.552 | 789.344 |
| 80 | 1427.968 | 1477.792 | 791.808 |
| 81 | 1606.656 | 1700.416 | 896.000 |
| 82 | 1608.736 | 1700.608 | 896.032 |
| 83 | 1610.464 | 1702.944 | 898.048 |
| 84 | 1602.368 | 1707.008 | 900.096 |
| 85 | 1617.376 | 1709.088 | 902.272 |
| 86 | 1610.784 | 1713.152 | 904.416 |
| 87 | 1627.168 | 1716.096 | 906.528 |
| 88 | 1614.656 | 1717.248 | 909.056 |
| 89 | 1643.328 | 1645.856 | 916.960 |
| 90 | 1644.288 | 1645.376 | 918.176 |
| 91 | 1649.664 | 1649.632 | 920.576 |
| 92 | 1661.856 | 1649.472 | 920.608 |
| 93 | 1657.888 | 1649.952 | 922.752 |
| 94 | 1661.952 | 1653.696 | 926.624 |
| 95 | 1652.000 | 1653.696 | 934.912 |
| 96 | 1667.904 | 1653.760 | 928.800 |
| 97 | 1957.376 | 1699.616 | 986.112 |
| 98 | 1988.192 | 1713.184 | 987.968 |
| 99 | 1987.040 | 1707.168 | 990.208 |
| 100 | 1983.232 | 1708.960 | 992.128 |
| 101 | 1986.336 | 1711.136 | 992.256 |
| 102 | 1994.528 | 1719.200 | 993.056 |
| 103 | 1988.896 | 1709.632 | 994.304 |
| 104 | 1994.432 | 1721.408 | 996.352 |
| 105 | 2004.672 | 1739.456 | 1002.144 |
| 106 | 2011.168 | 1737.472 | 1002.496 |
| 107 | 2013.216 | 1737.728 | 1004.512 |
| 108 | 2014.976 | 1743.680 | 1006.432 |
| 109 | 2019.008 | 1758.112 | 1008.640 |
| 110 | 2025.760 | 1748.000 | 1010.880 |
| 111 | 2025.440 | 1751.872 | 1010.688 |
| 112 | 2028.000 | 1772.736 | 1010.688 |
| 113 | 2057.984 | 1996.800 | 1189.664 |
| 114 | 2080.576 | 2010.944 | 1190.944 |
| 115 | 2050.048 | 2007.040 | 1193.728 |
| 116 | 2049.664 | 2006.752 | 1199.904 |
| 117 | 2060.064 | 2004.480 | 1201.152 |
| 118 | 2056.224 | 2025.440 | 1203.200 |
| 119 | 2062.112 | 2024.896 | 1203.232 |
| 120 | 2066.016 | 2025.280 | 1203.232 |

W8/U114 的首个 60-sample 窗口为 2294.976 us（min 2013.216）；因相邻点无相同上升，
使用 180 samples 复测为 2080.576 us（表中值）。保留这一观测说明，未据此新增 dispatch。
候选比较使用 20/40/60 samples，保留路线由上述 public curve 复核；测量含桌面 GPU 的尾部波动。

所有 route/schedule seams 的相邻 U 增量（us；“—”表示该 profile 在此无切换）：

| U 变化 | W8 | FP8 | Q4 |
|---|---|---|---|
| 4→5 | -1.472 | — | — |
| 8→9 | +14.784 | +4.128 | +28.480 |
| 16→17 | +22.976 | +18.816 | +24.608 |
| 24→25 | +6.080 | +14.496 | — |
| 32→33 | +35.200 | +38.112 | +47.168 |
| 40→41 | +81.856 | +57.344 | — |
| 48→49 | +57.728 | +157.664 | +143.616 |
| 64→65 | +131.360 | +150.048 | +89.984 |
| 80→81 | +178.688 | +222.624 | +104.192 |
| 88→89 | — | -71.392 | — |
| 96→97 | +289.472 | +45.856 | +57.312 |
| 112→113 | +29.984 | +224.064 | +178.976 |

w8-full 最大相邻 U 增量为 U96→97 的 289.472 us；实际 K/B 网格最大同 B 增量为 B7/K13→14 的 338.528 us。
fp8-full 最大相邻 U 增量为 U112→113 的 224.064 us；实际 K/B 网格最大同 B 增量为 B8/K14→15 的 252.544 us。
q4-optimized 最大相邻 U 增量为 U112→113 的 178.976 us；实际 K/B 网格最大同 B 增量为 B8/K14→15 的 192.544 us。

剩余台阶对应更宽 MMA tile、Ktile 或 column-warp 分配切换；完整候选中更小 tile 的重复
weight read、更大 row group 的 shared-memory 驻留损失，以及 R32/R48/direct-decode
都未给出更快的完整 Op。80/112/120 列实例已缩小这些台阶，不声称整个区间无台阶，
也不将 Op 结果扩展为 Engine 加速结论。
复现：`build/tests/ninfer_linear_topk_test`；
`build/bench/ninfer_linear_topk_bench --warmup 8 --repeat 60`；
较长矩阵可用 `--columns 129`。

### OP-06 — candidate_selector_path

合同：[candidate_selector.h](../../include/ninfer/ops/candidate_selector.h)。落点：
`src/ops/wrapper/candidate_selector.cpp`、`src/ops/candidate_selector/bf16/`。

问题：合同、strides 和 kernel 七步展开固定 K=7；每 request 一个 512-thread CTA 的调度
只在旧宽度有证据。原先将“只算 realized row”固定为唯一 production 方案限制了比较范围。

- [x] 从输入读取 K=1..15，改齐 `[16,K,B]`、`[256,K,B]`、`[K,B]` 的索引和循环；
  保留固定 M=16/R=256，以及 `(seed,F+i,DFlash2ProposalPurpose)` 的位置 RNG。
- [x] 比较逐步 realized-row、提前 staging、部分/完整 edge 预计算后的条件 walk；包括
  K=1 的单步和 K=15 的串行链。若新增 private workspace，提供匹配 query；不预定赢家。
- [x] 独立 FP64 lattice oracle 再执行条件 walk，检查实际前驱下的 q；覆盖混合温度、
  greedy tie、依赖前驱而改变选择的案例和 compact-row 重排下的 RNG。q 输出必须就是
  实际抽样使用的 FP32 分布，不从其他 route 的 scores 重建。

完成证据（RTX 5090 / sm_120a，CUDA 13.1）：K=1..4 使用每 request 一个 512-thread
CTA 的 realized-row walk，不使用 workspace；K=5..15 使用 512-thread CTA 并行计算
各位置的完整 predecessor/candidate edge lattice，再由每 request 一个 warp 执行条件 walk。
Workspace query 覆盖 K/B 区间，最大为 `16*16*15*8*sizeof(float)=122880` bytes。
第一位置只计算 anchor 对应的 edge row；后续位置按前一步实际选中的 candidate rank 索引，
不是沿预先固定的前驱走。全部输出索引使用实际 K。

比较了 direct、提前 staging 全部 successor/可能 predecessor product、完整 edge 预计算，
以及各自的 256/512-thread 配置。六种候选均通过独立 FP64 lattice + conditional-walk
oracle；完整 Op 测量选出上述两条路线，删除其他实例和 sweep 入口。提前 staging 的额外
global traffic/launch 没有稳定收益；full lattice 的 256-thread 路线没有胜过统一 512-thread
方案的足够证据。抽样使用输出的同一 FP32 q，CDF 使用 warp prefix-scan；它是私有归约
方式，未将其浮点关联顺序加入数学合同。

最终 public 测试覆盖全部 120 个 K/B 组合，沿用 q 的 absolute 2e-6 + relative 2e-5
准则，稳定边界 fixture 的 draft ids 精确比较。另有 K1/K15 的 uniform/greedy tie
（候选 global ids 逆序，明确按 candidate rank 破 tie）、K2/K15 构造的前驱改变选择案例、
K1/K7/K15 的 compact-row `[7,0,4]` 重排，以及从实际输出 q 与 counter RNG 验证抽样关系。
输入、token_counts 保持不变，未使用的 SamplingConfig 字段改变不影响公式。
精确所需容量 workspace 的 guards、scope/peak 和输出 guards 通过；零需求路线用独立
fixture 的最小 backing arena，实际 peak 为零。各 K 在 B1/B8 进行同址 Graph replay；
K1/K7/K15、B8 还在同一 graph 中更新 seed/positions，验证新条件路径后恢复旧输入重放。

完整 cold-cache Graph（256 MiB eviction，warmup=8/repeat=60）逐点 median，单位 us：

greedy：

| K | B1 | B2 | B3 | B4 | B5 | B6 | B7 | B8 |
|---|---|---|---|---|---|---|---|---|
| 1 | 3.360 | 4.864 | 3.648 | 4.928 | 5.088 | 5.056 | 5.088 | 5.216 |
| 2 | 5.376 | 5.376 | 5.344 | 5.376 | 5.376 | 5.376 | 5.376 | 5.376 |
| 3 | 7.328 | 7.456 | 7.456 | 7.456 | 7.456 | 7.456 | 7.456 | 7.424 |
| 4 | 7.456 | 7.456 | 7.456 | 7.456 | 7.456 | 7.424 | 7.456 | 7.456 |
| 5 | 7.424 | 7.456 | 7.456 | 7.456 | 9.216 | 9.472 | 9.504 | 9.504 |
| 6 | 7.456 | 7.456 | 9.344 | 9.472 | 9.472 | 9.472 | 9.504 | 9.504 |
| 7 | 8.160 | 9.504 | 9.504 | 9.504 | 9.472 | 11.424 | 11.520 | 11.520 |
| 8 | 9.504 | 9.504 | 9.472 | 9.504 | 11.552 | 11.552 | 11.552 | 11.520 |
| 9 | 9.504 | 9.472 | 9.504 | 11.520 | 11.520 | 11.552 | 11.552 | 13.568 |
| 10 | 9.472 | 9.504 | 11.552 | 11.552 | 11.552 | 12.032 | 13.600 | 13.568 |
| 11 | 9.504 | 11.488 | 11.520 | 11.552 | 13.568 | 13.600 | 13.600 | 13.600 |
| 12 | 11.552 | 11.552 | 11.552 | 13.600 | 13.600 | 13.600 | 15.456 | 15.616 |
| 13 | 11.520 | 11.552 | 13.600 | 13.600 | 13.600 | 15.648 | 15.648 | 15.648 |
| 14 | 11.552 | 11.552 | 13.600 | 13.600 | 15.648 | 15.648 | 15.648 | 17.664 |
| 15 | 11.552 | 13.568 | 13.600 | 14.080 | 15.648 | 15.648 | 17.664 | 17.696 |

stochastic：

| K | B1 | B2 | B3 | B4 | B5 | B6 | B7 | B8 |
|---|---|---|---|---|---|---|---|---|
| 1 | 5.344 | 5.376 | 5.376 | 5.344 | 5.344 | 5.344 | 5.376 | 5.344 |
| 2 | 5.408 | 5.408 | 5.376 | 5.408 | 5.408 | 5.408 | 7.136 | 6.560 |
| 3 | 7.424 | 7.456 | 7.456 | 7.456 | 7.456 | 7.456 | 7.424 | 7.424 |
| 4 | 9.504 | 9.504 | 9.504 | 9.472 | 9.472 | 9.472 | 9.472 | 9.472 |
| 5 | 9.504 | 9.504 | 9.504 | 10.816 | 11.552 | 11.520 | 11.520 | 11.552 |
| 6 | 11.488 | 11.520 | 11.520 | 11.552 | 11.552 | 11.552 | 13.568 | 13.568 |
| 7 | 11.552 | 11.552 | 11.552 | 13.280 | 13.376 | 13.568 | 13.568 | 13.600 |
| 8 | 11.552 | 13.600 | 13.600 | 13.600 | 13.600 | 15.616 | 15.616 | 15.616 |
| 9 | 13.600 | 13.600 | 13.600 | 15.616 | 15.648 | 15.616 | 15.648 | 17.664 |
| 10 | 13.600 | 15.520 | 15.616 | 15.648 | 17.216 | 17.664 | 17.664 | 17.664 |
| 11 | 15.616 | 15.616 | 15.616 | 17.664 | 17.664 | 17.664 | 19.712 | 19.712 |
| 12 | 15.616 | 17.472 | 17.664 | 17.664 | 19.168 | 19.712 | 19.744 | 19.744 |
| 13 | 17.696 | 17.696 | 19.392 | 19.712 | 19.680 | 19.744 | 21.760 | 21.760 |
| 14 | 17.664 | 17.696 | 19.712 | 19.712 | 21.728 | 21.760 | 21.760 | 23.808 |
| 15 | 19.520 | 19.744 | 21.248 | 21.792 | 21.568 | 23.328 | 23.808 | 23.808 |

mixed：

| K | B1 | B2 | B3 | B4 | B5 | B6 | B7 | B8 |
|---|---|---|---|---|---|---|---|---|
| 1 | — | 5.376 | 5.344 | 5.376 | 5.376 | 5.376 | 5.312 | 5.344 |
| 2 | — | 5.408 | 5.408 | 5.408 | 5.440 | 5.408 | 5.440 | 5.440 |
| 3 | — | 7.456 | 7.456 | 7.456 | 7.456 | 7.456 | 7.424 | 7.424 |
| 4 | — | 9.472 | 9.472 | 9.472 | 9.472 | 9.472 | 9.440 | 9.472 |
| 5 | — | 9.472 | 9.504 | 9.504 | 11.520 | 11.552 | 11.552 | 11.552 |
| 6 | — | 11.520 | 11.552 | 11.552 | 11.552 | 11.552 | 13.600 | 13.600 |
| 7 | — | 11.520 | 11.552 | 11.552 | 13.440 | 13.600 | 13.568 | 13.600 |
| 8 | — | 13.600 | 13.600 | 13.568 | 13.600 | 15.648 | 15.648 | 15.648 |
| 9 | — | 13.600 | 13.600 | 15.648 | 15.648 | 15.648 | 15.616 | 17.664 |
| 10 | — | 15.488 | 15.648 | 15.648 | 17.280 | 17.664 | 17.664 | 17.664 |
| 11 | — | 15.584 | 15.616 | 17.696 | 17.664 | 17.664 | 19.712 | 19.744 |
| 12 | — | 17.216 | 17.664 | 17.664 | 19.584 | 19.712 | 19.744 | 19.744 |
| 13 | — | 17.568 | 19.456 | 19.712 | 19.712 | 19.744 | 21.760 | 21.760 |
| 14 | — | 17.696 | 19.712 | 19.712 | 21.760 | 21.760 | 21.760 | 23.808 |
| 15 | — | 19.744 | 20.672 | 21.760 | 21.760 | 23.424 | 23.808 | 23.808 |

唯一 route seam 为 K4→5，其逐 B 增量（us）如下；没有依赖噪声点增加 B 阈值。

| mode | B1 | B2 | B3 | B4 | B5 | B6 | B7 | B8 |
|---|---|---|---|---|---|---|---|---|
| greedy | -0.032 | +0.000 | +0.000 | +0.000 | +1.760 | +2.048 | +2.048 | +2.048 |
| stochastic | +0.000 | +0.000 | +0.000 | +1.344 | +2.080 | +2.048 | +2.048 | +2.080 |
| mixed | — | +0.000 | +0.032 | +0.032 | +2.048 | +2.080 | +2.112 | +2.080 |

greedy 最大同 B 相邻 K 增量：B3/K2→3，2.112 us。
stochastic 最大同 B 相邻 K 增量：B4/K14→15，2.080 us。
mixed 最大同 B 相邻 K 增量：B7/K4→5，2.112 us。

本机短 Graph 计时存在约 2 us 的台阶和窗口波动，因此没有用微小差异拟合更多切换点。
原推荐点 K7/B1 的 greedy/stochastic 为 11.520/13.568 us，最终为 8.160/11.552 us；
K7/B8 的 stochastic 与原路线相当。K15/B1 的 direct 候选为 21.760/25.856 us，最终为
11.552/19.520 us；K15/B8 最终为 17.696/23.808 us。该结果限于完整 Op。
复现：`build/tests/ninfer_candidate_selector_test`；
`build/bench/ninfer_candidate_selector_bench --warmup 8 --repeat 60`。

### OP-07 — context_kv_materialize（已完成）

合同：[context_kv_materialize.h](../../include/ninfer/ops/context_kv_materialize.h)。落点：
`src/ops/context_kv_materialize/`；共享 W8 K-split contraction 仍归 `src/ops/linear/w8/`。

原问题：batched contract、四列 SIMT tile 和 grid 固定 W_c=8；raw K 的 BF16 舍入被写入
public formula/oracle。新 qualification 还发现旧 composed 路线的大 T_c `linear_pair`
MMA 分支不能通过本 Op 的 V criterion，因此本 Op 已移除该 composition 和原 SIMT 路线。

- [x] 接纳 W_c=1..16、B_c=1..8，以及单请求 W_c=1..2048；五层继续直接消费各自
  QKV-parent 的 K/V row views、norm 和 cyclic cache，不拼接或重排 persistent 权重。
- [x] 以 `E=B_c*envelope.max_count` 调度 projection。直接将 envelope 内的列映射回
  `[5120,W_c,B_c]`，无需额外 context pack；写入前按 device counts 检查有效性。
  两阶段路线的 raw K scratch 为 FP32 `[1024,E,5]`，K-post 恢复 physical position/slot。
  奇数宽度、request 边界、未写 padding 和 lane 重排均不依赖 W_c=8。
- [x] 数学 oracle 直接从 signed W8 code、实际 FP16 scale 和 BF16 context 计算完整 FP64
  projection→norm→RoPE，不在 raw K 处舍入。最终 BF16 K 和 `FP16_RNE(BF16)` V 是显式
  storage 边界；V oracle 直接将 FP64 dot 舍入到 BF16，再执行 FP16 codec。
  K criterion `(relative L2,gross abs,gross rel)=(3e-3,1e-2,6e-3)`、V criterion
  `(2.9e-3,4e-3,3.8e-3)` 保持原值。
- [x] 比较 per-request SIMT、K-split、row-tiled MMA、融合 K-post/store 和 composed。
  BF16 scaled-weight dequantization 的候选未通过 V criterion，已淘汰。生产 MMA 将 signed
  integer code 精确表示为 BF16 operand，每 32 维量化组之后在 FP32 应用实际 FP16 scale，
  然后进行 FP32 归并；raw K 全程保持 FP32。这是实现精度选择，不新增 raw K 语义 cast。
- [x] 通过全部 128 个 W_c/B_c 组合、零/短/满 counts、全零 projection、五组 parent row
  views、capacity=2048/padded=2056、lane 重排、2048 wrap 和约 262144 的绝对位置检查。
  B_c=8 的每个宽度及单请求 route anchors 均在同一 Graph 中改写 counts/positions/slots，
  分别验证满/短/零前缀；inactive state slots 可以是 -1，所有未寻址 cache 元素逐位不变。
  Parent/context/norm/counts/positions/slots 只读，cache/workspace guards、scope 释放和 query
  的实测区间峰值均通过。单请求补测 17、81、85、86、97、128、129、256、257、2048 列，
  以及 W_c=2048 下的 0/1/96/128/257 行前缀。

最终 dispatch 只依赖 E，所有路线保留独立五层参数和实际 physical width：

| E | Projection schedule | 完整 Op 的 Graph nodes | 实际 transient scratch |
| --- | --- | ---: | ---: |
| 1..16、25..32 | R16/C16，8 个 K-split warps | 2 | 20480*E B |
| 17..24 | R16/C24，8 个 K-split warps | 2 | 20480*E B |
| 33..64 | R64/C32/K128，2 个 column warps/row tile | 2 | 20480*E B |
| 65..80 | R64/C80/K128，5 个 column warps/row tile | 2 | 20480*E B |
| 81..96 | R64/C96/K128，6 个 column warps/row tile | 2 | 20480*E B |
| 97..256 | R128/C64/K128，融合 head norm/RoPE/cache store | 1 | 0 |
| 257..2048（B_c=1） | R64/C64/K64，2 个 column warps/row tile | 2 | 20480*E B |
| max_count=0 | host 返回，无 device work | 0 | 0 |

Query 对每个物理宽度覆盖所有合法 count envelopes，不能只看满块路线。比如 W_c=16/B_c=8
满块实际 scratch=0，但 max_count=12 需要 1,966,080 B；该 fixed-width query 必须覆盖后者。
W_c=1..16 区间在 B_c=1..8 下的精确 query 峰值依次为
`327680/655360/983040/1310720/1638400/1966080/1863680/1966080 B`。
单请求 W_c<=2048 的最大 transient 为 40 MiB；W_c=2048/count=1 实际只用 20,480 B。
五层 ring 的 persistent payload 仍为每请求 40 MiB，与 draft K 无关。

性能证据为 RTX 5090、sm_120a、CUDA 13.1，完整 public Op 的 cold CUDA Graph，256 MiB
flush、warmup=8、repeat=60，包含全部 device work。Full/one/ragged 各 128 点、18 个
prefill points、两个宽物理块的单行前缀完成测量；另六个 zero points 验证空 Graph，延迟记为
不适用。共享 contraction 的普通 W8 Linear 和 dynamic-conv-add 数值回归通过。

Full-count median（µs），列为 B_c，行为 W_c：

| W_c | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 44.288 | 44.320 | 44.320 | 46.144 | 46.336 | 46.336 | 46.336 | 46.336 |
| 2 | 44.320 | 46.016 | 46.336 | 46.336 | 46.368 | 48.384 | 48.384 | 48.416 |
| 3 | 44.800 | 46.336 | 46.336 | 48.128 | 48.384 | 50.944 | 52.512 | 56.608 |
| 4 | 46.048 | 46.336 | 48.160 | 48.384 | 52.480 | 56.608 | 64.800 | 66.848 |
| 5 | 46.336 | 46.368 | 48.416 | 52.480 | 62.752 | 66.592 | 74.752 | 74.976 |
| 6 | 46.336 | 48.256 | 50.944 | 56.608 | 66.720 | 74.720 | 75.008 | 75.040 |
| 7 | 46.336 | 48.384 | 52.512 | 64.800 | 74.880 | 74.976 | 75.008 | 75.008 |
| 8 | 46.304 | 48.384 | 56.608 | 66.848 | 74.912 | 75.040 | 75.008 | 75.008 |
| 9 | 46.336 | 50.464 | 64.800 | 74.784 | 75.008 | 75.040 | 75.264 | 95.488 |
| 10 | 46.400 | 52.480 | 66.720 | 74.976 | 75.040 | 75.040 | 95.488 | 95.520 |
| 11 | 48.192 | 54.560 | 74.848 | 75.008 | 75.040 | 95.360 | 96.000 | 105.728 |
| 12 | 48.160 | 57.056 | 74.752 | 75.008 | 75.008 | 95.488 | 105.728 | 107.520 |
| 13 | 48.416 | 64.768 | 75.008 | 75.008 | 95.488 | 95.488 | 105.760 | 118.016 |
| 14 | 48.416 | 64.800 | 75.008 | 75.008 | 95.456 | 105.728 | 117.888 | 117.920 |
| 15 | 48.416 | 66.816 | 75.008 | 75.040 | 95.488 | 106.240 | 117.888 | 117.920 |
| 16 | 48.416 | 66.848 | 75.040 | 75.520 | 95.520 | 107.584 | 118.048 | 123.680 |

旧 W_c=8 direct 的 B_c=1/8 基线分别为 62.592/386.304 µs；最终为
46.304/75.008 µs（约 1.35x/5.15x）。仅扩宽并改成 FP32 raw K 的 SIMT 在 W_c=16/B_c=8
为 781.088 µs，最终为 123.680 µs（约 6.32x）。候选比较还包括 R32/64/128、K64/128/256、
C8..128 和不同 warp 分配：例如 65..80 列的 C80/5-column-warp profile 将相关候选的约
110–114 µs 降至约 95–98 µs；更宽 C128/8-column-warp 未胜出。97..256 的融合路线与
两阶段方案比较后选入；超过 256 列时，两阶段方案吞吐更好。所有临时候选入口已删除。

One-count 的全部宽度/B 组合 median 为 46.208–48.416 µs；W_c=16/B_c=8 为 48.384 µs，
实际 scratch=163,840 B。W_c=2048/B_c=1/count=1 为 46.336 µs，验证工作量按前缀而非
物理 padding 扩大。Ragged 全曲线 median 范围为 46.048–117.792 µs，并通过相同状态 oracle。

单请求 prefill/route seams（µs）：

| W_c=count | median | nodes | query B | actual peak B |
| --- | ---: | ---: | ---: | ---: |
| 17 | 50.464 | 2 | 348160 | 348160 |
| 24 | 58.624 | 2 | 491520 | 491520 |
| 25 | 64.736 | 2 | 512000 | 512000 |
| 32 | 66.848 | 2 | 655360 | 655360 |
| 33 | 74.976 | 2 | 675840 | 675840 |
| 64 | 77.056 | 2 | 1310720 | 1310720 |
| 65 | 95.456 | 2 | 1331200 | 1331200 |
| 80 | 97.280 | 2 | 1638400 | 1638400 |
| 81 | 105.728 | 2 | 1658880 | 1658880 |
| 96 | 107.520 | 2 | 1966080 | 1966080 |
| 97 | 118.016 | 1 | 1966080 | 0 |
| 128 | 118.016 | 1 | 1966080 | 0 |
| 129 | 210.176 | 1 | 1966080 | 0 |
| 256 | 218.208 | 1 | 1966080 | 0 |
| 257 | 285.824 | 2 | 5263360 | 5263360 |
| 512 | 410.624 | 2 | 10485760 | 10485760 |
| 1024 | 783.616 | 2 | 20971520 | 20971520 |
| 2048 | 1504.352 | 2 | 41943040 | 41943040 |

129 列和 257 列仍有 tile/route 阶跃，上表保留实测值；没有把曲线描述为连续增长。
长 prefill 的 40 MiB workspace 和实际时延已单独记录，不能用七 draft 的短块结果代替。
这些结果只建立 Op qualification，不建立 Engine 或端到端性能结论。

### OP-08 — speculative_accept_sparse_drafts（已完成）

合同：[speculative_round.h](../../include/ninfer/ops/speculative_round.h)。落点：
`src/ops/wrapper/speculative_round.cpp`、`src/ops/launcher/speculative_round.cu`、
`src/ops/kernel/speculative_round.cuh`。

原问题：Sparse wrapper、raw-greedy kernel、launcher 和 workspace 固定 K=7/W=8，
而现有 sampling substrate 已支持每请求最多 16 个验证列。

- [x] K=1..15、B=1..8 全域接入，W=K+1 从 drafts extent 推导；target logits/tokens、
  candidate ids/q、licensed outputs、尾部清零和 row stride 全部使用实际 K/W。
  `P=clamp(current_extents[b],0,K)`，只消费 target 的 0..P 列，bonus 取第 P 列。
  Query 新增 inclusive draft-count interval，并覆盖 K/B 联合域；raw envelope 返回零容量。
- [x] General 路线复用原 target sampling 的 partial/group pipeline，不增加 target-distribution
  实现。保留 token_domain=248077、physical_rows=248320、target top-20 cap、top-k/top-p/min-p
  和 committed-history + accepted-prefix penalty overlay。q 始终读取 selector 实际产生的
  FP32 `[16,K,B]`，不改成 one-hot、不从 unary 重建，也不展开为 dense vocabulary q。
- [x] 接受判定改成一请求一 warp。p_i/q_i 给定后，各 draft 的 counter-RNG 接受事件可并行
  求值，ballot 的第一个失败位置给出 A；只使用该位置的 correction 或全接受时第 P 列的
  bonus。后续事件不会许可输出，也不改变随机数状态。Residual/bonus 的至多 20 项 CDF
  用 warp FP32 scan；这是私有 reduction profile，仍直接对照独立 FP64 p/q oracle。
  Raw-greedy 采用一个 CTA、B 个 warps，按实际 W 合并写出所有 licensed slots。
- [x] 所有 120 个 K/B 组合分别通过 raw 与 general fixtures；raw 另覆盖首/中/末拒绝、
  P=0、all-accept 和 extent clamp。General 覆盖 mixed modes、过滤、p/q 接受与 one-hot
  接受不同的案例、target/q support 不同的 residual，以及重复 draft 的历史 penalty。
  特别验证 p(d)=1/4、q(d)=1/2、u∈(0.30,0.45) 时接受，并将 d 放在最后一个有效 token id
  248076；physical padding 的高 logits 不得参与选择。
- [x] 同一 Graph 覆盖不同 P/逻辑长度；K=1/7/15、B=8 的 general 案例还原址更新 logits
  和 actual q，再恢复原输入。每次都独立计算 FP64 oracle，并精确比较 A、L、licensed
  tokens、provisional length/anchor 和无效物理尾。Token counts、configs、drafts、candidate
  ids/q、logits、target tokens、extents 均只读；outputs/workspace guards 和 arena scope/peak
  通过。原 one-hot speculative family 的回归测试一并通过。

生产只有 raw/general 两条公开 envelope 路线。Raw 为 1 个 Graph node、0 scratch；general
为 2 个 nodes，复用每请求一个 256 B 对齐的 sampling layout。Query 的联合区间容量与全域
实际 arena 峰值一致：K=1/7/15、B=8 分别为 1,304,576 / 5,189,632 / 10,369,024 B。
K=16 在两类 envelope 下都被拒绝，避免误把 W=16 当作 16 个 drafts。

比较了扩宽后的原串行接受、单 CTA 的 warp 方案和一请求一个 CTA 的 raw 方案。
两种 warp raw 布局的差异处于测量波动内，最终保留单 CTA；串行 sparse 路线和候选入口已删除。
给定路径后 p_i 使用的 penalty overlay 是前 i 个 drafts；第一个失败前这些 drafts 均已接受，
因此并行求值和原顺序拒绝采样具有相同的条件语义。

性能采用 RTX 5090、sm_120a、CUDA 13.1，完整 public Op 的 Graph，warmup=8、repeat=60，
每个 Graph 前 flush 256 MiB。覆盖全部 K 的 B=1/8 曲线，另测 K=1/7/15 的 B=2/4、P=0、
首/中/末拒绝和 raw 单次调用，共 174 points。下表 raw 是每 Graph 32 次调用后的每次均值，
flush 在整个 bundle 之前，后续调用复用 cache；其余列是一 Graph 一次完整调用的 median。
Synthetic target 的 top-k=20、top-p=0.95、temperature=0.6；满 extent 的主曲线全部接受。
“history”启用 presence penalty 和 committed-count 输入；“no history”走无 penalty 的
BF16 partial-key 路线。Mixed 依次循环 raw greedy / penalty greedy / stochastic，B=1 时
因此是 general envelope 中的单个 raw-greedy request。

单位 µs，括号内为 B：

| K | raw32 (1) | raw32 (8) | stoch no history (1) | stoch no history (8) | stoch history (1) | stoch history (8) | mixed (1) | mixed (8) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 1.312 | 1.312 | 23.808 | 56.576 | 25.856 | 64.320 | 5.376 | 38.144 |
| 2 | 1.313 | 1.319 | 23.808 | 75.040 | 25.856 | 85.280 | 5.376 | 46.336 |
| 3 | 1.313 | 1.316 | 27.936 | 91.424 | 29.984 | 107.776 | 5.408 | 56.416 |
| 4 | 1.247 | 1.320 | 29.952 | 113.920 | 32.000 | 132.384 | 5.408 | 64.256 |
| 5 | 1.248 | 1.318 | 32.000 | 130.304 | 36.096 | 152.864 | 7.456 | 72.992 |
| 6 | 1.310 | 1.313 | 34.048 | 144.672 | 38.144 | 175.392 | 7.456 | 83.232 |
| 7 | 1.245 | 1.320 | 36.128 | 162.912 | 40.192 | 195.872 | 7.424 | 91.424 |
| 8 | 1.321 | 1.314 | 40.224 | 181.152 | 44.320 | 220.448 | 7.456 | 103.680 |
| 9 | 1.318 | 1.314 | 42.240 | 195.872 | 46.368 | 242.848 | 7.456 | 111.904 |
| 10 | 1.317 | 1.313 | 44.512 | 212.032 | 50.464 | 265.472 | 7.424 | 122.144 |
| 11 | 1.313 | 1.319 | 46.368 | 226.560 | 52.512 | 285.952 | 7.456 | 132.384 |
| 12 | 1.315 | 1.312 | 50.432 | 242.944 | 56.576 | 310.272 | 7.456 | 140.576 |
| 13 | 1.313 | 1.317 | 50.464 | 259.360 | 58.624 | 331.008 | 9.216 | 152.576 |
| 14 | 1.319 | 1.320 | 54.528 | 275.712 | 62.272 | 355.616 | 9.504 | 161.056 |
| 15 | 1.312 | 1.317 | 56.576 | 291.936 | 64.800 | 376.096 | 9.504 | 171.296 |

与仅扩展形状、保留原串行接受的候选相比，K=15/B=8 的 raw32 从 3.113 降至 1.317 µs/次
（约 2.36x）；K=7/B=8 从 1.761 降至 1.320 µs/次。单次 cold raw Graph 仍约 3.328–3.360 µs，
本次不据此宣称独立单次调用得到相同倍数的加速。

History 全接受的 K=7/B=1、K=15/B=1、K=15/B=8，串行候选分别为
48.384 / 79.136 / 392.192 µs，最终为 40.192 / 64.800 / 376.096 µs（约 16.9% / 18.1% /
4.1% 的时延下降）。No-history 首位拒绝的 K=15/B=8，候选 292.032、最终 292.096 µs，
基本持平；不把长接受前缀的收益外推到首位拒绝。

边界测量（µs）：P=0 使用 history profile；首位拒绝使用 no-history；中/末位拒绝使用 history。
这些输入仍运行整个 public Op，包括所有 live target distributions 的构建。

| K/B | P=0 | 首位拒绝 | 中位拒绝 | 末位拒绝 | raw 单次 cold |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1/1 | 19.744 | 23.808 | 25.888 | 25.888 | 3.328 |
| 1/8 | 42.272 | 56.544 | 64.320 | 64.448 | 3.328 |
| 7/1 | 19.712 | 36.128 | 40.224 | 40.224 | 3.328 |
| 7/8 | 46.368 | 163.072 | 195.840 | 195.872 | 3.360 |
| 15/1 | 19.744 | 56.576 | 64.768 | 64.800 | 3.328 |
| 15/8 | 52.480 | 292.096 | 376.064 | 376.096 | 3.328 |

K=15/B=8 中位拒绝的一次采样集 p95=600.896 µs、median=376.064 µs；其余同形状主曲线
p95 约 378 µs。上表保留该采样集的 median，未将尾部抖动解释为 Op 或 Engine 的收益。
本项完成的是 Op 级 qualification；正式 Engine 接入和端到端评估仍依赖后续清单。

## 4. 已支持 extent、需要重新 qualification 的逐 Op 清单

这些项目不因为重新 qualification 就必须修改合同或 kernel。若测量要求改 route，应在原
family 完成实现和独立 oracle 验证。每项的“待复核”在相关格式和新 workload 证据齐备后
才改为完成；未改动且已有证据直接覆盖的点引用已有结果，不重复实验。

### 4.1 Context 与 proposal 主干

| ID / 状态 | Op 与合同 | 精确 profile / 新输入 | 复核与方案 |
|---|---|---|---|
| OP-09 已完成 | [linear](../../include/ninfer/ops/linear.h) feature projection | W8 `[5120,25600]`，decode T_c<=128，prefill<=2048 | 任意正 T、零 scratch 合同保持；T<=56 small-T，57..64 R16/C64/K128，65..128 R32/C64/K128，较大 T 保留 R64/C128。已完成 FP64 oracle、Graph 重放与完整新列数曲线，见下方记录。 |
| OP-10 已完成 | [linear](../../include/ninfer/ops/linear.h) selector-hidden | BF16 `[256,5120]`，U<=120 | 移除 64 列串行 launch；U<=76 为 16 个 K-warp/C8，U>=77 为 8 个 K-warp/C16，均一次 grid、零 scratch。逐元素 FP64 oracle、Graph 重放和冷缓存/连续调用曲线通过，见下方记录。 |
| OP-11 已完成 | [attn_input_proj](../../include/ninfer/ops/attn_input_proj.h) draft QKV | W8 parent `[6144,5120]`，直接输出 Q/K/V，T<=128 | 保留任意正 T；T<=48 small-T，49..63 R16/C64/K128，64..96 R32/C32/K128，97..128 R32/C64/K128（三 CTA launch bounds），较大 T 保留原路线。三输出 oracle、Graph 与完整列数曲线通过，见下方记录。 |
| OP-12 已完成 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) draft MLP | W8 `[34816,5120] -> [17408,T]`，T<=128 | T<=40 保留 small-T，41..96 按下表使用 K128 MMA；T=64 保留独立最快配置，97+ 保留已比较的原 K64 route。完整非线性 FP64 oracle、Graph、workspace/guards 和全域曲线通过。 |
| OP-13 已完成 | [sliding_window_attention](../../include/ninfer/ops/sliding_window_attention.h) | D128/Q32/KV8、window=2048，W=2..16、B=1..8 | 保留 W<=16/B<=8；FP32 partial 修正额外舍入，按 W/B/context 调整 splits，2048 用 4-warp merge，宽 batch/block 的 direct 上限128。完整 FP64、ragged/Graph/codec/guards、区间 scratch 和全域曲线通过，见下方记录。 |
| OP-14 已完成 | [kv_cache_append_prefix](../../include/ninfer/ops/kv_cache_append.h) cyclic form | BF16 K/V `[128,8,W_c,B_c]`，capacity=2048 | 保持正 extent 与独立 device count；cyclic 改为8元素向量、按 max_count×B 选择64/128 threads。所有 Wc/B、exact K/V codec、尾部/lanes/padding、Graph及4096回归通过，按实际counts完成重新测量，见下方记录。 |
| OP-15 已完成（依赖退出） | [linear_pair](../../include/ninfer/ops/linear_pair.h) | DFlash2 context 的该依赖已由 OP-07 移除；保留已有 MTP `[1024,5120]` / DFlash `[1024,2048]` 消费者 | 实际调用与共享模板影响已复核；两种 LinearPair profile 的现有 FP64 oracle/guards/只读回归通过。DFlash2 context 的新 extent qualification 由 OP-07 承担，见下方记录。 |
| OP-16 已完成 | [prepare_ragged_prefix](../../include/ninfer/ops/prepare_ragged_prefix.h) | source `[25600,W_c,N_lane]`，destination `[25600,W_c,B]` | 保留正 W/strided source；2KiB feature tile、128 threads，3D grid与唯一metadata写入者。全部Wc/B、N=0/1/短/满、16行all-accept、只读别名/Graph/exact/guards通过，并完成full/one/ragged/zero曲线。 |
| OP-17 已完成 | [embedding](../../include/ninfer/ops/embedding.h) | W8/row-FP8 `[248320,5120]`，proposal/target T<=128 | W8四code解码与FP8直接scale读取，按T选择10/5个CTA；保留合法输出对齐和正T。真实ids/mask、全部1..128列、Graph/prefill/codec边界、只读/guards与全W/B性能曲线通过；BF16舍入界按格式修正，见下方记录。 |
| OP-18 待复核 | [rmsnorm](../../include/ninfer/ops/rmsnorm.h) | context plain D5120、T_c；target offset D5120 及 Q/K D256、T<=128 | 明确 draft/context plain gain 与 target offset gain；补相关 extent/reduction 行布局。性能按实际调用规模复核，不将所有 norms 写成 plain 或只测 D5120。 |

### 4.1.1 OP-09 — feature Linear qualification

W8 `[5120,25600]` 的数学域保持任意正 T；没有按 draft K 截断 context 列数。新增路线由
`src/ops/linear/w8/w8_feature.cu` 拥有，复用原 W8 MMA contraction，直接写 BF16 输出。
T=57..64 采用 R16/C64/K128、T=65..128 采用 R32/C64/K128，warp tile=16x16、activation
单 stage；两者均为一个 kernel、零 scratch。T<=56 保留原 exact-small 路线，删除已退出
调度的 57..64 exact-small 实例；T>128 保留原通用 prefill 路线及大 T grid 分片机制。

原 T=128 R64/C128 仅有 80 CTA；5090 有 170 SM。针对该 kernel 的 NCU 显示 waves/SM
0.24、achieved occupancy 16.66%、eligible warps/scheduler 0.23。比较 R16/R32/R48/R64、
C64/96/128、K64/128，以及 CTA 内 2/4 路 K-split（含减少循环展开的版本）后，按完整 Op
延迟选择上述路线。整 tile 的 `Full` 专用编译版本在 R16/T=64 退化，最终统一使用带边界
判断的编译版本；没有因 T 整除而强行选用更慢的版本。

独立 oracle 从 signed INT8 code 和实际 FP16 scale 解码权重，以 represented BF16 X
计算 FP64 dot，最终 BF16 输出仍使用统一 A16 criterion：relative-L2<=1/256，最大
绝对误差<=1/256+(2/256)*max|reference|。MMA 路线内部将解码权重暂存为 BF16 是私有计算精度，
不是新增的数学舍入边界。新路线与原 small-T 均直接对同一 oracle 验证。

`ninfer_linear_w8_a16_test` 覆盖每个 T=1..128、prefill 129/256/257/1000/1024/2048 及
2049；覆盖 convenience 调用和 AllowA8 许可下的 A16 执行。数值比较采样 32 行和最多
32 列，每个采样 dot 遍历完整 K；所有输出扫描 finite/poison，并检查 guards、输入和权重
只读、workspace query/使用量及 scope。14 个边界/代表点 capture 后各重放两次，第二次
将输入 BF16 精确取负，并按该输入计算 FP64 oracle。全部 W8 Linear 回归通过。

RTX 5090 / sm_120a / CUDA 13.1；`ninfer_linear_bench --qtype w8 --n 5120 --k 25600
--sweep 1:128:1 --execution eager|graph --warmup 8 --repeat 60`，每次完整 Op 前 flush
256 MiB。新增 `--execution` 明确区分 eager 与 Graph，CSV 记录 Graph 节点数。以下为
median µs；Graph 全部为一个节点。T=57/58 的差异接近计时粒度，不宣称稳定收益。

| T | 原 eager | 新 eager | 新 Graph |
|---|---:|---:|---:|
| 8 | 97.152 | 95.488 | 95.488 |
| 32 | 144.640 | 144.608 | 144.672 |
| 56 | 208.064 | 208.128 | 208.128 |
| 57 | 220.352 | 220.416 | 220.416 |
| 64 | 240.928 | 224.512 | 226.080 |
| 65 | 428.064 | 251.168 | 251.168 |
| 96 | 433.376 | 257.088 | 257.024 |
| 120 | 462.080 | 273.664 | 273.696 |
| 128 | 410.880 | 279.840 | 281.536 |
| 129 | — | 481.984 | 482.432 |
| 1024 | — | 1344.800 | 1344.770 |
| 2048 | — | 2688.990 | 2687.260 |

T=128/129 仍有 281.536→482.432 µs 的 Graph 阶跃：129 已进入未改动的 prefill route。
Prefill 表项作为已保留路线的测量参考，不宣称 prefill 全域最优。完整 eager 曲线的 T=99
曾有 p95=488.704 µs、median=259.360 µs；Graph 曲线没有同类尾部尖峰。
最终移除整 tile 分支后复测了受影响的 64/128，其余点复用同一未改动编译路线的全域测量。

这些是 Op 级测量，不构成 Engine 端到端性能结论。

### 4.1.2 OP-10 — selector-hidden Linear qualification

BF16 `[256,5120]` 保持任意正列数；U=K*B 是当前 caller 的工作域，Op 不固定 K 或 U。
移除每 64 列串行发射的路径，改用完整 column grid，仅在超过 CUDA grid.y 上限时使用
既有 `for_each_token_slice`。U<=76 采用 R16/C8、16 个 K-split warp，U>=77 采用
R16/C16、8 个 K-split warp；均为双缓冲，直接写 BF16，当前 U<=120 全部一个 kernel、
零 scratch。比较过 4/8/16 个 K-warp、8/16 列 tile、单/双缓冲；未选候选全部移除。

一次冷缓存 Op 和连续调用的结果共同确定分界：U=65..72 时 C8 的冷缓存延迟更低；
77..80 的冷缓存结果相近而 C16 连续调用更快；U=81..120 时 C16 在两种测量中均有优势。
未仅凭减少 launch 数选择 kernel；单独把旧 C8 kernel 改成大 grid，在 U=120 的连续
调用中反而比旧两次 launch 慢，最终采用已比较的 C16 路线。

`ninfer_linear_bf16_a16_test` 从 represented BF16 W/X 调用既有独立 `dot_fp64`，计算
每行完整 K 的数学 dot；没有复制 FP32 staging 或 reduction tree。U=1..120 逐个验证
全部 256 行、全部列，并覆盖 121/127/128/129/256/1024。16 个代表点（含 64/65、76/77）
capture 后各重放两次；第二次输入 BF16 精确取负，以数学 dot 的对应取负结果为 oracle。
输出使用统一 A16 criterion：relative-L2<=1/256，最大绝对误差<=1/256+(2/256)*max|reference|；
同时验证 non-finite/poison、输出 guards、输入/权重只读、workspace query/使用量和 scope。
其余 BF16 Linear profiles 的既有回归也通过。

RTX 5090 / sm_120a / CUDA 13.1，`ninfer_linear_bench --qtype bf16 --n 256 --k 5120
--sweep 1:120:1 --execution graph --warmup 8 --repeat 60`。另用 `--graph-calls 32` 测连续
调用，所有延迟均归一化为 µs/Op。两种模式各有 120 个正式调度点。每个 Graph bundle
前 flush 256 MiB；32 次调用的后续调用复用缓存，不能视作每次 Op 都是冷缓存。该模式
的 CSV 记录总节点数和调用数，并停止报告不适用的 DRAM/read 百分比和 DRAM memory floor。

| U | 原冷缓存 | 新冷缓存 | 原 32-call | 新 32-call |
|---|---:|---:|---:|---:|
| 7 | 6.944 | 7.424 | 4.201 | 4.201 |
| 16 | 7.424 | 9.216 | 4.520 | 4.520 |
| 64 | 9.472 | 9.472 | 6.631 | 6.629 |
| 65 | 15.648 | 9.472 | 10.839 | 7.079 |
| 80 | 15.584 | 11.520 | 11.103 | 6.809 |
| 96 | 15.616 | 11.520 | 11.112 | 7.272 |
| 120 | 17.216 | 13.472 | 12.639 | 9.189 |

表中 32-call 对照采用原 kernel 与最终 kernel 同环境交错测量（warmup=20、repeat=180）。
U=16 的单次冷缓存 median 前后为 7.424/9.216 µs，同环境复测为 7.456/8.864 µs，
min 约 7.0 µs、p95 约 9.5 µs 的区间相近；原/新小列数 profile 的 400 条机器指令
编码相同，连续调用均为 4.520 µs。没有把该单点 median 变化归因于 kernel 回退或收益。

### 4.1.3 OP-11 — draft QKV projection qualification

三输出合同仍为 W8 parent `[6144,5120]`、BF16 X `[5120,T]`，直接写 Q `[4096,T]` 和
K/V `[1024,T]`，T 为任意正数；没有 packed QKV、附加 normalization/RoPE 或 transient
workspace。原 R32/C64 已经使用完整 column grid，64 是 CTA tile，不是 64 列串行发射限制。

新调度由原 W8 attention-input family 拥有：

| T | row/column/K tile | warp tile / launch bounds | 说明 |
|---|---|---|---|
| 1..48 | 原 exact-small | 原 K-split | 保留低 T 的速度和精度，移除本 Op 已退出的 49..53 small-T 实例 |
| 49..63 | 16/64/128 | 16x16 / min blocks 1 | 单 activation stage，使用带边界判断的编译版本 |
| 64..96 | 32/32/128 | 16x16 / min blocks 1 | 单 activation stage |
| 97..128 | 32/64/128 | 16x16 / min blocks 3 | 单 activation stage，限制寄存器占用 |
| 129..192 | 原 32/64/64 | 原配置 | 保留已存在的中等 prefill route |
| >=193 | 原 64/128/64 | 原配置 | 保留通用 prefill 和 CUDA grid 上限分片 |

比较了 17 个 MMA 配置/编译 profile，覆盖 R16/32/64、C32/64/96/128、K64/128、
activation 单/双 stage、warp 分工、launch bounds 和整 tile/带边界判断版本。强制 MMA
至 T=1 时 Q 超出现有 criterion，最终调度在该点继续使用原 small-T；没有放宽标准或
缩小 public domain。所有采用范围内的候选直接通过同一 oracle 后才进入性能比较。

针对 R32/C64/K128、T=112 的 NCU 确认：原 min-blocks=1 profile 有 81 registers/thread，
寄存器限制为 2 CTA/SM，384 CTA 形成 1.13 waves/SM；共享内存本可容纳 3 CTA。
min-blocks=3 版本使用 68 registers/thread，允许 3 CTA/SM、0.75 waves/SM；理论 occupancy
33.33%→50%，实测 achieved occupancy 30.94%→36.60%。这些 profile 数据解释上端路线选择，
实际 Op 延迟由下方独立 benchmark 给出。

`ninfer_attn_input_proj_test` 对每个 T=1..128 及 129/192/193/1024 检查三个输出。Oracle
逐项 exact-decode signed W8 code 和实际 FP16 scale，再从 represented BF16 X 做完整
FP64 dot；不复制 BF16 weight staging 或 FP32 reduction。所有列均比较，普通点每个输出
采样 7 行；16 个边界/代表 Graph 点各采样 31 行、重放两次，第二次输入精确取负并重新
计算 oracle。保持 A16 criterion：relative-L2<=2.9e-3，最大绝对误差<=4e-3+4.5e-3*max|ref|。
同时检查所有输出 fully-written/finite、guards、输入与 parent 只读；其余已注册格式和
35B W8 target/companion 的既有 attention-input 回归通过。

RTX 5090 / sm_120a / CUDA 13.1；`ninfer_attn_input_proj_bench --format w8-dflash2-qkv
--tokens ... --execution eager|graph --cache both --warmup 8 --repeat 60`。两种 execution
各覆盖 1..128 和上述 prefill 参考点；冷缓存每次完整 Op 前 flush 256 MiB。CSV/终端明确
execution、Graph 节点数和 GPU/toolchain；当前新 decode 范围均为一个节点、零 scratch。
以下为完整 Op median µs，不作 Engine 端到端收益推断。

| T | 原 eager 冷 | 新 eager 冷 | 新 Graph 冷 | 新 Graph 暖 |
|---|---:|---:|---:|---:|
| 8 | 27.904 | 27.904 | 27.904 | 26.112 |
| 48 | 52.480 | 52.512 | 52.512 | 53.728 |
| 49 | 66.752 | 56.576 | 56.576 | 50.656 |
| 53 | 72.992 | 56.576 | 56.576 | 52.672 |
| 54 | 76.896 | 56.576 | 56.576 | 51.424 |
| 64 | 75.040 | 58.624 | 58.624 | 54.944 |
| 65 | 79.136 | 62.752 | 62.752 | 60.352 |
| 96 | 81.152 | 68.832 | 68.736 | 69.408 |
| 112 | 83.232 | 79.904 | 79.136 | 81.152 |
| 128 | 89.376 | 83.232 | 83.232 | 85.536 |
| 129 | — | 97.568 | 97.568 | 97.280 |
| 193 | — | 152.768 | 152.768 | 152.864 |
| 1024 | — | 349.440 | 349.440 | 356.096 |

129/192/193/1024 为保留路线的参考测量；192→193 的 Graph 冷缓存延迟为
105.760→152.768 µs，仍有原 prefill 分界的阶跃。小 T warm 单次调用有较大尾部波动，
例如 T=1 的 Graph median/p95 为 20.864/37.440 µs；不据此推断低 T 的微小收益。

### 4.1.4 OP-12 — draft LinearSwiGLU qualification

数学仍是从 represented W8 codes/FP16 scales 和 BF16 X 计算两次投影，再计算
`SiLU(gate)*up`，只对最终 BF16 输出规定存储边界。T 保持任意正数，Op 直接写
`[17408,T]`，所有 W8 routes 的 caller workspace 仍为零。

| T | parent row/column/K tile | warp tile / min blocks / activation stages |
|---|---|---|
| 1..40 | 原 exact-small | 原配置；删除已退出的 41..51 实例 |
| 41..63 | 32/64/128 | 16x16 / 3 / 1 |
| 64 | 64/64/128 | 32x16 / 2 / 1 |
| 65..80 | 64/80/128 | 64x8 / 2 / 1 |
| 81..88 | 64/88/128 | 64x8 / 2 / 1 |
| 89..96 | 64/96/128 | 64x8 / 2 / 1 |
| >=97 | 原 64/128/64 | 64x16 / 2 / 2 |

比较了 19 个 MMA 配置/编译 profile，包括 R32/64/128、C64/80/88/96/128、K64/128、
warp 分工、activation stages、整 tile/带边界判断版本。T=64 的特例有实测优势，保留该
点并改为 R64/C64/K128；较大 T 的已选 K64 route 在本轮比较中继续占优。低 T 强制 MMA
在 T=16 的负输入非线性结果上超过原 criterion，最终继续使用原 small-T；没有放宽标准。

R64/C96/K128 使用 49 KiB 静态 shared memory，已在 sm_120a/5090 上验证。原通用
schedule 的 48 KiB 限制改为该平台的 99 KiB per-CTA 上限；这不扩大 public weight/shape
集合。仍复用原 activation staging 作为 fused epilogue 的临时区，并补齐容量约束：跨 warp
FP32 up tile 及 BF16 residual tile 都检查实际 `ACTIVATION_STAGES*BK`。没有保留测量未获
收益的阶段复用改造或大 row tile 单 stage 路线。

`ninfer_linear_swiglu_w8_a16_test` 对 T=1..128 及 129/256/1024 验证所有 17408 行和全部
列，复用同一个独立完整 FP64 oracle。第一列为 dense，其余列为精确零与轮转非零值；
oracle 跳过数学上的零项，不复制生产路径的 cast、workspace dtype 或 reduction tree。
18 个代表/边界点 capture 后重放原输入及精确取负输入；负输入重新计算完整非线性
oracle，不能简单取负原输出。A16 criterion 保持 relative-L2<=3.3e-3，最大绝对误差
<=5e-3+6.3e-3*max|reference|。验证 fully-written/finite、输出/输入/权重 guards、只读、
区间及逐点 workspace query/high-water/scope；companion W8 的既有回归也通过。

`ninfer_w8_linear_swiglu_bench` 现在仅测 public `linear_swiglu`，移除私有候选/拆分对照及
只供旧 benchmark 使用的未选 launcher。支持 eager/Graph，记录硬件、execution、节点数
和 workspace，并修正 `--profile`，在 warmup/flush 后仅开启一次完整 Op 的 CUDA profiler
区间。RTX 5090 / sm_120a / CUDA 13.1，warmup=8、repeat=60，每次完整 Op 前 flush
256 MiB；两种 execution 各覆盖 1..128 和上述 prefill 点，均为一个 kernel、零 scratch。
下表为 Op median µs；97+ 未宣称新的性能收益，整轮/Engine 集成仍属后续工作。

| T | 原 eager | 新 eager | 新 Graph |
|---|---:|---:|---:|
| 8 | 128.256 | 126.656 | 128.128 |
| 40 | 193.824 | 193.568 | 193.568 |
| 41 | 204.032 | 202.016 | 202.016 |
| 48 | 222.368 | 204.064 | 204.064 |
| 51 | 238.880 | 204.096 | 206.016 |
| 52 | 249.120 | 205.696 | 206.016 |
| 63 | 253.216 | 224.288 | 224.544 |
| 64 | 218.400 | 204.064 | 205.440 |
| 65 | 283.904 | 244.992 | 245.024 |
| 80 | 285.952 | 238.592 | 238.848 |
| 88 | 288.000 | 251.200 | 253.216 |
| 96 | 279.840 | 253.216 | 253.248 |
| 97 | 322.848 | 322.848 | 322.848 |
| 128 | 306.464 | 306.464 | 306.944 |
| 129 | — | 535.840 | 537.888 |
| 256 | — | 519.456 | 521.376 |
| 1024 | — | 1850.180 | 1846.560 |

129/256/1024 为保留 prefill route 的参考点。128→129 的 Graph 延迟仍有
306.944→537.888 µs 的阶跃，来自未改动的 C128 tile 覆盖范围变化；不宣称 prefill 全域最优。
最终 `--profile` 已经用 NCU 验证只捕获一个目标 kernel；T=96 的静态 shared memory
为 50,176 bytes、block=384 threads，和所选配置一致。

### 4.1.5 OP-13 — symmetric sliding-window Attention qualification

公开域保持 D128/Q32/KV8、W=1..16、B=1..8、window=2048/4096；DFlash2 使用 W=K+1。
可见集仍严格为 `abs(key_position-query_position)<window`，包含所有有效临时 query K/V，
不把 envelope 或推荐 W=8 当作数学限制。Cached K 按 BF16、cached V 按实际 FP16 codec
值解释；query K/V 是独立的 BF16 输入，context 全程只读。

旧 split 路线把未归一化 numerator 写成 BF16，再归并、归一化并再次转 BF16。在新增的
短实际 context / 宽 Graph envelope 与 ragged batch 用例中，这个中间舍入产生额外误差。
现改用 FP32 partial numerator，m/l 仍为 FP32，最终只写 BF16 out。共享 contraction
仅增加 FP32/BF16 partial storage 的编译期选择；已有 growing-context Attention 保留其
BF16 specialization，没有复制或改变共享的 QK/Softmax/PV 算法。零 valid 的 direct 路线
也修正为不依赖无效物理尾部 `positions[0]`；split 路线由归并 kernel 精确写零，无需额外选择 L。

验收仍使用完整、独立的 FP64 数学 oracle，没有仿制 FP16 P/V、partial 或输出舍入。
旧 gross cap `3e-4 + 0.003*max_abs(reference)` 对部分正确 BF16 输出也过紧：例如
reference=-0.529292 对应最近 BF16=-0.53125。BF16 最终舍入的相对误差上界约 1/256，
新统一 criterion 为 rL2<=0.0023、gross<=`3e-4 + 0.0045*max_abs(reference)`，拒绝非有限
结果；gross 显式包含最终 BF16 和 FP16 P/V 算术误差预算，rL2 从旧 0.00395 收紧。
旧 BF16 partial 路线在此 criterion 下仍失败；最终 FP32 路线的全部 352 个比较通过，
最大 rL2=0.001953125、gross/limit=0.755 以下。

比较 32/64-key tile、4/8/16/32 splits、1/4 merge warps、direct/split 后，2048 路线
保留 KeyBlock=32，merge 改用 4 warps；每个 partial CTA 的 warps=`ceil(W/4)`。
Direct 上限在 W=9..16 且 B=6..8 时为 128，其余为 96。Split 上限如下，最后再限于
`max(1,ceil(min(max_context,2047)/32))` 个实际可能存在的 context tiles：

| W | 基础 split 上限 | 较短 context 的限制 |
|---|---|---|
| 1..4 | B=1..2:32；B=3..6:16；B=7..8:8 | 无附加限制 |
| 5..8 | B=1:32；B=2:16；B=3..8:8 | B>=6 时再限于 max(4,floor(context_tiles/4)) |
| 9..16 | B=1..2:16；B=3..5:8；B=6..8:4 | B=2 时再限于 max(4,floor(context_tiles/2))；B>=3 时除数为4 |

Direct 为 1 kernel、零 scratch；split 为 2 kernels。Workspace query 扫描请求的 W 区间，
正确处理 4/5、8/9 等上限下降点，不能仅查询区间最大 W。2048 profile 的全域最大 scratch
为 10,649,600 B（W16/B5）；W16/B8 从 34,603,008 B 降至 8,519,680 B。4096 保留原
96-context/32-split/1-warp 调度，同时采用 FP32 partial，故其 W16/B8 的最大 scratch
变为 68,157,440 B；现有调用通过公开 query 获得容量。本项没有重新宣称 4096 性能最优。

Qualification 覆盖所有 128 个 W/B 组合及 dispatch 边界的完整 active-split 工作，
valid=0/1/短/满、NaN query 尾、无效 position 尾、八个 state lanes 的置换、padded ring、
2047/2048 距离边界、query 右侧可见性、wrap、多圈和 262144 位置。Graph 在固定 envelope
内改变实际长度、valid、lanes 和 query V 后再次与 oracle 比较；所有 cache/input 字节保持
不变，输出尾为精确 BF16 +0，guards、scratch scope/high-water 和非单调区间容量一致。
4096 的短/长 context、wrap、batch、zero prefix 与 Graph 回归通过；共享
`ninfer_softmax_attention_test` 通过。

性能证据：RTX 5090、CUDA 13.1、Release/sm_120a，非零 K/V，相同 cache codec，测量完整
公开 Op。下表为每次 flush 256 MiB 后的单次 Graph median（μs）；复测 warmup=20、repeat=200。

| W / B / context | 旧 BF16 partial | 新 FP32 partial / 调度 |
|---|---:|---:|
| 2 / 1 / 512 | 11.584 | 11.552 |
| 16 / 1 / 2048 | 19.744 | 19.712 |
| 2 / 8 / 2048 | 58.624 | 52.480 |
| 8 / 8 / 2048 | 70.944 | 58.624 |
| 16 / 8 / 2048 | 87.296 | 60.384 |
| 8 / 8 / 512 | 27.936 | 23.808 |
| 16 / 8 / 512 | 40.192 | 25.856 |

另测全部 120 个 DFlash2 W/B 点在 context=96/2048 的曲线，以及 0/97/128/129/512 的
W 全域边界、eager anchors、固定 max_context=262144 下的短实际 context。基准新增
`--envelope-max`、`--graph-calls` 和 tile/merge/workspace 元数据；warm 使用 32 次调用
组成的 Graph，按每次 Op 归一化，避免主机提交间隙。W2/B1、W8/B8、W16/B8 的 warm
2048-context 复测分别为 10.206/24.307/38.698 μs。Cold bundle 只在整个 bundle 前 flush，
不能把后续调用称作 cold。以上是 Op 性能证据，尚未建立正式 Engine 端到端收益。

### 4.1.6 OP-14 — cyclic KV prefix append qualification

合同保留 BF16 `[128,8,T,B]`、任意正物理 T、B=1..8；`counts[b]` 和 host envelope 决定
实际提交前缀，不能从物理 T、推荐 K=7 或 draft backbone W 推导 commit count。K 精确复制
原始位模式，V 按 `FP16_RNE(BF16 input)` 写入；capacity=2048/4096，lane、ring padding 和
absolute-position wrap 由显式 cache view/metadata 决定。明确零 count 不消费 positions/K/V，
也不要求 `positions[0]` 表示有效起点；frontier 仍由调用方拥有。

原 cyclic kernel 每线程复制16元素、每256-thread CTA处理四个 token，短前缀的 CTA 数偏少。
现统一每线程一个8元素/16-byte向量，删除 cyclic 双向量串行路径；在获取 lane、position
或 K/V 之前按 device count 退出无效线程。`max_count*B<=128` 使用64-thread CTA（半个
token/CTA），更大 envelope 使用128-thread CTA（一个 token/CTA）。分块与 stride 分别
取自 max_count 和物理 T，故固定宽 buffer 的短前缀不会多启动按物理宽度计算的 CTAs。
2048/4096 使用同一方案。比较了8/16元素向量和32/64/128/256 threads的七个有效候选，
以及单请求17..2048前缀；较大的前缀也选择8元素路径。Op始终零 scratch；max_count>0
为一个 kernel，max_count=0 无 kernel，后者与 device count=0但envelope>0的单-kernel
空提交不同。

独立 exact oracle 从输入位模式计算所有目的地址和 FP16 RNE codec，逐字节比较整个
cache（含 padding、未选 lanes、未提交尾），不拿另一个 GPU 路径作参考。新增180个
用例覆盖全部128个 Wc=1..16/B=1..8组合；混合 counts=0/1/短/满、奇数宽度、lane重排、
跨 ring 边界与多圈、持久 cache 连续更新均通过。Graph在相同地址上修改 counts、lanes、
positions、K/V后重放；NaN输入尾和负数position尾不能污染cache；所有输入和guards不变。
满W16/B8用例枚举全部有限BF16位模式，包含signed zero、FP16下溢/次正规/溢出等codec边界。
另覆盖physical T=16/upper=7、T=4097/upper=16，以及17/65/127/128/129/2048前缀；
4096 profile覆盖Wc=1/3/8/9/16/17、B=1/8和4096满前缀。原paged/full各storage回归也通过。

性能证据：RTX 5090、CUDA 13.1、Release/sm_120a。基准输入非零，C是实际device count；
公开Op的cold测量在每次调用前flush256MiB，warm将32个调用组成Graph并按每次Op归一化。
完成Wc=1..16/B=1..8与适用C=0/1/3/7/8/15/16的1088个cold/warm点，另测eager、
127/128/129调度边界、长前缀、短upper/宽物理buffer、host-zero-envelope和4096回归。
下表为warm median，单位μs，warmup=10、repeat=100：

| Wc / B / C | 原路线 | 新路线 |
|---|---:|---:|
| 1 / 1 / 1 | 1.455 | 1.407 |
| 8 / 1 / 8 | 2.196 | 1.454 |
| 16 / 1 / 16 | 2.208 | 1.445 |
| 1 / 8 / 1 | 1.651 | 1.447 |
| 8 / 8 / 8 | 2.208 | 1.460 |
| 16 / 8 / 7 | 2.088 | 1.565 |
| 16 / 8 / 16 | 2.231 | 1.585 |

Cold Wc8/16的满前缀从约5.4μs降至约3.36μs；单列cold约3.36μs持平。较大batch、
15/16列满或近满前缀仍有5.1..5.4μs的cold p95，其他CTA候选没有消除该尾部；不把median
改善写成尾延迟保证。单请求2048前缀最终cold/warm为11.488/4.304μs，4096满前缀cold
为23.840μs。`--max-count`、`--graph-calls`及max_count/Graph节点元数据已进入公开基准；
host-zero-envelope报告零GPU时间，device-zero-count保持真实kernel计时。本项未测Engine
端到端收益。

### 4.1.7 OP-15 — LinearPair 依赖退出复核

DFlash2 的 context 路线已由 OP-07 的 `context_kv_materialize` 直接消费五组 QKV-parent
K/V row views，完成 projection、K norm/RoPE 和 cache store；其实现没有 `linear_pair`
调用。原 composed 路线因精度和性能选择退出后，独立 LinearPair 不再是该 DFlash2 context
方案的运行依赖，本项按依赖退出关闭，不保留一条假设的 DFlash2 composed 优化路线。

真实运行调用逐一确认：

- `qwen3_6_27b/impl/variant.cpp::mtp_kv_projection` 使用 `[1024,5120]` 的两路 MTP
  projection，仍由原 LinearPair 正 T 合同和 85/86、960/961 调度覆盖。
- `qwen3_6/impl/runtime/dflash_impl.h` 的现有 DFlash context 在已启用该能力的35B-A3B
  Variant 上使用 `[1024,2048]`；27B Variant 的既有 DFlash capability 为 false。这不代表
  将 DFlash2 接到已有 DFlash 算法中。
- `[1024,2048]` 的 exact-T Pair 使用 OP-07 提取的 `w8_small_t_mma` 默认列映射和专用
  Pair row/epilogue；其 concat 路线也包含 OP-12 涉及的共享 W8 MMA 头文件。因此执行一次
  现有两种 profile 的数值回归，确认共享改动没有破坏这些仍在使用的消费者。

RTX5090 / sm_120a、CUDA13.1 下，`ninfer_linear_pair_w8_a16_test` 通过：K=5120 的10个
points包括T=1/24/85/86/87/512/960/961/962/1024；K=2048覆盖134个既有调度边界/内部
points至4096。两输出均直接比较解码signed W8 code和实际FP16 scale后的独立FP64完整dot，
按既有抽样行/列验收，同时扫描全输出的finite/poison，检查guards和输入/权重只读。
统一criterion保持 `(rL2,gross_abs,gross_rel)=(0.0029,0.004,0.0038)`；两profile最大rL2
分别0.002134和0.002228以下。本项不修改 LinearPair 数学域、已有调度或声称新的性能收益；
DFlash2 context 新 extent 的完整数值、Graph和性能证据归 OP-07。

### 4.1.8 OP-16 — ragged feature-prefix preparation qualification

公开 Op 继续支持正 W 和8元素对齐的 D，不按 K=7 或 W=8 收窄 generic gather。
DFlash2 用 `[25600,W_c,N_lane] -> [25600,W_c,B]`；N=ends-starts 决定复制的输入前缀，
N=W_c=16时完整保留all-accept的最后一行。source列/lanes可以有显式stride；输出
BF16位模式精确复制，尾部写BF16 +0。Positions对有效行写start+j，对尾部重复最后live
position；N=0重复start。Counts精确写N。三个输出彼此独立且不覆盖输入；只读输入允许
相互别名，没有为了并行实现而禁止合法的starts/ends共用。

原性能路径每列只有一个128-thread CTA，逐线程循环搬运完整feature列。比较11个block/
feature-tile配置及flat/3D grid后，统一每CTA处理1024个BF16元素（2KiB），128个线程各
搬运一个16-byte向量。Grid的x/y/z分别表示column、batch、feature tile，避免kernel内
对tile数做整数除法；Z受CUDA grid上限约束时通过grid stride覆盖更大的generic D。
Positions只由该列的feature tile 0/thread 0写入，counts再限定column 0，避免metadata
多写入者。所有模式均为1 kernel、零scratch；N=0仍必须清零完整输出并生成metadata。

新增独立 `ninfer_prepare_ragged_prefix_test`，从scatter测试移出该Op的组合调用，保留
scatter自身检查。Exact oracle逐元素计算source地址、前缀、position和count，不调用另一个
GPU Op。全部128个W_c=1..16/B=1..8组合通过，另测zero/one/full前缀、奇数宽度、八个
lane置换、column/lane padding、任意BF16位模式（含NaN payload），以及Graph重放时
同时改变source、lanes、starts、ends。输出和metadata从poison完全覆盖；source/输入
metadata及所有guards保持不变；zero-prefix Graph也覆盖starts和ends别名。D=8/24/16384
及W_c=17回归通过。`ninfer_scatter_test`通过。

性能证据：RTX5090、CUDA13.1、Release/sm_120a，完整公开Op；D=25600的全部W_c/B组合
分别测full/one/ragged/zero的cold单次Graph和warm 32-call Graph，共1024点，另有eager、
D=16384与W_c=17 anchors。Cold每次flush256MiB；warm按每次调用归一化，warmup=10、
repeat=100。代表median（μs）：

| Wc / B / 前缀 | 旧cold → 新cold | 旧warm → 新warm |
|---|---:|---:|
| 1 / 1 / full | 9.440 → 3.360 | 3.602 → 1.332 |
| 8 / 8 / full | 11.520 → 7.328 | 3.901 → 2.272 |
| 16 / 8 / full | 13.632 → 9.472 | 4.138 → 3.433 |
| 16 / 8 / ragged | 13.568 → 7.456 | 4.252 → 3.287 |
| 16 / 8 / zero | 5.376 → 5.376 | 3.050 → 3.051 |

Cold部分小shape的p95仍可跨越约3.36/5.4μs区间；表中是median，不代表尾延迟保证。
独立公开基准 `ninfer_prepare_ragged_prefix_bench` 已注册并记录实际live columns、Graph
nodes/calls、logical useful bytes和median/min/p95；所有候选开关已移除。收益仅在Op层测量。

### 4.1.9 OP-17 — quantized embedding qualification

Op的列数合同保持原样，没有新增T<=128限制。DFlash2涉及W8和row-FP8 `[248320,5120]`；
普通ids来自`0..248076`，draft mask为248070。旧基准将D=5120绑定MTP的K<=5，只默认
测到48列；原测试只使用少量重复IDs，且mask anchor仍是已有DFlash的248077。本项补齐
DFlash2真实mask、普通token、物理table末行、全1..128列及较长prefill。

W8/D5120现一次读取四个signed codes，共用所属32元素group的实际FP16 scale，在FP32
相乘并直接写BF16；T<=128每token10个128-thread CTA，T>128每token5个128-thread CTA。
省去原逐元素通用索引的64-bit除法，降低scale读取/转换与CTA数量。输出4-byte对齐时用
BF16 pair store，自然2-byte对齐时使用同一计算的scalar store；code plane不能word-load
时保留既有byte reader，未收窄合法对齐域。W8/D2048和Q6原计算路线保持不变。

FP8去掉共享row/scale的shared-memory广播与barrier，每线程从相同represented inputs
读取scale。T<=176采用10个128-thread CTA，T>176采用5个128-thread CTA；原48/49分界
退出调度。W8和FP8分别比较scalar/word decode、block数、thread数以及129..2048的
prefill分界，保留上述生产路线；候选开关和未选实例已移除。所有非空调用1 kernel、
零scratch，数学公式仍是独立decode(code)*stored_scale。

Numerical criterion修正有明确的格式原因：旧相对界0.0038不能覆盖正确BF16 RNE。
合法W8 code=1、FP16 scale=0x3c04给出ideal=1.00390625，正确BF16=1.0，相对误差为
1/257≈0.00389105。FP8有限code/合法BF16 multiplier的边界同样复现旧criterion失败。
统一pointwise界现为absolute=`2^-134`（半个最小BF16 subnormal）、relative=`2^-8`，
直接比较未舍入的FP64 ideal，拒绝非有限结果；没有把输出舍入或GPU路径复制进oracle。
W8 code仍限定[-127,127]，FP8使用所有有限code，zero multiplier行只放合法zero codes。

独立测试逐元素验证完整5120行输出：两格式各覆盖T=1..128，Graph覆盖token/seam anchors
及129/175/176/177/256/1024/2048，并在同一Graph中将普通IDs切换为mask-heavy IDs。
较长输入按128列块改变ID序列，避免简单重复掩盖索引错误。FP8含BF16最小subnormal和
最小normal row scale，W8含FP16 subnormal scale和最终BF16 halfway case。W8的2-byte
输出、FP8的4-byte输出均在小/大路线验证，前导guard不被覆盖；全部ids、选中weight rows/
scales和payload guards保持不变。Q6、W8/D2048和dense BF16回归通过。349个pointwise
比较的最大error/limit=0.996109，dense复制按exact判定。

性能证据：RTX5090、CUDA13.1、Release/sm_120a，完整公开Op。基准使用完整物理table
shape、非均匀合法code planes与正stored scales；不再用常数填充。普通模式两格式各测
1..128列与8个prefill anchors；masked模式覆盖全部120个W=2..16/B=1..8组合。Cold
单次Graph和warm 32-call Graph共1024点，另测eager和两个未改动格式的benchmark smoke。
每次cold前flush256MiB，warm按每个Op归一化；warmup=10、repeat=100。

| 格式 / T / IDs | 旧cold → 新cold（μs） | 旧warm → 新warm（μs） |
|---|---:|---:|
| W8 / 8 / normal | 3.328 → 3.360 | 1.574 → 1.306 |
| W8 / 64 / normal | 5.376 → 3.360 | 2.471 → 1.580 |
| W8 / 128 / normal | 7.424 → 5.376 | 3.682 → 1.831 |
| W8 / 1024 / normal | 34.048 → 13.600 | 20.129 → 4.973 |
| W8 / 128 / W16 masked | 7.424 → 5.024 | 3.659 → 1.829 |
| FP8 / 8 / normal | 5.376 → 3.328 | 1.825 → 1.301 |
| FP8 / 128 / normal | 7.392 → 5.376 | 2.129 → 1.764 |
| FP8 / 128 / W16 masked | 5.376 → 5.216 | 2.467 → 1.832 |
| FP8 / 1024 / normal | 13.632 → 13.568 | 5.088 → 5.056 |

部分cold点仍有跨约3.3/5.4μs区间的p95，不能把median视为尾延迟保证；warm各点p95
未超过其median的1.5倍。基准CLI以`--format`选格式、`--profile`采集单Op，另有
`--id-pattern`、`--block-width`、`--execution`、`--cache`、`--graph-calls`、`--csv-out`。
报告unique IDs与logical bytes，体现mask复用；删除将重复逻辑读写量标成DRAM READ%的
旧指标。`bench/README.md`已更新。以上不构成Engine端到端速度声明。

### 4.2 Target verify 与 feature capture

Target 是 64 层：16 个 full-attention、48 个 GDN、64 个 MLP。工作域从旧主要关注
T=56/64 的点扩为 W=2..16、B=1..8。Target full attention 的 Q/gate 宽度为 6144，
K/V 宽度 1024；GDN 为 Hq=16/Hv=48、D=128、FP32 persistent recurrent state。
Groupwise 和 nvfp4 的实际格式分别按 artifact binding 使用；nvfp4 中末八层 MLP 的 FP8
profile 也在范围内。不能把别的 target 的相似 geometry 测量当作本表的全部证据。

| ID / 状态 | Op 与合同 | 精确 profile / 新输入 | 复核与方案 |
|---|---|---|---|
| OP-19 待复核 | [scatter_bf16_batch](../../include/ninfer/ops/scatter.h) | 五次 `[5120,W,B]` capture 写入 `[25600,W,N_lane]` slices | 检查目标 view 的 stride、五个 feature offset、lane 和 valid columns；exact copy、不覆盖其他 slice/无效位置；测完整 capture 成本。 |
| OP-20 待复核 | [attn_input_proj](../../include/ninfer/ops/attn_input_proj.h) target form | Q4/Q5 与 row-FP8 target profiles，T<=128 | 复核新列数的 grouped projection、A16/A8 路径、column tile/尾部和 workspace；Q/gate/K/V 均对各 represented profile 的 oracle。 |
| OP-21 待复核 | [causal_softmax_attention](../../include/ninfer/ops/softmax_attention.h) | D256/Q24/KV4，W=2..16、B=1..8，已注册 target cache codecs | 重点：H24 B1 的特殊 chunked 选择目前仅 W<=8，W=9..16 会走 Prompt。比较这些宽度的短/长 context 和完整 append-and-attend，重选 Prompt/ChunkedSmallT 或改进 verify route；valid columns、causal mask 和 KV codec 一并验证。 |
| OP-22 待复核 | [gdn_norm_gating_proj](../../include/ninfer/ops/gdn_gating_proj.h) | D5120 norm + BF16 control `[96,5120]`，g/beta FP32 `[48,W,B]` | 已接纳正矩阵列数；补 T<=128 和 small/MMA split seam，完整 norm→control→gate/beta oracle，保留 FP32 public outputs。 |
| OP-23 待复核 | [gdn_input_proj_conv_record](../../include/ninfer/ops/gdn_input_proj.h) | Groupwise Q4/Q5 双 parent、row-FP8 `[16384,5120]`；W<=16/B<=8 | 已有 ReplaySSM 域。复核 aggregate 15/16 的 grouped seam、FP8 B1 W=9/10 的 fused/A8 seam及新增大 batch 列数；验证 request-local conv history、valid columns 和 raw conv records，无 persistent state 提前提交。 |
| OP-24 待复核 | [gated_delta_net_replay_record](../../include/ninfer/ops/gated_delta_net.h) | BF16 Q/K `[128,16,W,B]`、V/out `[128,48,W,B]`，FP32 g/beta/state | 已支持 W=2..16/B=1..8；覆盖不同 Q_b 的 FP32 recurrence、raw K/V/gate records 与无效后缀，保持 source state 不变。比较长 W 的串行 recurrence、launch 分工和 record 写入成本。 |
| OP-25 待复核 | [gated_rmsnorm](../../include/ninfer/ops/gated_rmsnorm.h) | D128、48 heads、W*B<=128 的 recurrent output/z | 完整 norm/gate 公式及 head/token flattening；复核新增 extent 的线程布局与内存吞吐，不能用单纯 RMSNorm 的证据替代。 |
| OP-26 待复核 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) target MLP | Q4、NVFP4、row-FP8 `[34816,5120]`，T<=128 | 分别覆盖实际绑定格式和 activation policy。重点复核 Q4 materialized、大 T NVFP4 M64/M96/97+ 切换、末八层 FP8；比较完整 fused/composed 成本并直接验证完整公式。 |
| OP-27 待复核 | [linear_add](../../include/ninfer/ops/linear_add.h) target output | Q5/row-FP8 `[5120,6144]`；Q5/NVFP4/row-FP8 `[5120,17408]`，T<=128 | Attention/GDN 与 MLP 两个 C profile 分别复核；包括 FP8 A16/A8、Q5 C32 和 NVFP4 W4A4。验证 residual in-place 与实际量化边界，不能只测不带 add 的 Linear。 |
| OP-28 待复核 | [linear](../../include/ninfer/ops/linear.h) target full head | W8/row-FP8 `[248320,5120]`，BF16 logits `[248320,W,B]` | 保持 dense target output 合同。重点复核 W8 64/65 之后 route、FP8 A16 GEMM 和新尾 tile；分别测到 T=128，不能从 proposal top-k 推导本 Op 性能。 |
| OP-29 待复核 | [argmax](../../include/ninfer/ops/argmax.h) | target logits `[248320,W,B]`，valid_rows=248077 | 验证所有列、padding 排除和最小 global-id tie；复核 T<=128。只在 acceptance envelope 允许时作为 raw-greedy target 输入。 |
| OP-30 待复核 | [rope](../../include/ninfer/ops/rope.h) target form | Q/K D256、rotary_dim=64，W<=16/B<=8 | 补真实 target position tensor 布局、逐 lane offset 和 partial MRoPE 数值；DFlash2 的一维 D128 RoPE 不替代此 Op。 |
| OP-31 待复核 | [sigmoid_mul](../../include/ninfer/ops/sigmoid_mul.h) | target attention/gate `[6144,W,B]` | 独立 sigmoid-gate 公式、原地输出与新尾 tile；按 16 个 full-attention layers 的调用范围复核性能。 |
| OP-32 待复核 | [offset_i32_positions](../../include/ninfer/ops/position.h) | I32 source/destination vectors 与 scalar delta；target Text/Vision 每 lane 的 W 列位置 | Exact 验证各 lane 调用使用匹配的 vector view/scalar offset，不能将不同 delta 的 batch 当作同一 vector 调用；DFlash2 context/query 的 F+i 绝对 cache position 保持独立。 |

## 5. 准备与提交相关的直接复用 Ops

这些项目仍逐项关闭，避免只扩 proposal 而遗漏 verify/commit 的 K+1 行。没有独立性能疑问
时使用对应行为验证；不为它们附加 benchmark campaign。

| ID / 状态 | Op 与合同 | 新输入与完成条件 |
|---|---|---|
| OP-33 待验证 | [prepare_masked_block](../../include/ninfer/ops/prepare_masked_block.h) | `[W,B]`，W=2..16，mask id=248070；exact 验证 anchor、K 个 masks 和 F..F+K positions，区分 proposal 与 target valid columns。 |
| OP-34 待验证 | [speculative_prepare_verify_ids](../../include/ninfer/ops/speculative_round.h) | drafts `[K,B] -> ids [K+1,B]`，P_b=0..K；exact 验证 anchor/live draft prefix 和安全尾部。若选择同时生成 positions 的同族 form，按同一 alignment 验证，不建立第二份逻辑。 |
| OP-35 待验证 | [speculative_select_accepted_hidden](../../include/ninfer/ops/speculative_round.h) | hidden `[5120,W,B]`，N>0 时 selector=N-1；exact gather 覆盖最大列和非满提交。N=0 的 schedule 不以 -1 调用本 Op。 |
| OP-36 待验证 | [scatter](../../include/ninfer/ops/scatter.h) continuation form | selected hidden `[5120,B]` 写入 owning state slot；exact copy 和 selector 重排。与 OP-35 联合覆盖短提交，保留其他 slots。 |
| OP-37 待验证 | [gdn_replay_fold](../../include/ninfer/ops/gdn_replay.h) prepared Op | 27B 的48个GDN层，records W<=16，commit_columns=N_b；独立状态 oracle 检查 FP32 recurrence 和 conv tail_3，N=0 无读写、N=W 全提交、部分提交与 source/destination slots。这是扩大可提交前缀引入的直接回归点。 |

## 6. Qualification 执行要求

### 6.1 正确性与 route 覆盖

- [ ] OP-01..08 的公共合同、wrapper、执行域和 capacity query 一致。既有 tests/bench fixtures
  改为显式 K/W/B，删除固定推荐宽度是唯一合法输入的断言，不建立兼容 aliases。
- [ ] 对有宽度语义的 Ops，所有支持 W 至少有代表性正确性覆盖；补 B=1/B=8、奇数宽度、
  非满 tile 和相同 T 不同 `(W,B)`，例如 `(W=8,B=2)` 与 `(W=4,B=4)`。矩阵重归约的
  oracle 覆盖每条新生产 route 和其实际分界，不用完整模型输出替代。
- [ ] 状态/稀疏采样项覆盖 device counts 与 physical width 分离、P=0/K、N=0/L、首次/中途
  拒绝和 all-accept；FP32 q 与 token/state effects 分别验证。确定性 codec、copy、索引
  用 exact criterion，浮点公式用声明的数值 criterion，不要求不同 K 产生同一 draft。
- [ ] 每个新增或改变的 workspace route 验证同址 Graph capture/replay、scope lifetime 和
  query 上界。Capacity 取区间内所有可达 routes 的最大值，不能只测最大 K/B 一个点。

### 6.2 性能比较与记录

在 RTX 5090、sm_120a、CUDA 13.1 上排他进行 GPU 计时。编译采用 `cmake --build <dir> -j`。
沿用适用的 public Op benchmark、CUDA Graph 与明确的 cache 条件，计入完整 Op 的所有
launch、scratch 读写和后处理；不把 isolated contraction 的改进直接标成 fused Op 改进。

先用短 K、推荐 K=7、最大 K=15、B=1/B=8 及会改变选路的 seams 区分候选，再补足最终
routes 在实际产品域的曲线。对 state/attention 保留 W/B/context/counts 维度，对纯矩阵按
T/U 去重。只在一个 kernel 已由 benchmark 识别、且 kernel-level 答案会改变当前选择时使用
ncu；本阶段不要求尚未接入的 Engine 或 nsys 作为前提。

每项关闭时只记录相关 geometry/format、K/W/B 或列数、context/counts、选定 route、
representative latency、数值/状态检查结果和必要限制。对实现未变且证据已覆盖的项目注明
复用依据；不要求重复所有旧实验，不从 K=7 的历史数据声称 K=15 性能已通过。

### 6.3 顺序与本阶段退出条件

1. 先落实 OP-01..08 的 extent/数学合同及对应 oracle；workspace 接口与执行一起修正。
2. 优先处理会错误索引的 conv、top-k、context 和 accept，随后用完整 Op measurement
   重选重投影/head、context materialization 和 target attention/GDN/MLP 的关键 routes。
3. 完成 OP-09..32 的逐项复核和 OP-33..37 的行为验证；对未修改的小 Op 按实际风险控制证据。
4. 每项关闭后回写最终 public contract、tests、benchmark 和必要的 active reference；
   旧 exact dispatch 仅在仍为合法且有证据的内部特例时保留，移除已被替代的项目自有路径。

本阶段完成时：37 项各有明确结果；K=1..15 的实际调用域无已知合同/索引/workspace 缺口；
新增或修改的数值/状态 routes 通过独立 oracle；关键 route 选择有 Op 级性能依据。入口的
固定七步 validation、Program 资源和提交/Graph 接线由 support plan 跟进。此时可以进入
正式 Engine 集成，但不能宣告 DFlash2 端到端产品或任何新 K 的 acceptance 已完成。

当前已关闭 OP-01..03，其余项待完成；本文不保留旧 checklist 的完成表或固定七步的优化结论。完成此次
治理与后续 Engine 接入后，稳定合同回归既有 active references，删除临时 checklist。
