# Qwen3.8-27B DFlash2 Op 修正与重新 qualification checklist

> 状态：已恢复治理，先处理重新打开项；OP-01..22 的复杂度复核结果见下表。
> 37 项中：16 项保留完成，1 项依赖退出，5 项重新打开，15 项待治理。
> “重新打开”保留已有实现和 qualification 证据，但不再宣称该项已整体验收。

算法以 [DFlash2 algorithm](2026-09-04-qwen3.8-27b-dflash2-algorithm.md) 为准；
跨阶段集成由 [support plan](2026-09-04-qwen3.8-27b-dflash2-support-plan.md) 管理，
权重/format 由 [artifact reference](qwen3.8-27b-artifact.md) 定义。
**下表是逐项进度的唯一记录；下文条目解释结论和待办，不另维护“已完成到第几项”的计数。**

## 1. 状态与验收含义

本轮纠正将推荐 `block_size=8` 误作算法限制的问题，目标为 `K=1..15`。
Op 合同、索引、数值/状态、workspace 和有效输入域必须正确；实现应以明确的计算机制和
稳定的相关性能收益支撑其复杂度。曲线平滑、逐点最优、无台阶或模板数量最少均不是独立目标。
一个已通过 oracle 的实现，仍可能因为分派和特化缺少充分依据而需要重新打开。

| 状态 | 含义 | 后续工作 |
|---|---|---|
| 保留完成 | 已有相关 qualification，源码复核未发现需要重新处理的复杂度问题 | 保留方案；受后续共享改动影响时做相关回归 |
| 依赖退出 | 已不在 DFlash2 所选运行路径中；仍使用的消费者回归已有记录 | 由替代 Op 承担 DFlash2 验收，不再做本项假设性调优 |
| 重新打开 | 已有实现及数值/状态证据，但当前特化或分派粒度的收益依据不足 | 比较更少配置的方案，收敛实现或补足保留理由，并重验实际改变的路径 |
| 待治理 | 首轮实现已存在，本轮新域 qualification 尚未执行完毕 | 按该项合同和风险实施，不继承旧 K=7 的完成结论 |

下文复杂度复核依据源码和此前记录；恢复后的实现与新验证结果随各 Op 提交更新。
条目中的时延和测试结果均为此前治理的记录，适用 RTX 5090、sm_120a、CUDA 13.1；
只支撑所述 Op/调用组合，不建立 Engine 收益，也不证明保留方案在所有点最优。
模板规模按源码中的本 profile 实例入口/配置计数，不是编译后 cubin 符号数；
不将 launcher、kernel 主体、运行时分支和模板实例混为同一个数量。
现有大批 exact-T 特化有些来自首轮实现，本轮保留了它们；来源早不代表已证明合理。

| ID | Op / profile | 当前状态 |
|---|---|---|
| OP-01 | rmsnorm_dynamic_grouped_conv_prepare — BF16 norm + control projection；W2..16、B1..8 | 保留完成 |
| OP-02 | linear_dynamic_grouped_conv_add — W8 C4096 / C17408 → D5120；W2..16、B1..8 | 保留完成 |
| OP-03 | rmsnorm_rope（pair / single） — D128；pair Q32/KV8、W2..16/B1..8；single context T≤2048 | 保留完成 |
| OP-04 | rmsnorm_pack_tail — BF16 D5120；[5120,W,B] → [5120,(W−1)*B] | 保留完成 |
| OP-05 | linear_topk — W8 / row-FP8 full head 248320×5120；Q4 optimized 131072×5120；U≤120 | 重新打开 |
| OP-06 | candidate_selector_path — M16 / rank256；K1..15、B1..8 | 保留完成 |
| OP-07 | context_kv_materialize — 五组 W8 K/V parent views；decode Wc1..16/Bc1..8；single prefill≤2048 | 重新打开 |
| OP-08 | speculative_accept_sparse_drafts — K1..15/B1..8；physical vocab248320 / valid248077 | 保留完成 |
| OP-09 | linear — feature projection — W8 5120×25600；Tc≤128，prefill≤2048 | 保留完成 |
| OP-10 | linear — selector-hidden projection — BF16 256×5120；U≤120 | 保留完成 |
| OP-11 | attn_input_proj — draft QKV — W8 6144×5120 → Q4096 / K1024 / V1024；T≤128 | 保留完成 |
| OP-12 | linear_swiglu — draft MLP — W8 34816×5120 → 17408×T；T≤128 | 保留完成 |
| OP-13 | sliding_window_attention — D128/Q32/KV8；window2048；W2..16/B1..8 | 重新打开 |
| OP-14 | kv_cache_append_prefix — cyclic — BF16 K / FP16_RNE(BF16) V；Wc1..16/Bc1..8，长前缀保留 | 保留完成 |
| OP-15 | linear_pair — context 依赖 — DFlash2 context 已改由 OP-07 直接处理 | 依赖退出 |
| OP-16 | prepare_ragged_prefix — BF16 feature D25600；[D,Wc,N_lane] → [D,Wc,B] | 保留完成 |
| OP-17 | embedding — W8 / row-FP8 248320×5120；T≤128；mask id248070 | 保留完成 |
| OP-18 | rmsnorm — D5120 plain/offset；D256 offset Q24/K4；T≤128 | 保留完成 |
| OP-19 | scatter_bf16_batch — feature capture — 五次 D5120 slice → [25600,W,N_lane]；W≤16/B≤8 | 保留完成 |
| OP-20 | attn_input_proj — target — Q4/Q5 parents 7168×5120；row-FP8 parent14336×5120；T≤128 | 重新打开 |
| OP-21 | causal_softmax_attention — D256/Q24/KV4；五种 cache codecs；W2..16/B1..8 | 重新打开 |
| OP-22 | gdn_norm_gating_proj — BF16_CTRL parent96×5120；h BF16、g/beta FP32；T≤128 | 保留完成 |
| OP-23 | [gdn_input_proj_conv_record](../../include/ninfer/ops/gdn_input_proj.h) | 待治理 |
| OP-24 | [gated_delta_net_replay_record](../../include/ninfer/ops/gated_delta_net.h) | 待治理 |
| OP-25 | [gated_rmsnorm](../../include/ninfer/ops/gated_rmsnorm.h) | 待治理 |
| OP-26 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) target MLP | 待治理 |
| OP-27 | [linear_add](../../include/ninfer/ops/linear_add.h) target output | 待治理 |
| OP-28 | [linear](../../include/ninfer/ops/linear.h) target full head | 待治理 |
| OP-29 | [argmax](../../include/ninfer/ops/argmax.h) | 待治理 |
| OP-30 | [rope](../../include/ninfer/ops/rope.h) target form | 待治理 |
| OP-31 | [sigmoid_mul](../../include/ninfer/ops/sigmoid_mul.h) | 待治理 |
| OP-32 | [offset_i32_positions](../../include/ninfer/ops/position.h) | 待治理 |
| OP-33 | [prepare_masked_block](../../include/ninfer/ops/prepare_masked_block.h) | 待治理 |
| OP-34 | [speculative_prepare_verify_ids](../../include/ninfer/ops/speculative_round.h) | 待治理 |
| OP-35 | [speculative_select_accepted_hidden](../../include/ninfer/ops/speculative_round.h) | 待治理 |
| OP-36 | [scatter](../../include/ninfer/ops/scatter.h) continuation form | 待治理 |
| OP-37 | [gdn_replay_fold](../../include/ninfer/ops/gdn_replay.h) prepared Op | 待治理 |

先处理重新打开项。优先解决共同的 exact-T/live-column 问题，再处理 attention
的 chunk/split 规则和轻量 Op 的细分阈值；每个 Op 完成后仍单独提交。
共享 primitive 可以服务多个 Op，但逐项验证对应输出/状态，不能一次改共享模板就同时关闭所有消费者。
随后继续 OP-23..37；已有资格有效且实现未变的行为复用证据。

正式 Engine schedule、binder 接线、Program state/workspace composition、Graph inventory 和
端到端 acceptance/吞吐仍属于后续集成。改变 K 不要求重下 checkpoint、重转 artifact、
repack 权重或增加格式；较短 K 的结果不要求等于较长 backbone 的前缀。

## 2. 统一 workload、索引与数值边界

### 2.1 运行 extent

| 符号 | 含义 | 支持范围 |
|---|---|---|
| B | active requests | 1..8 |
| K | 启动选定的 draft count | 1..15，推荐 7 |
| W | proposal/verify 物理 block width | K+1，2..16 |
| T | backbone/target matrix columns | W*B，最大 128 |
| U | mask-head/selector matrix columns | K*B，最大 120 |
| P_b | 每请求可验证 draft extent | 0..K |
| Q_b | target live input columns | P_b+1 |
| A_b | accepted draft count | 0..P_b |
| L_b | provisional output count | A_b+1 |
| N_b | Frontend 最终提交数 | 0..L_b |
| W_c、B_c | context materialization 物理布局 | decode W_c=1..16、B_c=1..8；prefill W_c=1..2048、B_c=1 |
| T_c | context projection columns | W_c*B_c；prefill 最大 2048 |
| N_lane | pending-feature lane capacity | startup-fixed 1..8，且 >=B |
| S_state | StateImage slot capacity | startup-fixed，覆盖本轮 state selectors |

同一 Engine 的物理 W 由选定 K 决定；不能为全部配置计算有效 W=8 或 W=16 来规避扩展。
Proposal attention 可使用 valid_columns=W，target attention/GDN 使用 Q_b，context append
使用 counts=N_b。W_c 是 pending storage/gather extent，不能由 accepted draft count 替代；
all-accept 会提交 K+1 个 target input rows，必须保留最后一行 feature。

性能输入域以 `{(K+1)*B}` 和 `{K*B}` 的实际乘积集合定义。纯矩阵 Op 可以按相同列数去重；
卷积、attention、GDN 和状态 Op 必须保留 W/B 划分以及 device counts。已有 Linear 类 Op 的
任意正 T 合同继续成立；最大 128/120 是本轮产品性能范围，不是这些合同的新上限。

### 2.2 固定模型 geometry 与张量布局

```text
hidden H=5120; MLP I=17408; target feature H_f=25600
draft Q=32*128=4096; K/V=8*128=1024; five local layers; window/capacity=2048
conv groups=320; group width=16; taps=2; sides=2
selector candidates M=16; rank R=256
physical vocab=248320; valid global ids=0..248076; optimized head rows=131072

proposal residual       BF16 [5120,W,B]
finish_delta            BF16 [320,2,W,B]
packed mask hidden      BF16 [5120,U]
candidate ids/scores    I32/FP32 [16,K,B]
projected selector h    BF16 [256,K,B]
drafts / proposal q     I32 [K,B] / FP32 [16,K,B]
target logits           BF16 [248320,W,B]
pending features        BF16 [25600,W,N_lane]
```

Artifact Weight `[N,C]` 表示 `[output rows,input columns]`；runtime Tensor 的 ne[0] 连续。
Base kernel 保留 artifact `[side,tap,channel]=[2,2,5120]`，runtime view 为 `[5120,2,2]`；
codebook 保留 artifact `[vocab,rank]`，runtime view 为 `[256,248320]`。这些固定 8/16/256
等模型维度不能随运行 W/K 一起替换。

### 2.3 保持的数学与实现自由度

- Dynamic convolution 仅在当前 request block 内跨相邻位置，位置零没有 previous tap；
  finish 消费同一 prepare 的 output-side delta，无跨轮卷积状态。
- Proposal SWA 为 `abs(key_position-query_position)<2048`；当前 block 的有效 query 互相
  可见。Q/K 使用 plain head RMSNorm 和 theta=1e7 的 full-D128 split-half 1-D RoPE。
- Final norm-pack 跳过 anchor；top-16 使用 score 降序、同分 global-id 升序。Optimized
  head 的 row id 先映射为 global id，再供 selector 使用。
- Selector 是实际前驱条件下的逐位 walk；保留实际抽样的 FP32 q。逐 row、部分预计算、
  完整 lattice 均可作为同一 Op 的内部方案，不能换成独立逐位置 unary argmax 或 Viterbi。
- Sparse accept 使用实际 q 的 p/q rejection 和 `(p-q)+` correction；N 而非 P/A/L 决定
  最终持久状态提交。Proposal RNG purpose 与 target accept/correction/bonus 分离。
- Norm/conv prepare、projection/conv/residual、norm/RoPE、norm/pack、projection/top-k、
  selector walk 和五层 context materialization 保持闭合 public 边界。Private staging、
  reduction、tiling、launch 数由 qualified route 决定，不要求每个融合 Op 只有一个 kernel。
- Oracle 从 represented public inputs 及精确解码的 stored weights 计算完整公式；不可观察
  中间精度作为 implementation profile。OP-07 已去掉 raw K 的额外语义舍入；最终 BF16 K、
  `FP16_RNE(BF16)` V storage cast、public BF16 outputs 和实际 FP32 q 保持各自边界。

## 3. OP-01..22 逐项复核结论

### OP-01 — rmsnorm_dynamic_grouped_conv_prepare

合同：[dynamic_grouped_conv.h](../../include/ninfer/ops/dynamic_grouped_conv.h)。
复核落点：[bf16_dynamic_grouped_conv_prepare_plan.cpp](../../src/ops/dynamic_grouped_conv/bf16/bf16_dynamic_grouped_conv_prepare_plan.cpp)、[bf16_dynamic_grouped_conv_prepare_reduce.cu](../../src/ops/dynamic_grouped_conv/bf16/bf16_dynamic_grouped_conv_prepare_reduce.cu)。
验证入口：[ops/test_dynamic_grouped_conv_prepare.cpp](../../tests/ops/test_dynamic_grouped_conv_prepare.cpp)。

**已有行为：** 已解除 W=8 限制；prepared `[5120,W,B]`、finish_delta `[320,2,W,B]`，request 内 previous tap 与矩阵 T 分片分离。Query 与执行共用选择逻辑。

**实际实现与复杂度：** reduce 从 15 个 exact-W 实例改成容量 8/16 的两个布局，所有公开地址和 previous tap 使用运行时 W。Partial 合并 C24/C40 后保留 6 个 MMA tile；两阶段合计由 23 个实例降至 8 个。三节点执行和 split/scratch 规则保持不变。

**验收：** 既有完整 FP64 norm→projection→conv 测试通过全部 120 个 W/B，覆盖 prepared/finish_delta、request 边界、guards、workspace 和同址 Graph。测试入口不变，无需新增重复 fixture。相同 cold Graph 基准（RTX5090/sm_120a/CUDA13.1，warmup4/repeat25）比较全部 120 点：median 差值的中位数为 0µs；W2/B1 为 15.648→15.616µs，W8/B8 为 25.824→25.600µs，W16/B8 为 32.000→32.000µs。个别点约 2µs 档位变化，不主张全域提速。

**结论：** 完成本项清理；保留决定 MMA 并行度的有限 tile，不继续追逐局部交叉点。合同、输出数值判据与最大 scratch 2,621,440 B 均不变。

### OP-02 — linear_dynamic_grouped_conv_add

合同：[dynamic_grouped_conv.h](../../include/ninfer/ops/dynamic_grouped_conv.h)。
复核落点：[w8_dynamic_grouped_conv_add_materialized.cu](../../src/ops/dynamic_grouped_conv/w8/w8_dynamic_grouped_conv_add_materialized.cu)、[w8_dynamic_grouped_conv_add_plan.cpp](../../src/ops/dynamic_grouped_conv/w8/w8_dynamic_grouped_conv_add_plan.cpp)。
验证入口：[ops/test_linear_dynamic_grouped_conv_add.cpp](../../tests/ops/test_linear_dynamic_grouped_conv_add.cpp)。

**已有行为：** 真实 W 决定 conv 边界；两种 C 共用 BF16 projected scratch，完整 Op 为 projection + finish 两节点，最大 scratch 1,310,720 B。

**实际实现与复杂度：** 消除两套 T1..64 exact 表，复用现有支持 live-column 屏蔽的 W8 MMA。T≤88 按实际 8-column accumulator 容量选择 8..88；两种 C 合计 22 个实例，替代此前 128 个 exact 实例和 7 个 tiled 实例。T≥89 统一使用已有 K128 MMA，删除 C4096/T89..96 的 C32 特例；主 route 从六种收敛为两种。没有新增或修改共享 contraction，finish 保持运行时 W。

**验收：** 现有两种 C、共 240 个 W/B 的完整 FP64 projection→conv→residual 测试通过，包括原地 residual、request 边界、guards、Graph 和 workspace。相同 cold Graph（RTX5090/sm_120a/CUDA13.1，warmup4/repeat20）测量 240 点，两格式 median 差值的中位数分别为 0/−0.032µs。W16/B8：C4096 62.720→62.592µs，C17408 238.848→238.848µs；W8/B1 为 21.760→21.696 / 68.864→68.832µs。

**结论与取舍：** 完成本项清理。Runtime mask 和收敛上端 route 在部分点有代价：本轮最大 median 增量约 2.24µs，C4096/T91 为 58.432→60.672µs；未声称全部点提速，也不为这类局部差异恢复 exact-T 表。保留两种计算机制及自然 MMA 容量，零额外 launch，scratch 仍为 5120*T*2 B。

### OP-03 — rmsnorm_rope（pair / single）

合同：[rmsnorm_rope.h](../../include/ninfer/ops/rmsnorm_rope.h)。
复核落点：[launch.cu](../../src/ops/rmsnorm_rope/launch.cu)。
验证入口：[ops/test_rmsnorm_rope.cpp](../../tests/ops/test_rmsnorm_rope.cpp)。

**已有行为：** Pair 使用真实 W*B 和 positions；single 保留原 context 域。

**实际实现与复杂度：** 同一 kernel 主体按 Pair=true/false 各一个实例；固定 256 threads，每 CTA 8 heads，运行时 grid 覆盖 token。没有按 K/W/B 设置性能阈值。

**已有证据及其边界：** 既有 120 个 pair W/B 和 single 代表点直接通过完整 FP64 oracle、绝对大位置和 Graph 检查。Warm Graph32：pair W8/B1 由 3.242→2.001µs，W16/B8 由 3.294→2.429µs。

**结论 / 后续方案：** 本次未发现需要重新打开的分派复杂度问题；保留两种语义 form，不追加逐 W 调优。

### OP-04 — rmsnorm_pack_tail

合同：[rmsnorm_pack_tail.h](../../include/ninfer/ops/rmsnorm_pack_tail.h)。
复核落点：[rmsnorm_pack_tail.cu](../../src/ops/launcher/rmsnorm_pack_tail.cu)。
验证入口：[ops/test_rmsnorm_pack_tail.cpp](../../tests/ops/test_rmsnorm_pack_tail.cpp)。

**已有行为：** 运行时 W/K 索引，跳过 anchor，无额外 normalized block。

**实际实现与复杂度：** 一个 512-thread kernel 实例，零 scratch，无 shape 性能分支。

**已有证据及其边界：** 既有 120 个 W/B 的 FP64 norm/pack oracle、anchor 隔离和 Graph 通过。128/256/512-thread 比较无可辨别的稳定差异，保留统一配置；warm 每次 Op 约 1.6–2.1µs。

**结论 / 后续方案：** 保留完成；不以这类微秒级差异新增阈值或恢复候选。

### OP-05 — linear_topk

合同：[linear_topk.h](../../include/ninfer/ops/linear_topk.h)。
复核落点：[linear_topk.cpp](../../src/ops/linear_topk/linear_topk.cpp)、[w8_m64.cu](../../src/ops/linear_topk/w8_m64.cu)。
验证入口：[ops/test_linear_topk.cu](../../tests/ops/test_linear_topk.cu)。

**已有行为：** 任意正 U，较长矩阵按最多 128 列 chunk；不再从 U%7 推断 B，也不将未命中形状交给固定 56 列实例。保持 global-id tie、padding 排除和 optimized id map。

**实际实现与复杂度：** W8/FP8/Q4 小列数分别 24/24/16 个 exact-U producer，共 64 个；MMA 分别 9/10/8 个配置，共 27 个，尚不含共享 merge。存在 C40/48/80/96/112/120 等密集 tile，以及 FP8 U88/89 的 Ktile 切换。

**已有证据及其边界：** 既有三格式 U1..120 和跨 chunk 点直接通过 FP64 scores oracle、稳定候选/tie exact 验证、Graph 和容量检查。Full U7 的 grouped reduction 将总 scratch 14.36→1.79MB；U120 相比初始扩展候选明显更快。上述收益不能证明 91 个 producer 实例的逐项必要性。

**结论 / 后续方案：** 保留 grouped top-16 的流量收益与 Q4 小 U 的数值/性能要求；比较少量列容量桶、运行时 live columns 和有限 K-split/MMA 分界。优先审查 C40/112/120 与 FP8 C96 的双 Ktile；重验三个格式的 scores、ids、merge counters、尾列和 query。

### OP-06 — candidate_selector_path

合同：[candidate_selector.h](../../include/ninfer/ops/candidate_selector.h)。
复核落点：[candidate_selector_path_plan.cpp](../../src/ops/candidate_selector/bf16/candidate_selector_path_plan.cpp)、[candidate_selector_path.cu](../../src/ops/candidate_selector/bf16/candidate_selector_path.cu)。
验证入口：[ops/test_candidate_selector.cpp](../../tests/ops/test_candidate_selector.cpp)。

**已有行为：** 按实际前驱做 conditional walk，输出实际抽样使用的 FP32 q；K 是运行时循环及索引。

**实际实现与复杂度：** 两种有明确计算差别的方案：K≤4 direct walk，K≥5 并行 lattice + walk。三个非 exact-K kernel，只有一个 K 分界，最大 scratch 122,880 B。

**已有证据及其边界：** 既有全 120 个 K/B、前驱改变选择、greedy tie、混合温度、RNG lane 重排与 Graph 检查通过。K15/B1 direct→最终 greedy 21.760→11.552µs、stochastic 25.856→19.520µs。

**结论 / 后续方案：** 保留完成；direct/lattice 是串行链与预计算之间的实质取舍，不增加按 B 或采样模式拟合的分界。

### OP-07 — context_kv_materialize

合同：[context_kv_materialize.h](../../include/ninfer/ops/context_kv_materialize.h)。
复核落点：[launch.h](../../src/ops/context_kv_materialize/launch.h)、[materialize.cu](../../src/ops/context_kv_materialize/materialize.cu)。
验证入口：[ops/test_context_kv_materialize.cpp](../../tests/ops/test_context_kv_materialize.cpp)。

**已有行为：** 按 E=Bc*envelope.max_count 发射并映射回物理 Wc；device counts 决定写入。不可观察 raw K 不强制 BF16 舍入；最终 BF16 K 和 FP16_RNE(BF16) V 是存储边界。五层 ring 仍为每请求 40MiB。

**实际实现与复杂度：** 7 个 projection 配置、8 个连续 E 区间；E17..24 单独 C24，随后 25..32 又回 C16；另有 C80/96 和仅 E97..256 的融合路径。不存在逐 E 模板爆炸，但路由偏细，且 scratch 在融合区间降为零，使容量分析更复杂。

**已有证据及其边界：** 既有完整 FP64 projection→norm→RoPE 及 V codec oracle、所有 Wc/Bc、ragged/zero/wrap/Graph/只读检查通过。Wc8/Bc8 旧 direct→最终 386.304→75.008µs。C80 相比相关候选约 110–114→95–98µs，说明有实际收益，不能未经比较直接删除。

**结论 / 后续方案：** 从 K-split、普通 MMA、融合 store 三种机制出发比较更少 tile；重新判断 C24 回跳与 C80/96 的必要性。保持 exact stored scale 的逐组 FP32 应用和两输出 criterion；若共享 primitive 改动，复验实际消费者。容量仍覆盖区间内全部 count envelopes。

### OP-08 — speculative_accept_sparse_drafts

合同：[speculative_round.h](../../include/ninfer/ops/speculative_round.h)。
复核落点：[speculative_round.cu](../../src/ops/launcher/speculative_round.cu)、[speculative_round.cuh](../../src/ops/kernel/speculative_round.cuh)。
验证入口：[ops/test_speculative_round.cpp](../../tests/ops/test_speculative_round.cpp)。

**已有行为：** 实际 FP32 q 的 p/q rejection 与 (p−q)+ correction；P 决定 live target extent，A/L 是 provisional 结果，最终 N 提交仍属于 schedule。

**实际实现与复杂度：** raw/general 两种已有 sampling envelope 路线；K/W 为运行时量，raw 一 CTA、每 request 一 warp，general 复用 sampling pipeline。没有逐 K 特化或 shape 微调表。

**已有证据及其边界：** 既有 120 个 K/B 分别覆盖 raw/general，独立 FP64 概率与 exact token/state checks、P0、拒绝/all-accept、padding、history penalty、Graph/q 更新和 workspace 通过。K15/B8 raw Graph32 由 3.113→1.317µs/Op；收益不外推为所有 sampling 模式。

**结论 / 后续方案：** 保留完成；后续必须继续直接消费 selector 的 q，不能退为 one-hot。

### OP-09 — linear — feature projection

合同：[linear.h](../../include/ninfer/ops/linear.h)。
复核落点：[w8_small_t.cu](../../src/ops/linear/w8/w8_small_t.cu)、[w8_feature.cu](../../src/ops/linear/w8/w8_feature.cu)。
验证入口：[ops/linear/test_w8_a16.cpp](../../tests/ops/linear/test_w8_a16.cpp)。

**已有行为：** 任意正 T、直接 BF16 输出、零 scratch。

**实际实现与复杂度：** T1..56 的 56 个 exact-T 实例改为容量 8/16/24/32/40/48/56 的 7 个实例，live T 由现有 tiled contraction 屏蔽；统一 shared scale，不保留 T1..4 的读取特例。小 T 实现归本 profile 的 w8_feature.cu，删除旧 launcher 表和不再使用的 shared schedule。T57..64、65..128 的两种 K128 MMA 与 prefill 路线保留：它们分别解决不同 CTA 并行度问题。

**验收：** ninfer_linear_w8_a16_test 全部通过，包含 feature T1..128、prefill、独立完整 K dot 抽样 oracle、convenience/policy、Graph/guards 和既有 W8 Linear 回归。相同 cold Graph（RTX5090/sm_120a/CUDA13.1，warmup4/repeat20）128 点的 median 差值中位数为 −0.032µs；T1/8/16 约 95.5/97.5/99.6µs，T32 144.672→142.624µs，T56 210.176→205.952µs。部分尾 tile 有 runtime 屏蔽代价，T51 183.360→187.648µs，约 2.3%；不以此恢复 exact-T。

**结论：** 完成清理，56 个实例收敛为 7 个，任意正 T、原 A16 数值判据、单 kernel 和零 scratch 保持。T≥57 的实现未改变，其测量波动不作为新性能收益；不继续拟合 56/57 的局部交叉点。

### OP-10 — linear — selector-hidden projection

合同：[linear.h](../../include/ninfer/ops/linear.h)。
复核落点：[bf16_n256_k5120.cu](../../src/ops/linear/bf16/bf16_n256_k5120.cu)。
验证入口：[ops/linear/test_bf16_a16.cpp](../../tests/ops/linear/test_bf16_a16.cpp)。

**已有行为：** 任意正 U，完整 column grid，零 scratch；已消除每 64 列串行发射。

**实际实现与复杂度：** 同一 MMA 主体只有两种 schedule：U≤76 为 C8/16 K-warps，其余 C16/8 K-warps；实际 U 在运行时传入，没有 exact-U 表。

**已有证据及其边界：** 既有 U1..120 全元素 FP64 dot、prefill、Graph 和 BF16 family 回归通过。U120 cold 17.216→13.472µs，Graph32 12.639→9.189µs；分界同时比较了 cold 和重复调用。

**结论 / 后续方案：** 保留完成；两种资源布局加一个分界有充分工程依据，不以阈值不是 2 的幂为理由重开调优。

### OP-11 — attn_input_proj — draft QKV

合同：[attn_input_proj.h](../../include/ninfer/ops/attn_input_proj.h)。
复核落点：[w8_dflash2_attn_input.cu](../../src/ops/attn_input_proj/w8/w8_dflash2_attn_input.cu)、[w8_config.h](../../src/ops/linear/w8/w8_config.h)。
验证入口：[ops/test_attn_input_proj.cpp](../../tests/ops/test_attn_input_proj.cpp)。

**已有行为：** 任意正 T，直接三个 BF16 输出，无 packed output 或 scratch。

**实际实现与复杂度：** small-T 从 48 个实例改为 20 个：T1..16 保留编译期输入加载展开，T17..48 使用容量 24/32/40/48 的四个实例。前两个 MMA tile 采用全动态方案时，T9..16 的完整 Op 出现约 15–20% 回退，因此保留这段 exact-T 有实际依据。Scale access 不再在 T14/15 回跳，T9..16 统一 direct。上端三种 K128 与两种 prefill 配置保留原有并行度分工。

**验收：** 最终 `ninfer_attn_input_proj_test --dflash2-only` 通过全部 T1..128、prefill 和三个输出的完整 K FP64 抽样 oracle，以及同址 Graph/guards；原 A16 判据不变。该选项复用原 suite 的 DFlash2 用例，默认仍运行所有格式；其余格式回归已在本轮通过。RTX5090/sm_120a/CUDA13.1，cold Graph、warmup4/repeat25，24 个代表点：T8 27.904→27.904µs，T9 27.904→27.936µs，T15 36.096→29.952µs，T16 29.920→29.984µs，T48 52.320→52.480µs，T128 83.232→83.072µs。

**结论与取舍：** 完成清理，保留对低列数延迟有实际价值的展开，不以最少模板数为目标。较宽 runtime tile 的部分点约有 2µs 代价（T25 为 38.144→40.192µs）；不主张全部点提速。未获益的共享 staging 实验已撤回；最终只改变本 Op 的 small-T 分派和配置，任意正 T、三个直接输出、单 kernel/零 scratch 保持。

### OP-12 — linear_swiglu — draft MLP

合同：[linear_swiglu.h](../../include/ninfer/ops/linear_swiglu.h)。
复核落点：[w8_dflash2_linear_swiglu.cu](../../src/ops/linear_swiglu/w8/w8_dflash2_linear_swiglu.cu)、[w8_linear_swiglu_plan.cpp](../../src/ops/linear_swiglu/w8/w8_linear_swiglu_plan.cpp)。
验证入口：[ops/linear_swiglu/test_w8_a16.cpp](../../tests/ops/linear_swiglu/test_w8_a16.cpp)。

**已有行为：** 完整 SiLU(gate)*up，只有最终 BF16 输出是存储边界；任意正 T、零 scratch。

**实际实现与复杂度：** 40 个 exact-T small kernel 改为容量 8/16/24/32/40 的 5 个实例；容量 24 用 8 K-warps，其余用 4，删除 T22..24 的单独切换与 padded-staging 回跳。Shared pair epilogue 接收全局 column 和 live extent，SwiGLU 屏蔽真实尾列；context consumer 同步采用全局 column，移除重复 column_begin 偏移。上端删除 C88，81..96 共用 C96；保留 T41..63 的 R32/C64 和完整 T64 的 R64/C64，前一区间强行合并到 R64 的实测代价约 5–8%。

**验收：** W8 LinearSwiGLU 的完整 FP64 SiLU(gate)*up 测试通过，包含 T1..128、负输入 Graph、prefill、guards 和 companion 回归，原数值判据不变。受共享 epilogue 影响的 context_kv_materialize 全套数值/索引/cache/Graph 测试通过；其 global column 不能再加一次 tile 起点。RTX5090/sm_120a/CUDA13.1，cold Graph、warmup4/repeat25，29 个代表点：T16 140.576→133.920µs，T24 142.272→142.624µs，T48 203.872→202.016µs，T64 202.240→202.016µs，T96 255.104→253.216µs。

**结论与取舍：** 完成清理，small kernel 40→5、主 route 区间 7→6。保留完整 tile 的资源差别；去掉 C88 后 T88 255.264→265.280µs，约 3.9% 的局部代价已记录。单 kernel、零 scratch 和完整非线性边界保持；不声称全域提速，也不继续枚举上端相邻列 tile。

### OP-13 — sliding_window_attention

合同：[sliding_window_attention.h](../../include/ninfer/ops/sliding_window_attention.h)。
复核落点：[launch.cu](../../src/ops/softmax_attention/sliding_window/launch.cu)。
验证入口：[ops/test_sliding_window_attention.cpp](../../tests/ops/test_sliding_window_attention.cpp)。

**已有行为：** 对称 abs(key−query)<2048，当前有效 query 互相可见；FP32 partial 修正额外舍入，zero-live 输出精确零。4096 是保留的既有 profile。

**实际实现与复杂度：** 16 个 exact-W 贯穿 direct/partial/merge，并按两种 window 实例化。主机制只有 direct/split，但 split-limit 另按三段 W、多段 B、context_tiles/2 或 /4 选择；direct 也为 W>8/B≥6 设置 128 的例外阈值。

**已有证据及其边界：** 既有 352 次独立 FP64 对照及 ragged/zero/Graph/cache/4096 回归通过。最终 BF16 舍入预算修正后 rL2≤0.0023、gross≤3e−4+0.0045*max|ref|；旧 BF16 partial 仍失败。W16/B8/context2048 cold 87.296→60.384µs。

**结论 / 后续方案：** 保留 FP32 partial；比较按 query-row 容量分桶的 kernel 与基于总 CTA/每 split 工作量的少量策略，替代 W×B×context 的细表。审查 direct 例外；host 容量与 device active splits 必须一致，并覆盖非单调 interval query、ragged 与 window 回归。

### OP-14 — kv_cache_append_prefix — cyclic

合同：[kv_cache_append.h](../../include/ninfer/ops/kv_cache_append.h)。
复核落点：[launch.cu](../../src/ops/kv_cache/append/launch.cu)。
验证入口：[ops/test_kv_cache_append.cpp](../../tests/ops/test_kv_cache_append.cpp)。

**已有行为：** device counts 与物理 Wc 分离，零前缀不读输入，精确保留未寻址 cache；lane/wrap 不依赖 K。

**实际实现与复杂度：** 每容量只有 64/128-thread 两种配置，按 max_count*B 是否超过 128 选择；两种既有 capacity 共 4 个 cyclic 实例。DFlash2 decode 域只走 64-thread 配置。

**已有证据及其边界：** 既有 exact 地址/codec oracle、全部 Wc/Bc、有限 BF16 位模式、Graph metadata 更新及 4096/长前缀回归通过。Wc16/B8/full warm 2.231→1.585µs。

**结论 / 后续方案：** 保留完成；两个布局区分短前缀并行度与大前缀调度成本，无需进一步按 counts 拟合。

### OP-15 — linear_pair — context 依赖

合同：[linear_pair.h](../../include/ninfer/ops/linear_pair.h)。
复核落点：[materialize.cu](../../src/ops/context_kv_materialize/materialize.cu)。
验证入口：[ops/linear_pair/test_w8_a16.cpp](../../tests/ops/linear_pair/test_w8_a16.cpp)。

**已有行为：** DFlash2 不再调用 LinearPair；现有 MTP / 35B DFlash 消费者继续使用原 Op。

**实际实现与复杂度：** 本项没有新增或修改 LinearPair route；不能把不再使用的 Op 算成一次 DFlash2 性能优化。

**已有证据及其边界：** 既有两种真实消费者 geometry 的 FP64 dot/guards/只读回归通过，验证共享 W8 primitive 改动没有破坏现有路径。

**结论 / 后续方案：** 按依赖退出关闭。DFlash2 context qualification 与后续复杂度收敛归 OP-07；不另做假设的 LinearPair DFlash2 benchmark。

### OP-16 — prepare_ragged_prefix

合同：[prepare_ragged_prefix.h](../../include/ninfer/ops/prepare_ragged_prefix.h)。
复核落点：[prepare_ragged_prefix.cu](../../src/ops/launcher/prepare_ragged_prefix.cu)、[prepare_ragged_prefix.cuh](../../src/ops/kernel/prepare_ragged_prefix.cuh)。
验证入口：[ops/test_prepare_ragged_prefix.cpp](../../tests/ops/test_prepare_ragged_prefix.cpp)。

**已有行为：** 复制真实前缀、清零输出尾并生成 positions/counts，N=16 保留最后一行；允许合同内的 strided source 与只读别名。

**实际实现与复杂度：** 一个非模板 kernel，统一 128 threads / 2KiB feature tile 和三维 grid，metadata 各有唯一写入者；零 scratch，无 shape route。

**已有证据及其边界：** 既有全部 Wc/Bc 的 exact oracle、zero/one/full、padding/lanes、Graph 输入更新及通用 D 回归通过。Wc1/B1 cold 9.440→3.360µs，Wc16/B8/full warm 4.138→3.433µs；zero 未主张收益。

**结论 / 后续方案：** 保留完成；不为全零或单列另加专门性能路线。

### OP-17 — embedding

合同：[embedding.h](../../include/ninfer/ops/embedding.h)。
复核落点：[embed_gather.cu](../../src/ops/launcher/embed_gather.cu)。
验证入口：[ops/test_embedding.cpp](../../tests/ops/test_embedding.cpp)。

**已有行为：** 保留任意正 T、合法对齐与真实 token 域；分别按 signed code / FP8 code 和 stored scale 解码。

**实际实现与复杂度：** 两格式各两种 CTA 布局：10 或 5 CTA/token；W8 在 T128、FP8 在 T176 切换。W8 的 pair/scalar store 与 byte-reader 是合法对齐支持，不能当成逐点性能例外删除。DFlash2 T≤128 每格式只有一个性能配置。

**已有证据及其边界：** 既有 T1..128 全输出、Graph 切换普通/mask IDs、prefill、codec 舍入和对齐回归通过。Pointwise 界 abs=2^-134、rel=2^-8 来自 BF16 RNE，而非拟合 production。W8/T128 normal warm 3.682→1.831µs；FP8 2.129→1.764µs。

**结论 / 后续方案：** 保留完成；维持格式/对齐分工，不追加针对重复 ID 数量的路线。

### OP-18 — rmsnorm

合同：[rmsnorm.h](../../include/ninfer/ops/rmsnorm.h)。
复核落点：[rmsnorm.cu](../../src/ops/launcher/rmsnorm.cu)。
验证入口：[ops/test_rmsnorm.cpp](../../tests/ops/test_rmsnorm.cpp)。

**已有行为：** 每行沿 D 归约，Q/K 的行数分别 24*T、4*T；保持自然对齐回退和任意正列数。

**实际实现与复杂度：** D256 offset 统一使用 4 行/128-thread CTA，去掉 512/1024/2560 的行数分派及 8 行专用实例，不再回到通用 16 行实现。D5120 的固定宽度优化和自然对齐回退保留。Gated epilogue 的 prefetch 策略属于另一 Op，不计入本项 offset 路线。

**验收：** ninfer_rmsnorm_test、ninfer_gated_rmsnorm_test、ninfer_rmsnorm_rope_test 全部通过，复用已有全公式 FP64、Graph、in-place/对齐/guards 与 prefill 用例，数值判据不变。RTX5090/sm_120a/CUDA13.1，warm Graph32、warmup4/repeat30，Q/K 两种实际 geometry 的 22 个代表/边界点完成对照。Q/T32 为 1.508→1.615µs，T64 为 1.625→1.721µs，T128 为 1.970→2.014µs；K/T128 为 1.479→1.452µs，Q/T1024 为 5.711→5.696µs。

**结论：** 完成清理；保留统一行布局，记录部分点约 0.1µs 的差异，不主张新加速，也不为这些差异恢复多段阈值。每次一 kernel、零 scratch；Q/K head 仍作为独立归约行。

### OP-19 — scatter_bf16_batch — feature capture

合同：[scatter.h](../../include/ninfer/ops/scatter.h)。
复核落点：[scatter.cu](../../src/ops/launcher/scatter.cu)、[scatter.cuh](../../src/ops/kernel/scatter.cuh)。
验证入口：[ops/test_scatter_bf16_batch.cpp](../../tests/ops/test_scatter_bf16_batch.cpp)。

**已有行为：** 保留父张量 column/lane stride，五个 slice 独立写入；未选 lane、无效列和其他 slices 位模式不变。

**实际实现与复杂度：** 一个 batch kernel，固定 128 threads / 2KiB tile，无按 W/B/counts 的性能路线，零 scratch。

**已有证据及其边界：** 既有 163 个 exact fixtures，逐次检查全池、Graph 五次 capture 与 metadata 更新、通用 D 回归通过。五次 capture 的 W16/B8/full warm 9.197→8.578µs；zero 4.738→5.356µs，是明确保留的取舍。

**结论 / 后续方案：** 保留完成；空批次约 0.62µs/五次 capture 的代价不作为制造新分支的理由。

### OP-20 — attn_input_proj — target

合同：[attn_input_proj.h](../../include/ninfer/ops/attn_input_proj.h)。
复核落点：[fp8_attn_input_a16_small_t.cu](../../src/ops/attn_input_proj/fp8/fp8_attn_input_a16_small_t.cu)、[q4_q5_attn_input_plan.cpp](../../src/ops/attn_input_proj/q4_q5/q4_q5_attn_input_plan.cpp)。
验证入口：[ops/test_attn_input_proj.cpp](../../tests/ops/test_attn_input_proj.cpp)。

**已有行为：** 四个独立 BF16 输出 Q/K/gate/V，任意正 T；FP8 A16 已消除每 11 列串行投影。Activation policy 是许可集合。

**实际实现与复杂度：** FP8 A16 T6..33 有 28 个 exact-T MMA；大 T 另有 4 种 schedule，含 129..160 回跳；A8 有 6 种 schedule。Q4/Q5 在 mixed 与 pair 间反复切换，65..104、129..192 分别保留 pair 配置。

**已有证据及其边界：** 既有 T1..128 四输出 FP64 oracle、Graph/容量/其他格式及 prefill 回归通过。T128 cold：FP8 A16 650.560→163.072µs，Q4/Q5 212.256→150.816µs，A8 79.072→66.816µs。收益主要证明新计算路径有效，未隔离每个 narrow route 的贡献。

**结论 / 后续方案：** 把 A16 live T 从 tile 模板中解耦；分别比较少量 A16/A8 schedules。Q4/Q5 以 mixed/pair 的明确机制差异决定分界，重新判断 104、129..192 等局部区间；同步四输出、policy、workspace 和 Graph qualification。

### OP-21 — causal_softmax_attention

合同：[softmax_attention.h](../../include/ninfer/ops/softmax_attention.h)。
复核落点：[causal_softmax_attention.cpp](../../src/ops/softmax_attention/dense/causal_cache/causal_softmax_attention.cpp)、[small_t.cu](../../src/ops/softmax_attention/dense/causal_cache/small_t.cu)。
验证入口：[ops/softmax_attention/causal_cache.cpp](../../tests/ops/softmax_attention/causal_cache.cpp)。

**已有行为：** masked physical W 可大于 max_visible_keys；各 lane 用自身 live prefix，尾列精确零且不写 cache。INT8 PV 的 FP16 算术与 FP32 partial 修正数值问题。

**实际实现与复杂度：** Q24 chunk 最大从 6 扩到 8，新增 7/8 token 特化；还叠加五格式共 18 个 Prompt 宽度区间、NVFP4 B>1/W12/短 context 的 6+6 特例、BF16/INT8 均分 chunk 例外及 batch split 规则。旧 small_t 的 INT8 T5、T≥6/context 窄区间也仍存在，不能只审查新增函数。

**已有证据及其边界：** 既有唯一 FP64 QK→softmax→PV oracle 删除了 private rounding 模仿；INT8 原 criterion 未放宽。全 softmax 576 次对照、Graph/cache/codec/query 及 merge racecheck 通过。W16/B8/L8192 BF16 cold 818.432→410.816µs；NVFP4 W12 特例主要收回约 2.048µs，证据强度与核心收益不同。

**结论 / 后续方案：** 优先收敛单形状 chunk 例外和密集 Prompt 阈值；以有限 chunk 策略、总 CTA/每 split 工作量重新组织选择，覆盖旧 INT8 特例。保留数值修正及五 codec 边界；重验 masked/Graph 和容量，不把减少节点数或 scratch 当作必须同时单调下降的目标。

### OP-22 — gdn_norm_gating_proj

合同：[gdn_gating_proj.h](../../include/ninfer/ops/gdn_gating_proj.h)。
复核落点：[bf16_gdn_norm_gating_proj_27.cu](../../src/ops/gdn_gating_proj/bf16/bf16_gdn_norm_gating_proj_27.cu)、[bf16_gdn_gating_proj_plan.cpp](../../src/ops/gdn_gating_proj/bf16/bf16_gdn_gating_proj_plan.cpp)。
验证入口：[ops/test_gdn_gating_proj.cpp](../../tests/ops/test_gdn_gating_proj.cpp)。

**已有行为：** 任意正 T，独立完整 norm→control 公式；h 是公开输出，g/beta 不强制以已舍入 h 为输入。

**实际实现与复杂度：** T≤42 的一个融合主体有 4 个 Tile/Threads 实例；43..128 走同一既有 RMSNorm + cooperative split-8 control 路线，包含 full/predicated 与不满足驻留时的 unsplit 处理。大 prefill 的 split-4/2/unsplit 按总 grid 驻留降低 split。并非整个 Op 只有 4 个 kernel，但没有逐 T 实例表。

**已有证据及其边界：** 既有 187 个 fixtures / 684 次 FP64 对照、Graph/容量/prefill 及 racecheck 通过。g/beta rL2=0.0008 不变，gross=1.5e−4+0.002*max|ref|，调整依据为独立归因的 BF16 staging 传播。T32 warm 9.684→8.180µs；T≥43 未主张新增 GPU 算法收益。

**结论 / 后续方案：** 保留完成：当前融合/组合分工和有限 tile 规模可接受，未证明它是最少实例数。实验阶段对大量 MMA/cooperative 候选及细交叉点投入过多，相关新实验代码已移除；后续不继续为了缩小每个分界的差异增加配置。

## 4. OP-23..37 待治理项

下列为首轮已存在的 Op，本轮尚未完成验收。旧阈值只用于定位原实现，不能成为新合同
或必须保留的路线。OP-23 已开始阅读代码，尚无实现修改或新 qualification 结果。

### 4.1 Target verify 与 feature capture

Target 有 16 个 full-attention、48 个 GDN、64 个 MLP；T=W*B≤128。
Groupwise/nvfp4 identity 的实际绑定格式分别验收，包含末八层 MLP 的 FP8。
GDN recurrent state 为 FP32；conv history / conv record 为 BF16，不能混淆两种状态边界。

| ID | Op 与合同 | 精确 profile / 新输入 | 复核与方案 |
|---|---|---|---|
| OP-23 | [gdn_input_proj_conv_record](../../include/ninfer/ops/gdn_input_proj.h) | Groupwise Q4/Q5 双 parent、row-FP8 `[16384,5120]`；W<=16/B<=8 | 已有 ReplaySSM 域。复核 aggregate 15/16 的 grouped seam、FP8 B1 W=9/10 的 fused/A8 seam及新增大 batch 列数；以同一物理块的 snapshot 输出及 history 为参照检查 bit 一致性；验证 valid columns、raw conv records 和 source 不提前修改。 |
| OP-24 | [gated_delta_net_replay_record](../../include/ninfer/ops/gated_delta_net.h) | BF16 Q/K `[128,16,W,B]`、V/out `[128,48,W,B]`，FP32 g/beta/state | 已支持 W=2..16/B=1..8；以对应 snapshot Op 检查 out 的 bit 一致性，exact 检查 raw K/V/gate records、无效后缀和 source 不变。比较长 W 的串行 recurrence、launch 分工和 record 写入成本。 |
| OP-25 | [gated_rmsnorm](../../include/ninfer/ops/gated_rmsnorm.h) | D128、48 heads、W*B<=128 的 recurrent output/z | 完整 norm/gate 公式及 head/token flattening；复核新增 extent 的线程布局与内存吞吐，不能用单纯 RMSNorm 的证据替代。 |
| OP-26 | [linear_swiglu](../../include/ninfer/ops/linear_swiglu.h) target MLP | Q4、NVFP4、row-FP8 `[34816,5120]`，T<=128 | 分别覆盖实际绑定格式和 activation policy。重点复核 Q4 materialized、大 T NVFP4 M64/M96/97+ 切换、末八层 FP8；比较完整 fused/composed 成本并直接验证完整公式。 |
| OP-27 | [linear_add](../../include/ninfer/ops/linear_add.h) target output | Q5/row-FP8 `[5120,6144]`；Q5/NVFP4/row-FP8 `[5120,17408]`，T<=128 | Attention/GDN 与 MLP 两个 C profile 分别复核；包括 FP8 A16/A8、Q5 C32 和 NVFP4 W4A4。验证 residual in-place 与实际量化边界，不能只测不带 add 的 Linear。 |
| OP-28 | [linear](../../include/ninfer/ops/linear.h) target full head | W8/row-FP8 `[248320,5120]`，BF16 logits `[248320,W,B]` | 保持 dense target output 合同。重点复核 W8 64/65 之后 route、FP8 A16 GEMM 和新尾 tile；分别测到 T=128，不能从 proposal top-k 推导本 Op 性能。 |
| OP-29 | [argmax](../../include/ninfer/ops/argmax.h) | target logits `[248320,W,B]`，valid_rows=248077 | 验证所有列、padding 排除和最小 global-id tie；复核 T<=128。只在 acceptance envelope 允许时作为 raw-greedy target 输入。 |
| OP-30 | [rope](../../include/ninfer/ops/rope.h) target form | Q/K D256、rotary_dim=64，W<=16/B<=8 | 补真实 target position tensor 布局、逐 lane offset 和 partial MRoPE 数值；DFlash2 的一维 D128 RoPE 不替代此 Op。 |
| OP-31 | [sigmoid_mul](../../include/ninfer/ops/sigmoid_mul.h) | target attention/gate `[6144,W,B]` | 独立 sigmoid-gate 公式、原地输出与新尾 tile；按 16 个 full-attention layers 的调用范围复核性能。 |
| OP-32 | [offset_i32_positions](../../include/ninfer/ops/position.h) | I32 source/destination vectors 与 scalar delta；target Text/Vision 每 lane 的 W 列位置 | Exact 验证各 lane 调用使用匹配的 vector view/scalar offset，不能将不同 delta 的 batch 当作同一 vector 调用；DFlash2 context/query 的 F+i 绝对 cache position 保持独立。 |

### 4.2 准备与提交

这些项目按 exact/状态行为关闭；没有独立性能疑问时不启动 benchmark campaign。

| ID | Op 与合同 | 新输入与验收条件 |
|---|---|---|
| OP-33 | [prepare_masked_block](../../include/ninfer/ops/prepare_masked_block.h) | `[W,B]`，W=2..16，mask id=248070；exact 验证 anchor、K 个 masks 和 F..F+K positions，区分 proposal 与 target valid columns。 |
| OP-34 | [speculative_prepare_verify_ids](../../include/ninfer/ops/speculative_round.h) | drafts `[K,B] -> ids [K+1,B]`，P_b=0..K；exact 验证 anchor/live draft prefix 和安全尾部。若选择同时生成 positions 的同族 form，按同一 alignment 验证，不建立第二份逻辑。 |
| OP-35 | [speculative_select_accepted_hidden](../../include/ninfer/ops/speculative_round.h) | hidden `[5120,W,B]`，N>0 时 selector=N-1；exact gather 覆盖最大列和非满提交。N=0 的 schedule 不以 -1 调用本 Op。 |
| OP-36 | [scatter](../../include/ninfer/ops/scatter.h) continuation form | selected hidden `[5120,B]` 写入 owning state slot；exact copy 和 selector 重排。与 OP-35 联合覆盖短提交，保留其他 slots。 |
| OP-37 | [gdn_replay_fold](../../include/ninfer/ops/gdn_replay.h) prepared Op | 27B 的48个GDN层，records W<=16，commit_columns=N_b；将 FP32 state 和 BF16 conv tail_3 与同一完整块的第 N 个 snapshot 逐位比较，覆盖 N=0 无读写、N=W 全提交、部分提交与 source/destination slots。这是扩大可提交前缀引入的直接回归点。 |

## 5. 重新关闭项目的要求

### 5.1 先确定方案，再做有限比较

1. 写明当前问题、真实输入域、少量候选机制及选择依据。浮点精度、codec、request 边界、
   counts/物理宽度和 workspace 是约束，不能以简化分派为由破坏。
2. 将运行时 live extent 与编译 tile 容量分开评估。固定硬件 geometry、格式和合法对齐的
   必要分工可以保留；逐 T/K/W 特化、非连续回跳、单形状例外必须说明相对更少配置的增量收益。
3. 使用完整公开 Op、相同 fixture 和 cache 条件比较。先测短/推荐/最大 K、B1/B8 及能区分
   方案的边界；纯矩阵按实际 T/U 去重，attention/state 保留 W/B/context/counts。
   已能区分方案时停止扩充候选，不为每个相邻点重复扫配置。
4. 短调用用重复 Graph 或图内事件排除 host 提交间隙；冷缓存离散档、单个异常 median 和
   未超过波动的差异不足以新增路线。保留少量路线不需要证明它是理论最小数量。
5. 额外配置若保留，记录其适用区间、完整 Op 的稳定收益、scratch/launch 取舍及原因。
   不能只以“这个点最快”或“补平台阶”作为理由；也不预设删除配置可以无损提速。
   不设机械的统一模板上限或统一百分比门槛，不为达成数字牺牲相关性能。

### 5.2 Qualification 与证据复用

- 先从 Op 的可观察承诺选择验收参照。数学计算使用完整公式及实际 stored scale 的独立
  FP32/FP64 oracle；精确转换检查地址/位模式；概率过程检查条件分布、RNG 和可许可输出。
  ReplaySSM OP-23/24/37 以对应 snapshot 执行为参照，要求输出和提交状态 bit 一致；
  N 列提交对应同一完整块的第 N 个 snapshot，不能另跑宽度 N 的投影。无需为这种等价性
  再做 FP64 对照；若修改 snapshot 的数学实现，则单独验证其数学合同。
  数值判据变化须有格式/误差依据，不能为某个 kernel 放水。
- 有宽度语义的 Op 覆盖每个支持 W，并检查 request 边界、奇数/尾 tile、ragged/zero/full；
  纯矩阵重点覆盖每条生产 route 和改变的边界。相同 T 不同 W/B 在 state/conv/attention 中
  不是相同输入。未改变且证据仍适用的行为无需重跑全部旧实验。
- 改变 workspace 或调用分解时，复验区间 query、实际峰值、scope 归还和同址 Graph
  capture/replay。Capacity 必须覆盖所有可达 envelope，不能只查最大 W/B 的满块路线。
- 只在已识别 kernel 的具体资源问题会改变方案时用 NCU；此阶段不要求未接入的 Engine
  或 nsys。排他 GPU 计时；编译使用 `cmake --build <dir> -j`。
- 每项记录合同/格式、实现机制与实例规模、代表性性能取舍、实际通过的数值/状态检查、
  仍存在的限制和下一步。记录选定方案的证据，不保留逐点全曲线、失败候选流水或计时噪声日记。
  Benchmark 的可用命令以 [bench reference](../../bench/README.md) 为准。

### 5.3 本阶段退出条件

37 项均为“保留完成”或“依赖退出”，没有未解决的域、索引、数值/状态、workspace 或
实现复杂度问题；修改后的 routes 有相关 qualification 和足够的 Op 性能依据。
此时可以进入正式 Engine 集成，不能据此宣告 DFlash2 端到端产品或所有 K 的 acceptance 完成。
后续共享改动若使某项证据失效，更新该项状态和具体缺口。

本清单是临时工作文档。Op 治理及后续 Engine 接入完成后，稳定合同回归现有 active
references，删除本清单；不保留另一份历史完成表或并行版本。
