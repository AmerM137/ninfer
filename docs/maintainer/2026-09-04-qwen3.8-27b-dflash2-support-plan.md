# Qwen3.8-27B DFlash2 支持临时执行计划

> 状态：artifact/converter/binder 和首轮 Op 实现已存在；已修正运行范围为 `K=1..15`。
> 当前先完成 Op 合同修正与重新 qualification，再接入正式 Engine。
>
> 目标版本：现行 `.ninfer` v2。
>
> 生命周期：本文是跨阶段工作的临时路线记录，不是并行的 artifact、模型或 Engine 权威，也不
> 加入公共文档索引。实现稳定后，应将最终合同分别回写到现有 active maintainer references、
> CLI/serving 文档和 model card，并删除本文。

## 1. 目标与范围

目标是在现有 Qwen3.8-27B Engine 路径中加入完整 DFlash2 speculative backend，并满足：

1. `qwen3.8-27b/groupwise-int` 和 `qwen3.8-27b/nvfp4` 两个重新转换的 artifact 都内嵌
   DFlash2 权重；
2. MTP 与 DFlash2 权重共存于同一个 artifact，但 Engine 启动时只选择并 materialize 一个
   speculative backend；
3. 已发布、不含 DFlash2 的两个旧 artifact 继续支持现有 Text、Vision、MTP、prefix reuse、
   CLI、serving 和 measurement 行为；
4. 只有显式选择 DFlash2 且 artifact 缺少完整 DFlash2 bundle 时才报能力缺失错误；
5. DFlash2 通过同一个公共 `.ninfer` Engine 路径执行，并覆盖一至八个启动固定 active
   requests、compact decode batch、prefix/continuation、CUDA Graph 和最终性能验证；
6. DFlash2 最终支持 Qwen3.8-27B 的 image/video Vision prompt，并复用已在 35B DFlash 中
   修正的多模态 attachment 和位置语义。

本工作明确不依赖、不等待也不修改未生效的 v3 迁移草案。当前任务只扩展 v2。

算法语义见
[`2026-09-04-qwen3.8-27b-dflash2-algorithm.md`](2026-09-04-qwen3.8-27b-dflash2-algorithm.md)，
当前逐 Op 工作由
[`2026-09-05-qwen3.8-27b-dflash2-op-checklist.md`](2026-09-05-qwen3.8-27b-dflash2-op-checklist.md)
管理。本计划不把 SGLang 或 vLLM 变成 NInfer 的执行或正确性路径。两者只用于
核对 checkpoint mapping、算法边界和状态顺序；最终语义、实现、oracle 和性能证据由 NInfer
自己拥有。

## 2. 已冻结的产品决策

### 2.1 Backend 身份

公共 C++ 枚举、CLI、日志和统计使用独立的 DFlash2 身份：

```text
SpeculativeBackend::DFlash2
--spec dflash2
```

现有 35B-A3B `SpeculativeBackend::DFlash` 保持原义。DFlash 与 DFlash2 可以共享 family-owned
生命周期机制，但不得在产品诊断和统计中互相冒充。

一个 Engine 实例在启动时冻结为 `none`、`mtp`、`dflash` 或 `dflash2` 之一，不在请求间或请求
内切换 backend，也不同时构造两个 speculative module 的权重 view、状态、workspace 或 Graph。

### 2.2 Draft 几何

Checkpoint 推荐七个 draft token。NInfer 沿用已有 DFlash 的参数含义，在 Engine 启动时
选择 `draft_tokens=K`，支持全部整数 `1..15`：

```text
recommended draft_tokens = 7
supported draft_tokens K = 1..15
query block = 1 anchor + K mask positions
physical verify width W = K+1 = 2..16
backbone/verify columns T = W*B <= 128
mask-head/selector columns U = K*B <= 120
```

SGLang 的 `speculative_num_draft_tokens` 是包含 anchor 的 W；vLLM 的
`num_speculative_tokens` 和 NInfer 的 `draft_tokens` 是 K。本地 vLLM 直接以运行 K+1
设置卷积宽度，SGLang 的 `set_block_size()` 同步更新各层卷积；两者都允许覆盖推荐值。

改变 W 会改变非因果 attention 的可见 query 集和 proposal q，也必须改变卷积的 request
stride；短 block 的结果不必等于长 block 的前缀。各宽度都是合法 proposal，使用实际 q
的 rejection/correction 保持 target 分布。Acceptance 和吞吐用于选择高效配置，不决定
该宽度在算法上是否成立。Op 不得只注册 W=8，也不得将所有配置统一填充成有效 W=16。

每请求的 verify extent `P_b=0..K`、live columns `Q_b=P_b+1`、接受数 `A_b` 和最终提交数
`N_b=0..A_b+1` 分开记录。固定 K 的调度可以保留完整 K 位 proposal、仅验证前 P_b 位；
proposal attention 使用 valid_columns=W，target attention/GDN 使用 Q_b。这里不引入运行中
自动改变 K 或请求间不同 K 的调度策略。

Converter 仍核对注册 checkpoint 的 source `block_size=8`，权重没有 K 轴；覆盖运行 K
不要求转换新 artifact。入口已有的 `draft_tokens==7` validation 应随合同修正，正式 Engine
接线完成前保留执行能力未实现的拦截。

### 2.3 Proposal head

DFlash2 支持现有两种启动固定 proposal-head route：

- `full`：复用主模型完整 `text/output_head`；
- `optimized`：复用现有 `text/draft_head` 和 `text/draft_head_token_ids`。

两条 route 都需要输出每个 proposal position 的 top-16，而不是现有 DFlash 的单个 argmax。
optimized route 必须先把 shortlist row 映射为完整 token id，再访问完整词表上的 selector
codebook。Shortlist 可以改变 draft/acceptance，但 target verification 仍保证最终输出语义。

### 2.4 Vision

DFlash2 的完成定义包含 Vision prompt。Text eager 闭环可以作为中间里程碑，但不能作为最终
产品完成条件。Vision 路径在 Text decoder 已融合视觉 embedding 后捕获相同的目标层 hidden
features，并沿用 35B DFlash 已支持的 Vision/DFlash 共存合同；若后续事实推翻这一 attachment
语义，必须显式修改本计划和产品合同。

## 3. 已确认的上游权重事实

### 3.1 来源与本地位置

```text
repository = z-lab/Qwen3.8-27B-DFlash2
revision   = 50307d4c4cde6860d4eee73e2547cd786fe8e8a4
local      = /home/neroued/models/llm/qwen/Qwen3.8-27B/dflash2
```

仓库快照直接放在 `dflash2/`，保留上游相对路径，不再嵌套仓库名。下载内容包括
`.gitattributes`、`README.md`、`assets/dflash2-figure.png`、`config.json` 和
`model.safetensors`。

```text
repository bytes        = 3,849,113,002
model.safetensors bytes = 3,848,817,896
tensor payload bytes    = 3,848,808,960
source tensors          = 81 BF16
```

### 3.2 固定模型几何

| Fact | Value |
|---|---:|
| draft layers | 5 |
| hidden / intermediate | 5120 / 17408 |
| query heads / KV heads / head dim | 32 / 8 / 128 |
| target feature layers | `[5,19,33,47,61]` |
| feature projection input | `5 x 5120 = 25600` |
| attention class | five non-causal sliding-attention layers |
| sliding window | 2048 |
| checkpoint 推荐 block size / draft positions | 8 / 7，运行范围见 Section 2.2 |
| mask token id | 248070 |
| dynamic convolution taps / group size | 2 / 16 |
| selector rank / candidate count | 256 / 16 |
| vocabulary rows | 248320 |

### 3.3 Source tensor groups

- context encoder：`fc.weight [5120,25600]`、`hidden_norm.weight [5120]`；
- five decoder layers：attention、dense MLP、norms，以及 attention/MLP 两组 dynamic
  convolution；
- final norm：`norm.weight [5120]`；
- selector：`hidden_projection [256,5120]` 和两个 `[248320,256]` codebook。

Checkpoint 没有私有 token embedding 或 LM head。anchor 和 mask embedding 使用主模型
`text/token_embedding` 的行视图，候选 logits 使用主模型 full 或 optimized proposal head。

## 4. v2 Artifact 兼容合同

### 4.1 Core 与可选 companion bundle

现行 v2 合同放宽为：

```text
(model_id, weights_id)
├── fixed core inventory and storage semantics
└── zero or more target-declared, all-or-none optional companion bundles
```

本任务只引入一个这样的 bundle：Qwen3.8-27B DFlash2。它不改变 core Text、MTP、Vision、
frontend、已有 proposal head 或任何现有对象的语义。

以下变化仍要求新的 `weights_id`：

- 修改、删除或重新解释 core object；
- 改变 core fusion、numeric format 或 storage layout 合同；
- 使同名 core object 的 payload 不再按原合同解码。

仅追加一个不影响 core 的、target reference 明确注册的可选 companion bundle，不要求新
`weights_id`，也不要求新 framing revision。

### 4.2 有效 inventory

对于现有两个 Qwen3.8 identity，v2 binder 只接受：

1. 精确 legacy core inventory；或
2. 同一 legacy core inventory 加完整 DFlash2 bundle。

规则为：

- 没有任何 `dflash2/` object：合法旧 artifact；
- bundle sentinel 存在：逐个绑定并验证完整 DFlash2 descriptor；
- sentinel 不存在但出现其他 `dflash2/` object：`Binder::finish()` 以未消费对象拒绝；
- sentinel 存在但任一对象缺失或 descriptor 错误：按不完整 bundle 拒绝；
- 未注册的其他额外对象仍被拒绝。

Generic v2 Reader、prefix、JSON root/object schema、payload offset 和 layout registries 均不改变。
Binder 只需增加无副作用的对象存在查询；bundle 的完整性和意义继续由 Qwen3.8 target binder
拥有。

### 4.3 选择与 materialization

| Artifact | `none` | `mtp` | `dflash2` |
|---|---|---|---|
| legacy core-only | 正常 | 正常 | 启动时明确报告 DFlash2 bundle 缺失 |
| core + DFlash2 | 正常，DFlash2 validate-only | 正常，仅 MTP resident | 正常，仅 DFlash2 resident |

补充不变量：

- 新 artifact 中未选 DFlash2 时仍验证完整 descriptor，但不进行 DFlash2 H2D；
- 选择 DFlash2 时 MTP tensors 只验证、不上传，也不分配 MTP KV/state/Graph；
- optimized proposal head 只在选中 `ProposalHead::Optimized` 时上传；
- qwen3.6-27b profiles 选择 DFlash2 时报告 target capability 不支持，而不是伪装成缺少
  Qwen3.8 bundle；
- 35B-A3B 的 artifact 合同不变；其 DFlash runtime 已允许与 Vision 同时启用。

### 4.4 Converter 输出策略

新 converter 只有一种当前输出：完整 core 加完整 DFlash2 bundle。可选性只服务运行时读取已
发布旧 artifact，不增加 `--without-dflash2`、legacy converter 或第二条产品生成路径。

```text
qwen3.8-27b/groupwise-int  -> identity unchanged, recipe sidecar may become v2
qwen3.8-27b/nvfp4          -> identity unchanged, recipe sidecar may become v2
```

`recipe_id`、source revision 和转换报告不是运行时身份。当前两个旧 artifact 及 sidecar 在重新
转换前移动到明确、稳定的 compatibility 路径；不得用 glob、mtime 或“latest”名称选择它们。

## 5. DFlash2 Artifact Inventory 与量化路线

### 5.1 已注册对象边界

对象名不携带 format spelling。当前 namespace 为：

```text
dflash2/feature_projection
dflash2/context_norm
dflash2/layers/{l}/input_norm
dflash2/layers/{l}/attention_conv/base_kernel
dflash2/layers/{l}/attention_conv/kernel_projection
dflash2/layers/{l}/attention/query_key_value
dflash2/layers/{l}/attention/query_norm
dflash2/layers/{l}/attention/key_norm
dflash2/layers/{l}/attention/output
dflash2/layers/{l}/post_attention_norm
dflash2/layers/{l}/mlp_conv/base_kernel
dflash2/layers/{l}/mlp_conv/kernel_projection
dflash2/layers/{l}/mlp/gate_up
dflash2/layers/{l}/mlp/down
dflash2/final_norm
dflash2/candidate_selector/hidden_projection
dflash2/candidate_selector/predecessor_codebook
dflash2/candidate_selector/successor_codebook
```

每层将 source Q/K/V 合并为 `[6144,5120]` parent，将 gate/up 合并为 `[34816,5120]`
parent。按上述边界，bundle 是六个 global objects 加 `5 x 12` layer objects，共 66 个
tensor objects；完整 inventory 与 format 以 `qwen3.8-27b-artifact.md` 为准。

### 5.2 当前 Op 治理使用的存储配置

两种主模型 artifact 的 DFlash2 bundle 均按已落地 recipe 使用：

- feature、attention 和 MLP projection matrices：`W8G32_F16S`；
- dynamic-convolution coefficient projection、base kernels 和 norms：`BF16`；
- selector codebooks 和 hidden projection：`BF16`；
- embedding、full head 和 optimized head：继续 alias core objects，不复制 payload。

两种 artifact 使用相同的 DFlash2 format allocation；不同 Program 不共享 mutable state
或 device allocation。修正 K/W 和重新 qualification 直接消费现有权重，不开启新的量化格式
或 source calibration 工作。

### 5.3 后续量化决策与本轮边界

本轮 Op 完成条件是已注册格式在新 K/B 域的正确性和性能。原产品计划的最终量化比较留在
完整 Engine 接入后，以便结合 acceptance 和吞吐判断，而不是只根据文件大小决定：

1. 当前 W8 contractions + BF16 coefficient projections/codebooks 基线；
2. 适用矩阵的 Q4/Q5 组合；
3. W8 或更低精度 codebook 及其直接 gather/decode route；
4. 有真实 activation calibration 和完整 divisor 合同支持时的 NVFP4 候选。

这些后续候选不作为本轮 Op 修正的先决条件。若最终选择改变 allocation，必须同步更新
artifact reference、converter、Op 合同和 qualification。

当前 NVFP4 format 依赖匹配的 input divisor。不能把 BF16 DFlash2 source 无依据地直接包装成
NVFP4，也不能默认为 divisor `1`。若不建立校准合同，nvfp4 主 artifact 中的 DFlash2 module
继续使用 W8/BF16 是合法且清晰的混合方案。

量化质量以 end-to-end acceptance、tokens/round 和输出吞吐评估。Greedy 最终输出正确性仍由
target verification 保证；概率采样还要求 proposal `q` 与 accept/correction 消费的 `q` 完全
一致，不能用“最终文本看起来合理”替代验证。

## 6. 代码所有权和集成边界

### 6.1 Converter 与 artifact

`tools/convert/qwen3_8_27b` 拥有：

- 固定 DFlash2 source revision 和 config 校验；
- 81 个 BF16 source tensor 的完整 name/shape/dtype preflight；
- DFlash2 source-to-object recipes、fusion 和量化；
- 两个 Qwen3.8 inventories 的 bundle 追加；
- conversion report 中独立的 DFlash2 source/component 记录。

`src/artifact` 只增加 target-neutral 的 presence query，不拥有 DFlash2 namespace、可选性规则或
执行能力。

### 6.2 Exact 27B package

`src/targets/qwen3_6_27b` 拥有：

- Qwen3.8 DFlash2 固定 dimensions、storage profiles 和 exact binding；
- `DFlash2Weights<5>` 的已填充 target payload；
- Qwen3.8 profile 与 DFlash2 capability 的启动期匹配；
- target-specific leaf formats、workspace facts、Graph frontier profiles 和 Program bytes。

Qwen3.6-27B 与 Qwen3.8-27B 仍使用同一个 27B family Variant，但 capability 在 package/load
planning 阶段解析完毕。Family hot path 不读取 artifact identity string 或 object descriptor。

### 6.3 Qwen family runtime

`src/targets/qwen3_6` 继续拥有：

- target feature capture 和 context append；
- DFlash2 proposal schedule；
- shared target verify、generated-token transaction、commit/rollback 和 publication；
- prefix/continuation、state-image、workspace 和 CUDA Graph orchestration；
- compact batch 的 host/device ingress/egress。

DFlash 与 DFlash2 可以复用明确相同的 block-draft lifecycle，但保留独立 config、weight view、
proposal mathematics、accept input 和统计身份。不复制一份完整 Program 或请求调度器。

### 6.4 Ops

所有闭合 device transformation 位于 `include/ninfer/ops` 和 `src/ops`。Target/family schedule
只组合 public semantic Op，不调用私有 launcher/kernel，也不实现模型标记的 CUDA 数学。

## 7. 状态和执行结构

### 7.1 Persistent state

DFlash2 五层全部使用 window 2048 的 local cyclic K/V：

```text
5 layers x K/V x 8 heads x 128 x 2048 x 2 bytes = 40 MiB / StateImage
```

它没有 DFlash Full layer，因此：

- 不创建 DFlash Full paged-KV pool；
- 不申请 `backend_kv_pages`；
- context frontier 仍需独立记录，以证明 local cyclic state 对应哪个绝对前缀；
- assistant-boundary、Host replica、fork/copy 和 long-distance restore 继续通过 StateImage
  transaction 管理这 40 MiB local state；
- 不能为了复用当前 DFlash 代码而伪造一个零用途 full-cache row。

### 7.2 Decode round

稳定 round 的逻辑顺序为：

```text
materialize previous committed target input features into DFlash2 local context
    -> prepare [anchor, mask x K] at positions F..F+K
    -> main embedding alias
    -> five DFlash2 layers
       (dynamic conv -> non-causal SWA -> dynamic conv
        -> dynamic conv -> MLP -> dynamic conv)
    -> final norm
    -> full/optimized head top-16 per draft position
    -> selector conditional walk, with on-demand or precomputed edge scores
    -> retain selected path's sparse proposal q
    -> target verify physical width W=K+1, live width Q_b=P_b+1
       and capture five target feature layers
    -> greedy or sparse-q accept/correction
    -> produce L_b=A_b+1 provisional outputs
    -> Frontend preview selects N_b committed outputs
    -> commit N_b target input rows and retain their features as pending
    -> publish N_b outputs; the last output is the next unprocessed anchor
```

与现有 DFlash 一样，DFlash2 context 只提交 target 已确认的 prefix，不提交未验证 draft 的 query
K/V。Partial terminal 和 cancellation 使用 shared pending transaction，不引入每轮 40 MiB undo。

当最终提交 N>0 时，提交的 target input rows 是 `[anchor,d_1,...,d_(N-1)]`，continuation
hidden 选择 column N-1。Pending features 保存这些输入行，不能只按 accepted drafts 的数量
截取。保存 checkpoint、fork 或 Host replica 前先完成 context catch-up。

Proposal roots、verify records/logits 和 lane-owned pending features 按启动 W 查询容量；
pending storage 为 `[25600,W,N_lane]`。Context Op 的物理 W_c 与 counts=N_b 分开表达，
支持 batched W_c=1..16 和单请求 prefill W_c<=2048。五层 2048 ring 的持久容量与 K 无关。

## 8. Op 工作边界

### 8.1 已落地能力与当前缺口

- target hidden feature sink；
- `prepare_masked_block`；
- embedding、RMSNorm、RoPE；
- feature/context projection、QKV projection、MLP 和 residual composition；
- symmetric non-causal sliding-window attention；
- cyclic KV append和StateImage transfer；
- target verification、GDN record/replay 和 accepted-hidden selection；
- proposal-head alias/remap、pending publication 和 CUDA Graph infrastructure。

Window 2048 的 sliding attention/cyclic append、所需 Linear profiles 及七个新 Op 已存在。
首次实现将推荐 K=7 错误固化进多个 public contract、wrapper、kernel 和 workspace；原有
K=7 结果只说明对应输入点的实现情况，不说明新 K=1..15 域已经通过 qualification。

### 8.2 正式 Engine 接入前的 Op 治理

逐项范围、文件落点、修正方案和退出条件由新的 Op checklist 管理。当前主线为：

1. OP-01..08 已完成 dynamic-conv prepare/finish、RMSNorm+RoPE pair、norm-pack、linear-topk、
   selector、context materialization 和 sparse-q accept 的宽度、索引、dispatch、workspace 治理；
2. 保持现有融合 Op 的 public 输出/状态边界，允许内部按新 extent 选择融合或 materialized
   route；selector 的 realized-row/lattice 调度也由性能决定；
3. Context materialization 已移除不可观察 raw K 的强制 BF16 rounding，并完成完整数学
   oracle、count-envelope dispatch 和可变 context 宽度 qualification；cache storage cast 保持显式；
4. 对已支持新 extent 的 Linear、attention、GDN、head 和 copy/gather Ops 重新 qualification，
   重点覆盖 T<=128、U<=120、W<=16、request 边界和各 route seam。

Sparse-q accept 是概率正确性边界。`speculative_accept_greedy_drafts` 把 proposal 视为
one-hot，不能用于 DFlash2 stochastic path。Selector 产生的实际 FP32 q 直接传到 accept，
不在两个 Op 之间从 unary logits 或舍入后的 scores 重建 q。

## 9. 分阶段执行路线

### Phase 0：Source acquisition 与路线共识

状态：完成。

- 固定并下载上游 revision；
- 确认目录、文件、大小、81 个 BF16 tensors 和 config；
- 检查 NInfer 当前 DFlash/MTP、Qwen3.8 converter/binder，以及本地 SGLang/vLLM 支持；
- 确认本文中的 v2、兼容、backend 和 Vision 方向；运行 K 范围按 Section 2.2 修正。

### Phase 1：v2 合同与精确 storage map

工作：

- 更新 `artifact-container.md` 的 core/optional-bundle evolution 规则；
- 更新 `qwen3.8-27b-artifact.md` 的两个合法 inventory、DFlash2 source、对象和aliases；
- 在模型/Engine active references 中定义 DFlash2 config、attachment、state 和 public backend；
- 冻结两个 profile 的第一版 format allocation 和完整 object plan；
- 明确 legacy artifact 的固定 compatibility 路径。

退出条件：仅凭 active references 就能判断 legacy、新版、partial bundle 和 backend selection 的
合法性；没有未决问题会改变 converter inventory 或 runtime state shape。

### Phase 2：Converter、Binder 与新 Artifact

工作：

- 保存当前两份真实旧 artifact及sidecar；
- 为两个 converter 增加必需的 `--dflash2-model` source；
- 完成 config、source inventory、recipe coverage 和完整 preflight；
- 新 converter 始终追加完整 DFlash2 bundle；
- Binder 实现 core-only/core+bundle acceptance 和 conditional placement；
- 生成两份同 `model_id/weights_id` 的新 artifact。

退出条件：

- source 81 tensors 恰好全部被消费，且没有未声明 source；
- 新 artifacts 的 identity 不变，bundle 完整；
- legacy `none`/MTP load plan 正常，legacy DFlash2 只在选择时明确失败；
- 新 artifact 的 `none`/MTP route 没有 DFlash2 H2D；
- converter-side代表性decode证明每种stored tensor符合其format oracle。

Converter、binder 和已注册 format map 已存在；真实 artifact 的发布验收属于该阶段的产品
工作。运行 K 的修正不使既有 bundle 失效，也不要求为每个 K 生成 artifact。

### Phase 3：DFlash2 数学与 Op contracts

状态：本次先修正文档语义；受影响 public Op headers 和实现仍待按 checklist 修改。

工作：

- 定义 target feature capture 点、context K/V、非因果可见集和block边界；
- 定义dynamic convolution完整公式和prepare/finish中间值的可观察性；
- 定义top-16 tie、selector edge、path sampling、RNG domain和FP32 `q`；
- 定义 sparse proposal rejection/correction及target sampling filters/penalties的交互；
- 为每个闭合 Op 编写独立 FP32/FP64 或 exact oracle。
- 将 K=1..15、W=K+1、T=W*B、U=K*B 和 W_c/counts 分别落实到受影响合同；
  推荐值与模型固定维度不再承担执行域限制。

退出条件：Op contracts 足以独立实现和验证，不再依赖 SGLang/vLLM 的执行结果作为 oracle；
Program-owned lifecycle 与 Op-owned数学没有交叉所有权。

### Phase 4：Op 实现与资格验证

状态：旧 K=7 范围的首轮实现已落地；本阶段按新 checklist 重新打开。

工作：

- 完成已有 Ops 的 domain 修正、所有受影响 route 和 workspace 扩展；
- 覆盖 `K=1..15,B=1..8` 的真实 shapes、非二次幂 W、Graph capture/replay 和
  greedy/stochastic 分支；
- 为选定的 groupwise/NVFP4 artifact formats 补齐直接消费 route；
- 只对可能影响当前设计的实际热点做性能比较。

退出条件：checklist 的逐 Op 项闭合，生产 routes 在新域直接通过对应独立 oracle，性能比较
覆盖会改变选路的实际产品点。本阶段形成 Op 级证据；acceptance 和端到端吞吐在 Engine
闭环后测量，不以尚未接入的 Engine 作为 Op 阶段前置依赖。

### Phase 5：Eager Engine 闭环

工作：

- public option、startup validation、immutable model view和conditional residency；
- DFlash2 local state、workspace和round-state布局；
- Text/Vision prefill feature capture及context construction；
- 启动选定 `K=1..15` 的 proposal、selector、target verify 和 sparse-q accept；
- initial proposal、steady rounds、ordinary capacity tail、partial terminal和cancellation；
- prefix/continuation、assistant boundary、Host replica和长于2048 token的restore；
- mixed-length compact batches和`B=1..8`。

退出条件：

- DFlash2 target verification、accept/correction和状态提交分别通过其独立数学/事务 oracle；不同
  数值执行路径之间不要求greedy token逐项一致；
- stochastic route 满足同一 target 分布合同；
- target GDN、continuation hidden、DFlash2 local context及所有frontier在accept/reject/terminal后
  一致；
- image/video prompts完成同一闭环；
- none/MTP、35B DFlash和两个Qwen3.8 core route无回归。

### Phase 6：CUDA Graph、量化定稿与产品收尾

工作：

- 为任一启动选定 `K=1..15` 的 exact `B=1..8` 和必要 frontier tiers 支持 Graph capture/replay；
  一个 Engine 只构造其选定 K 的资源，不预先捕获所有 K，也不把短 K 扩成有效 W=16；
- 验证stable地址、workspace peak、40 MiB state-image transfers和无Full-pool资源规划；
- 比较不同 K、full/optimized proposal head 及 Section 5.3 量化候选的 acceptance、
  tokens/round 和 end-to-end tok/s；
- whole-inference attribution仍不清楚时才使用`nsys`，识别热点后才对必要kernel使用`ncu`；
- 验收两种 artifact 的已注册 DFlash2 allocation；仅在独立量化决策改变 allocation 时重新转换；
- 更新CLI、serving、performance、model card和active maintainer docs；
- 删除本文。

退出条件：完整产品矩阵通过，性能结论有RTX 5090、`sm_120a`、实际artifact、工作负载和汇总结果
作为解释上下文，没有已知in-scope问题阻止使用。

## 10. 最终验证矩阵

### 10.1 Artifact 与加载

- legacy groupwise：none、MTP成功，DFlash2按选择失败；
- legacy nvfp4：none、MTP成功，DFlash2按选择失败；
- new groupwise：none、MTP、DFlash2成功，inactive bundle不resident；
- new nvfp4：none、MTP、DFlash2成功，inactive bundle不resident；
- partial/unknown bundle：无条件拒绝；
- full/optimized proposal head：各自只materialize依赖闭包。

### 10.2 数值与状态

- dynamic convolution、top-16、selector和sparse rejection独立oracle；
- K=1..15 的宽度覆盖，包含非二次幂 W、相同 T 的不同 request 划分和最大 W=16/B=8；
- real-shape quantized-weight decode和代表性model intermediates；
- greedy exact target output；
- stochastic分布、penalty/filter和RNG domain；
- all-accept、first-reject、middle-reject、zero/short output和partial terminal；
- context跨2048 cyclic边界、prefix fork、Host restore和continuation；
- CUDA Graph与eager通过同一语义准则。

### 10.3 产品与性能

- Text、image和video；
- CLI、OpenAI和Anthropic serving；
- concurrency `1..8`、mixed request lengths和bounded FIFO；
- full/optimized head；
- 推荐 K=7、最大 K=15 和改变性能决策的较短 K；记录 K 与 W，避免两个上游参数的歧义；
- 与ordinary和现有MTP的acceptance、tokens/round、decode tok/s、load bytes和HBM residency比较；
- 未选择DFlash2时的性能和内存回归检查。

## 11. 已知高风险点与控制门

| Risk | Control |
|---|---|
| 推荐 K=7 再次进入 Op 数学域或 workspace 常量 | 新 checklist 逐 Op 修正并覆盖 K=1..15、T<=128、U<=120 |
| 奇数 W 的 tile 跨 request 或遗漏尾列 | 卷积按真实 W 断开；context projection 显式按 request 内 tile 调度 |
| P、W、N 混用而提交错误 feature/GDN 前缀 | provisional L 与最终 N 分开；只提交 N 个 target input rows |
| v2旧规则把完整inventory绑定到weights_id | Phase 1先正式引入core + exact optional bundle合同 |
| partial bundle被当作legacy | sentinel、逐项require和`Binder::finish()`三重闭合 |
| 复用one-hot accept导致概率错误 | 独立sparse-q accept contract和distribution oracle |
| selector path与rejection看到不同`q` | 同一FP32 q rows贯穿proposal与accept |
| all-local DFlash2被迫申请Full paged pool | resource/state布局显式支持zero full layers |
| 量化提高kernel速度但降低acceptance | 用end-to-end accepted tok/s决定最终allocation |
| optimized shortlist映射后索引错codebook | top-16先映射为global token id，再做selector gather |
| Vision多模态路径未完整接入 | 复用35B已验证的capture/position合同，Phase 5仍作为完成门 |
| 为DFlash2复制Program或调度器 | 只新增module-private proposal数学，复用family transaction |
| profiling 替代必要的 Op 测量 | 本阶段按 Op benchmark 选择方案；Engine 接入后才做端到端归因 |

## 12. 当前状态

| 项目 | 状态 |
|---|---|
| 上游下载与revision固定 | 完成 |
| config、tensor inventory和大小确认 | 完成 |
| NInfer/SGLang/vLLM边界检查 | 完成 |
| v2兼容路线与阶段共识 | 完成 |
| 35B DFlash/Vision错误互斥移除 | 完成；multimodal capture、逐lane target RoPE和真实image/video已验证 |
| artifact reference、converter/binder 实现 | 已存在；具体产品 artifact 验收不由本次文档修改宣告完成 |
| 算法与支持范围文档 | 已修正为 K=1..15；逐 Op 治理见新 checklist |
| public Op 合同、实现及重新 qualification | 待完成；旧 K=7 实现和测量不能覆盖新域 |
| Engine集成和最终验证 | 待开始 |

下一项工作是按新 checklist 完成 Phase 3/4 的 Op 修正与重新 qualification，随后进入
Phase 5 的正式 Engine 集成。本文与算法文档的修订不表示代码中的固定七步限制已经解除。
